<% content_for :title, "Transporte" %>

<%= render "shared/detail_list", items: {
      "Ruta"     => @info.route_name,
      "Vehículo" => @info.vehicle_plate,
      "Parada"   => @info.stop_name,
      "Hora de recogida" => @info.pickup_time
    } %>
