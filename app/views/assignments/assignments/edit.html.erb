<% content_for :title, "Editar tarea" %>

<%= render "shared/page_header", title: "Editar #{@assignment.title}" %>

<%= render "form", assignment: @assignment, url: assignments_subject_assignment_path(@subject, @assignment),
      rubric_templates: @rubric_templates %>
