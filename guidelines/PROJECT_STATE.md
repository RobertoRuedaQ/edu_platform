# edu_platform — Estado del proyecto (magro)

> **Qué es este documento.** La **fuente única de verdad del ESTADO ASENTADO** del proyecto
> `edu_platform`: invariantes de arquitectura decididos, mapa de dominios, RBAC. Es el que se
> lee/pega cada iteración para entender qué ya existe antes de decidir el próximo slice.
>
> **Split editorial (v1.5.1):** este doc solía cargar también el changelog completo y las
> narrativas de "cómo se construyó" cada pieza — eso ahora vive en **`HISTORIA.md`** (archivo
> append-only, hermano de este archivo), y se carga solo cuando hace falta el *por qué* de algo.
>
> **Split editorial #2 (v1.21.0):** el backlog ordenado y los guardrails operativos (que crecen con
> CADA slice) ahora viven en **`OPEN_PROCESS.md`** (otro hermano) — este documento queda enfocado
> en lo asentado (§1–§10), sin la sección que más rápido crecía.
>
> **El repositorio sigue siendo la fuente de verdad del código.** Ante discrepancia entre lo escrito
> aquí y lo que hay en disco, gana el repositorio, y se corrige este documento en la siguiente versión.

---

## Metadatos de versión

| Campo | Valor |
|---|---|
| **Versión del documento** | `v1.59.0` |
| **Fecha** | 2026-07-24 |
| **Tests** | 893 runs / 0 fallos / 1 skip preexistente (suite completa, en serie — ver `OPEN_PROCESS.md`) |
| **Estado en una línea** | Identidad real + RBAC/entitlement reales + portales de persona + autoservicio de staff + auditoría + CHECKPOINT E + #4 barrido (`teacher_management`/`group_management`/`schedules`-calificaciones/`counseling`) + matrícula por término real (`Schedules::ActiveTermEnrollmentScope`, v1.15.0) + `attendance` (v1.16.0) + `report_cards` (v1.17.0) + `finance` (v1.18.0) + `communication` completo (v1.19.0/v1.20.0) + **`assignments` — TRACK COMPLETO (v1.21.0–v1.26.0, ítem #6 del MVP)**: publicar/ver/calificar directo · entrega de texto · entregas grupales · adjuntos de entrega · materiales del docente · **rúbricas (v1.26.0): biblioteca reutilizable (`RubricTemplate`/`RubricCriterion`/`RubricLevel`/`RubricCellDescriptor`, tablas normalizadas), asociada vía `evaluation_method` (toggle bloqueado tras publicar, molde `group_work`), estructura congelada como snapshot jsonb al publicar (molde `price_tiers_snapshot`/`lines_snapshot`); calificar por rúbrica calcula y escribe la nota vía `GradeRecorder`/`GroupGrader` SIN CAMBIOS — la rúbrica nunca almacena la nota; portal ve nivel+descriptor por criterio, sin RBAC** + **`calendar`** (v1.27.0, ítem #7 del MVP): calendario compartido, audiencia institución-wide/grado/grupo por dos columnas de scope mutuamente exclusivas (mismo idioma que `role_assignments.scope_*`), tres ramas de `authorize!` sobre un solo permiso `calendar.manage`, merge de los vencimientos de `assignments` SOLO en el portal (`Calendar::Timeline`, entradas sintéticas nunca filas) + **`extracurriculars`** (v1.27.0, ítem #8 del MVP): actividades addon-gated (deporte/arte/refuerzo), cupo agregado con lock + índice único parcial, ambas vías de inscripción (colegio y acudiente), actividad paga = un `Finance::Charge` vía el puente `fee_cents`→`BigDecimal`/100 + **Portal del cuidador ampliado — ítem #9 del MVP (v1.28.0)**: el único hueco real era `attendance` (dominio v1.16.0 sin ninguna superficie de portal) — cerrado con `Attendance::StudentView` (query object, un solo camino de lectura reusado por ambas superficies) + `Portals::GuardianAttendanceController`/`StudentAttendanceController` (mismo molde relación-gated que `calendar`/`report_cards`, sin `authorize!`, fuera de `Navigation::Registry`). De paso: los hubs del portal (`guardian_students#show`, `student_portal#show`) enlazaban Boletines/Tareas/Finanzas pero NO Calendario/Actividades (construidos en v1.27.0 sin cablear su link desde el hub) — cerrado también. Y un hallazgo aparte: **no existía ningún enlace de cierre de sesión en TODA la app** (ni shell de staff ni portal) — `shared/_logout_link` (un `link_to` + `data-turbo-method: delete`, nunca `button_to`/`<form>`, para no chocar con los specs de portal que exigen "cero forms" en superficies de solo lectura) ahora vive en ambos layouts + **Provisioning de instituciones + correo real — ítem #10 del MVP (v1.29.0), CIERRA EL CAMINO CRÍTICO COMPLETO DEL MVP**: `Provisioning::ProvisionInstitution` (un solo flujo, una transacción) crea la institución (`Provisioning::CreateInstitution`, ya existía, usado antes solo por `db/seeds.rb`) Y bootstrapea su primer `institution_admin` real (`IdentityAccess::Bootstrap::FirstAdmin`: siembra el rol con TODO el catálogo de permisos salvo `cross_tenant_reports.view`, resuelve/crea la persona vía `Core::People::Resolver`, invita por el MISMO camino que `PeopleController#create`) — cierra un chicken-and-egg real que no tenía ningún camino de producción (solo pasaba en tests y en un rake QA-only). `ControlPlane::InstitutionsController#new/#create` expone esto (molde de `PlansController`, audit log incluido); `Core::Institution` ganó las validaciones de unicidad/inclusión que antes solo vivían como constraint de BD. Correo real: SMTP genérico vía `Rails.application.credentials.dig(:smtp, ...)` con fallback a `ENV["SMTP_*"]` (funciona con cualquier proveedor, cero gemas nuevas) en producción; development pasó de descartar correos en silencio a escribirlos en `tmp/mails/` (`delivery_method: :file`, nativo de la gema `mail`, cero gemas nuevas) + **S3b — emisión real de uso por dominio (v1.30.0, post-MVP, M1 medio cerrado)**: `ControlPlane::Usage::Ingest.emit` (variante resiliente de `.call` — traga `Rejected`, nunca rompe la acción de negocio si el addon no está sembrado/medido) ahora se llama de verdad desde 6 dominios: `Communication::MessageSender` (mensajes), `Attendance::RecordsController` (registros, uno por estudiante del roster), `Extracurriculars::EnrollmentCreator` (inscripciones), `Assignments::SubmissionRecorder` (entregas), `ReportCards::Publisher` (boletines, keyed por (estudiante,término) para sobrevivir la regeneración del snapshot), `Finance::ChargeCreator`/`PaymentRecorder` (transacciones). `PeriodCut`/`RollupJob` sin cambios (ya sumaban correctamente). `transportation` se corrigió a `metered:false` en el seed (Clase C, no tenía ningún evento real que medir — el seed anterior era aspiracional) + **RBAC intra-plano (v1.31.0)**: `platform_admins.role` (`super_admin`/`billing_ops`/`viewer`, default `super_admin` backward-compatible) + `ControlPlane::Authorization` (mapeo estático rol→permisos, no el esquema tenant-side) gatea catálogo/provisioning/billing/gestión-de-admins; lecturas siguen abiertas a cualquier admin activo + **Schedule recurrente (v1.32.0)**: `Core::Headcount::SnapshotAllJob`/`ControlPlane::Billing::PeriodCutAllJob`/`IdentityAccess::Invitations::ExpireAllJob` (fan-out per-institución nuevos) + `config/recurring.yml` corren `SnapshotJob`/`RollupJob`/`PeriodCutJob`/`Expirer` solos ahora (rollup 1am, snapshot 2am, sweep de invitaciones 4am diarios; corte de facturación 3am del día 1 de cada mes) — los rakes manuales siguen vivos, ya no son el único camino + **Hardening de billing (v1.33.0)**: `EXCLUDE USING gist` (btree_gist) en `subscriptions`/`institution_entitlements` prohíbe solapamiento de rangos de fecha por institución (más allá de "una activa a la vez", que los índices únicos ya cubrían) — destapó que `Entitlement#revoke!` nunca cerraba `valid_until` (a diferencia de `Subscription#end!`), corregido con el mismo molde + **`analytics_bi` (v1.34.0)**: `InstitutionDashboard` real (tenant-scoped, sin BYPASSRLS) — reemplaza el stub de números fijos; `CrossTenantReportRoster` (cross-tenant, `edu_bi_reader`) sigue en stub a propósito, diferido a su propio slice + **`analytics_bi` Slice 1 (v1.35.0, `guidelines/BI_DOCUMENT.md`)**: `CrossTenantReportRoster` real — primera conexión `BYPASSRLS` de la app (`AnalyticsBi::BiReaderRecord`, pool de conexión separado, nunca reconfigura el primario), agregados SIEMPRE agrupados por `institution_id` explícito, cada acceso auditado (`cross_tenant_report_accessed`). Ver `HISTORIA.md` v1.35.0. + **`analytics_bi` Slice 2 — Lente 1 "Mapa de Empatía Espacial" (v1.36.0, `guidelines/BI_DOCUMENT.md`)**: geometría de aula net-new en `group_management` (decisión A2 — `GroupManagement::ClassroomLayout`/`SeatAssignment`, efectivo-fechadas, append-only, tres `EXCLUDE gist` de no-solapamiento/no-double-booking; reconfiguración vía `ClassroomReconfigurer`/`SeatAssigner` con `requires_new` savepoint, gateada por `groups.manage`) + capa de calor derivada in-memory de T1 (notas/asistencia, `AnalyticsBi::Lens::SpatialHeatmap`, HSL server-side por asiento) renderizada como SVG server-rendered (`AnalyticsBi::Svg::SeatGrid`) + dimming vía Stimulus sin round-trip; superficie de lectura en `analytics_bi` gateada por el nuevo `hps.classroom.view` (molde #4, tenant-scoped, NO cross-tenant). Aura overlay (§5.7) diferido a Slice 3. Ver `HISTORIA.md` v1.36.0. + **`analytics_bi` Slice 3 — Lente 5 "Auras de Cuidado" (v1.37.0, `guidelines/BI_DOCUMENT.md`)**: proyección `care_auras` net-new EN `analytics_bi` (tenant-scoped, RLS `ENABLE+FORCE`, `uuidv7()`, índice líder `institution_id`, efectivo-fechada) — enum CERRADO `aura_kind` (`string`+CHECK, molde `extracurriculars.kind`) + `guidance_text` (cero PII clínica) + `authored_by_counselor_id` (FK a `institution_users`, identidad; **cero asociación a `counseling`**). Escrita SOLO por `AnalyticsBi::Aura::Projector` invocado DESDE `counseling` (`Counseling::CareAurasController`, gate `counseling.write` — el key EXISTENTE), append-only (molde `SeatAssigner`); índice único PARCIAL "una activa por (student, kind)". El docente la lee como un ícono «♥» ADITIVO sobre la Lente 1, gate `hps.aura.view` (permiso nuevo, 2ª mitad del split de dos lados), vía `AnalyticsBi::Lens::AuraScope` que devuelve un `Data` de 4 campos (allowlist por construcción). **Aislamiento clínico probado a nivel de MODELO** (SQL tap + estructura de asociaciones): ninguna query del docente toca `counseling_cases`/`session_notes`/`referrals`. Ver `HISTORIA.md` v1.37.0. + **`analytics_bi` Slice 4 — temporalidad año-a-año (v1.38.0, `guidelines/BI_DOCUMENT.md`)**: `GroupManagement::StudentPlacement` (`student_placements`, net-new, decisión A1 resuelta a favor de `group_management`) efectivo-fechada/append-only con `EXCLUDE gist` de no-solapamiento, escrita SOLO por `GroupManagement::SectionReassigner` — el único seam que mantiene `students.section_id` (caché vivo) y la historia en lock-step (`memberships_controller` refactorizado para pasar por él); `GroupManagement::PlacementBackfill` (idempotente, reusa la auto-sanación del seam) coloca el primer placement de cada estudiante ya matriculado. `AnalyticsBi::HpsTermSnapshot` (`hps_term_snapshots`, net-new EN `analytics_bi`, molde `report_cards.lines_snapshot`) congela por `(student, academic_term)` un payload jsonb (nota/asistencia/heat/placement) vía `AnalyticsBi::Hps::Snapshotter` (TERM-scoped, no rodante) y el fan-out `HpsTermSnapshotJob`/`HpsTermSnapshotAllJob` (guardrail v1.32.0, no cableado a `config/recurring.yml` — fin-de-término es dependiente de dato). Ver `HISTORIA.md` v1.38.0. + **`analytics_bi` Slice 5 — instrumento de carácter, T2 (v1.39.0, `guidelines/BI_DOCUMENT.md`)**: el instrumento staff-autoría molde rúbrica (`CharacterFramework`→`CharacterEvaluation` con `framework_snapshot` congelado→`CharacterDimensionScore`) y, separado, el camino de pares/acudientes (`peer_appreciations`, catálogo cerrado `peer_appreciation_tags`, umbral de agregación antes de surfacear, XOR de identidad del dador, DOS índices únicos parciales anti-brigading, moderación append-only auditada) gateado por `AnalyticsBi::CharacterProgramConsent` — el **primer consentimiento del codebase** (el molde citado `assignments.requires_consent` no existía, corrección de referencia). Permisos nuevos `hps.character.author`/`hps.character.moderate`. Ver `HISTORIA.md` v1.39.0. + **`analytics_bi` Slice 6 — Lente 2 "Ficha de Personaje" (v1.40.0, `guidelines/BI_DOCUMENT.md`)**: la primera lente de AUTOSERVICIO (sin RBAC, sin nav — gate por `GuardianScope`/`StudentSelfScope`), CERO tablas nuevas (consumidor puro del Slice 5): `AnalyticsBi::Lens::CharacterCard` (radar + brújula + medallas + crecimiento intra-estudiante) leído por `AnalyticsBi::Svg::RadarChart` (molde `SeatGrid`, ordinal SOLO geométrico, nunca un número visible); más la UI de consentimiento del acudiente y la superficie de dar un aporte de par, ambas deferidas del Slice 5. Ver `HISTORIA.md` v1.40.0. + **Matrícula por materia, acción deliberada (v1.41.0, `guidelines/CLOSURE_PLAN.md` §4.4)**: hasta ahora `Schedules::Enrollment` solo nacía como efecto secundario de `GradeEntriesController#create` (la primera nota de un estudiante) — `Schedules::EnrollmentsController#create` expone la MISMA llamada idempotente (`find_or_create_by!`) como una acción propia, sin necesitar una nota, reusando `grades.write` (sin permiso nuevo). Sin acción de retiro/withdraw todavía — el índice único de `enrollments` no está scoped por `status` como el de `activity_enrollments`, así que un flujo real de retiro necesita su propia decisión de migración. Ver `HISTORIA.md` v1.41.0. + **`analytics_bi` Slice 7 — Lente 3 "Constelación de Afinidades" (v1.42.0, `guidelines/BI_DOCUMENT.md`)**: `AffinityTaxonomy`/`StudentAffinity` net-new (T2), scope institución-wide o `department_id` (columna nueva en `affinity_taxonomy`, documentada, reusando el lector `:department` existente — nunca un `scope_type` nuevo); búsqueda solo de talento vía columna `search_tsv` GENERADA nativa de PG18 (nunca de persona, probado estructuralmente sobre el SQL); grafo ensamblado por `ConstellationBuilder` (iniciales al cliente, nunca ranking); **primera librería JS real del codebase** (Cytoscape.js pinneada vía importmap, progressive enhancement real con `try/catch` sobre un `import` dinámico); primer consumidor de `IdentityAccess::PermissionCheck#scope_for`. Ver `HISTORIA.md` v1.42.0. + **`analytics_bi` Slice 8 — Lente 4 "Núcleo Familiar" (v1.43.0, `guidelines/BI_DOCUMENT.md`) — LAS 8 LENTES/SLICES DEL ROADMAP ORIGINAL, CERRADAS**: `AnalyticsBi::Household`/`GuardianRelationship` net-new (T2), extienden `guardian_students` 1:1 (nunca lo duplican); grafo orbital (estudiante-centro + cuidadores + hermanos) vía `FamilyGraph`, Cytoscape.js REUSADO (segunda vez, cero librería nueva); hermanos detectados por query (`FamilyCoreScope`, cero tabla nueva, exige cuidador primario compartido); `custody_kind` segregado por construcción (nunca en el payload del grafo, probado estructuralmente — tercera instancia de esta disciplina); "tensión del vínculo" (`BondTension`, decisión A6 resuelta: viva, nunca persistida) de señales T1 reales (login/lectura de mensajes); alerta de lazos fraternales (`SiblingBondAlert`, read-model auditado, heurística documentada como placeholder). `hps.family.view` institución-wide ÚNICAMENTE, sin entrada de `Navigation::Registry` (enlace desde `group_management/students#show`, evitando un directorio de personas). Ver `HISTORIA.md` v1.43.0. + **`core`: primera UI de términos académicos (v1.44.0, `guidelines/CLOSURE_PLAN.md` §4.2)**: `Core::AcademicTermsController` (crear/editar/activar/cerrar, permiso único `academic_terms.manage`) — hasta ahora `Core::AcademicTerm` solo se creaba por `db/seeds.rb`/consola. "Cerrar término" es TAMBIÉN el disparador manual confirmado por el owner para `AnalyticsBi::HpsTermSnapshotJob` (molde `report_card.publish`, nunca un reloj/cron). CHECK nuevo `ends_on >= starts_on` en BD (antes solo validación de app implícita, nunca alcanzable sin una UI). Ver `HISTORIA.md` v1.44.0. + **`student_support`: `disciplinary_logs` real (v1.45.0, `guidelines/CLOSURE_PLAN.md` §3.1/Fase B)**: `StudentSupport::DisciplinaryLog` reemplaza `DisciplinaryLogRoster` (stub con filas falsas — `#create` era un no-op literal). Molde `counseling`: tenant-scoped, autor identity-accountable (`reported_by_institution_user_id`, RESTRICT), append-only (sin update/destroy), auditado (`disciplinary_log.recorded`). Reusa el permiso `disciplinary_logs.manage` YA existente (ningún permiso nuevo). Sin superficie de portal (misma postura que `counseling`, nunca expuesto a acudientes/estudiantes). `medical_history`/`accommodations` siguen diferidos como tier C. Ver `HISTORIA.md` v1.45.0. + **`analytics_bi` — Lente 6 "Alertas Tempranas" (v1.46.0, `guidelines/BI_DOCUMENT.md` §5.8 amendment MAJOR, `guidelines/CLOSURE_PLAN.md` Fase C) — CRITERIO DE HECHO END-TO-END COMPLETO (los 9 procesos de `CLOSURE_PLAN.md §1` son todos reales)**: `AnalyticsBi::Lens::EarlyWarningScope`, cero tabla nueva, sintetiza `heat` de `hps_term_snapshots` + `disciplinary_logs` recientes + `SiblingBondAlert` reusado (auras activas solo como contexto, nunca disparan por sí solas); `hps.early_warning.view` (institución-wide únicamente) solo abre la superficie — cada señal individual REVALIDA su propio permiso de origen (`disciplinary_logs.manage`/`hps.family.view`/`hps.aura.view`), mismo criterio que `SupportDashboardController` ya establecía. Entrega NUNCA automática — solo un enlace a `communication`/Lente 4 para que un humano decida. Construida sin una regla de negocio real confirmada, por autorización explícita del owner de asumir la opción recomendada en cada decisión pendiente (heurística documentada como PLACEHOLDER). Ver `HISTORIA.md` v1.46.0. + **`cafeteria`: chequeo de alérgenos real, primer incremento de Fase D (v1.47.0, `guidelines/CLOSURE_PLAN.md` Fase D)**: `Cafeteria::DietaryRestriction` ya era real (tabla + modelo + seed, `~5%` de estudiantes) pero `CheckoutsController` seguía leyendo un stub paralelo (`DietaryRestrictionRoster`, retirado) — ahora el bloqueo de compra por alergia corre 100% contra datos reales del estudiante. `Menu`/`Purchase`/`StudentAccount` (aún stub) quedan fuera de este incremento a propósito — el resto de Fase D (`student_support` medical/accommodations, `transportation`, timetable, `admissions`/library) sigue sin construir, driver-based. Ver `HISTORIA.md` v1.47.0. + **`student_support`: `medical_history`/`student_allergies`/`accommodations` reales, segundo incremento de Fase D (v1.48.0, `guidelines/CLOSURE_PLAN.md` Fase D)**: tres tablas net-new (`medical_histories`, `student_allergies`, `accommodations`, todas RLS `ENABLE+FORCE`) reemplazan los dos stubs paralelos restantes de `student_support` (`MedicalHistoryRoster`/`AccommodationRoster`, retirados); patrón de dos tiers reusado (`medical_history.view` completo vs. `medical_history.view_summary` resumen); `AccommodationsController` ganó `new`/`create` (antes solo `index/edit/update`, sin ningún camino para crear la primera fila — mismo hueco operativo que motivó el CRUD de `academic_terms` en v1.44.0); sin cifrado de columna, misma postura que `counseling` T3 (verificado por grep). Ver `HISTORIA.md` v1.48.0. + **`transportation`: rutas/paradas/pasajeros/abordaje reales, tercer incremento de Fase D (v1.49.0, `guidelines/CLOSURE_PLAN.md` Fase D)**: el dominio peor parado de todos — nav real ("Rutas"/"Abordaje") + controllers/rutas reales sobre `RouteRoster`/`RiderRoster`, dos `Data.define` con filas 100% inventadas. Cuatro tablas net-new (`routes` con `driver_staff_member_id` FK real a `StaffManagement::StaffMember`, `route_stops`, `route_riders` con `shift` am/pm, `boarding_events` append-only). **Hallazgo mayor**: el scope `:route` de `Authorization::Assignment::SCOPE_READERS` nunca estuvo conectado al motor real de RBAC — `role_assignments` no tenía `scope_route_id`, así que "un conductor ve solo su propia ruta" solo existía en tests (`Authorization::StubResolver`). Cerrado en el mismo incremento: columna nueva + `IdentityAccess::PermissionCheck#scope_type_for`/`#scope_id_for` ahora reconocen `:route`, `grant_role!` gana su rama. Portales de acudiente/estudiante reescritos sobre `Transportation::RiderView` (molde `Attendance::StudentView`) vía `GuardianScope`/`StudentSelfScope`. Ver `HISTORIA.md` v1.49.0. + **`schedules`: horario institucional/salones reales, cuarto incremento de Fase D (v1.50.0, `guidelines/CLOSURE_PLAN.md` Fase D)**: el segundo dead-end activo — nav real ("Horario institucional") + controllers/rutas reales sobre `RoomRoster`/`ScheduleEventRoster`, 100% `Data.define`. Dos tablas net-new (`rooms`, `meeting_patterns` — patrón PLANO `day_of_week`+`starts_at`/`ends_at`, sin tabla `periods` compartida). El doble-booking de un salón se PERMITE (sin `EXCLUDE gist`) — `Schedules::MeetingPatternPresenter` CALCULA el conflicto real (solapamiento de salón O de sección, institución-wide) en vez de reflejar el flag inventado del stub; reusado por `ScheduleScope`/`TimetableScope`/`RoomsController#show` (que de paso corrige un bug latente: filtraba por `room_name` string, ahora por `room_id`). Ver `HISTORIA.md` v1.50.0. + **`cafeteria`: Menú/Compra/Saldo reales, quinto incremento de Fase D (v1.51.0, `guidelines/CLOSURE_PLAN.md` Fase D) — cierra `OPEN_PROCESS.md` ítem #1**: los dos últimos stubs del dominio (`MenuRoster`/`StudentAccountRoster`) retirados; una compra es un `Finance::Charge` contra la ÚNICA cuenta compartida (`finance.student_accounts` — sin wallet propio de cafetería, mismo precedente que `Extracurriculars::EnrollmentCreator`), nunca una deducción de crédito prepago (no existe flujo de "recarga" en la app). Tres tablas net-new (`cafeteria_menu_items`, `cafeteria_purchases`, `cafeteria_purchase_lines`, todas RLS `ENABLE+FORCE`); dinero NUEVO → `*_cents bigint` (F6), `MenuItem#price_amount`/`Purchase#total_price_amount` el único puente cada uno (molde `Activity#fee_amount`). `Cafeteria::PurchaseRecorder` (molde `ChargeCreator`/`PaymentRecorder`/`EnrollmentCreator`): `account.lock!` transaccional, idempotencia propia (`Cafeteria::Purchase.idempotency_key`) además de la de `Finance::ChargeCreator`, delegado dentro de la MISMA transacción. Sin UI de autoría de menú (seeded como `dietary_restrictions`, misma postura que `character_frameworks`). Los dos portales (antes 100% stub) ahora leen `Finance::StudentAccount` real vía `GuardianScope`/`StudentSelfScope` + las propias `Cafeteria::Purchase` del estudiante como movimientos. M1 metering para `cafeteria` sigue sin cablear a propósito (mismo criterio que `transportation`/`schedules` en v1.49.0/v1.50.0). Ver `HISTORIA.md` v1.51.0. + **M1 — metering real para `cafeteria`/`transportation` (v1.52.0, `OPEN_PROCESS.md` ítem #5)**: ambos dominios tenían ya un evento de negocio real (`Cafeteria::Purchase` desde v1.51.0, `Transportation::BoardingEvent` desde v1.49.0) pero sin `ControlPlane::Usage::Ingest.emit` cableado — cerrado ahora, molde S3b (v1.30.0) sin cambios: `Cafeteria::PurchaseRecorder` emite una unidad "compras" por venta real (misma idempotencia que ya protegía la venta); `Transportation::BoardingEventsController` emite una unidad "abordajes" por fila real de `boarding_events` (cada escaneo es su propio evento distinto — a diferencia de asistencia, no hay `(ruta,estudiante,día)` que colapsar). `seed_catalog.rb` los pasa a `metered:true` conservando su `monthly_fee_cents` existente (híbrido base+overage, sin romper ninguna suposición de pricing previa). M1 queda abierto solo para `student_support`/`counseling`/`analytics_bi`/`schedules`-timetable. Ver `HISTORIA.md` v1.52.0. + **Onboarding: purga real de `roster_import_rows` post-commit (v1.53.0, `OPEN_PROCESS.md` ítem #2)**: `Core::RosterImport::RowPurger` borra las filas (PII cifrada en `raw` jsonb) de batches `committed` fuera de la ventana de retención (`RETENTION = 30.days`, PLACEHOLDER sin regla de negocio real); `committed_at` (columna nueva en `roster_import_batches`, estampada por `Committer`) es el ancla explícita, nunca `updated_at`. `Core::RosterImport::PurgeRowsAllJob` (molde `IdentityAccess::Invitations::ExpireAllJob`, `config/recurring.yml` 5am diario) hace el fan-out por institución. Purga DIFERIDA a propósito (nunca síncrona con el commit) — `RosterImportsController#show` es la única pantalla de detalle de fila, pre-commit Y post-commit a la vez, y necesita la ventana para que staff revise el resultado de su propio import. Los otros tres ítems de onboarding hardening (batch-invite, async parse+validar, webhook de `BounceHandler`) siguen gateados. Ver `HISTORIA.md` v1.53.0. + **`library` real, primer dominio greenfield de Fase D (v1.54.0, `guidelines/library_prompt.md`, `OPEN_PROCESS.md` #1 — confirmado explícitamente por el owner)**: sin stub previo del que convertir, a diferencia de todos los incrementos anteriores de Fase D — el owner entregó la especificación funcional completa. Tres tablas net-new (`library_resources`/`library_resource_copies`/`library_loans`, todas RLS `ENABLE+FORCE`). Dos correcciones a la spec verificadas contra el repo real antes de construir: `Core::Person` (mencionado en la spec) no existe — es `Core::User`; y el "prestatario" solo nombraba `institution_user_id`, pero la propia UX de la spec exige que estudiantes vean sus préstamos en portal — corregido con `borrower_institution_user_id` XOR `borrower_student_id` (molde `conversation_participants`). `Library::LoanRecorder`/`ReturnRecorder` bloquean `copy` (nunca `loan`) — primer molde de este codebase que guarda la propia columna de estado de la fila bloqueada, no un COUNT/SUM de filas hijas como `EnrollmentCreator`/`ChargeCreator`; respaldado por índice único parcial `(institution_id, copy_id) WHERE status='active'`. Límites de préstamo simultáneo (`MAX_ACTIVE_LOANS_STUDENT`/`STAFF`) y multas por mora quedan PLACEHOLDER/diferidas (sin settings-por-institución, A3). Portales estudiante/acudiente (molde `cafeteria`, summarizan sin nesting per-child) leen préstamos propios + catálogo. Metering M1 cableado desde el día uno. Admissions (increments 2-3 de Fase D) sigue pendiente. Ver `HISTORIA.md` v1.54.0. + **`admissions` Incremento 2 (base), segundo dominio greenfield de Fase D (v1.55.0, `guidelines/library_prompt.md`, `OPEN_PROCESS.md` #1)**: cuatro tablas net-new (`admission_campaigns`/`admission_applicants`/`admission_applications`/`admission_documents`, todas RLS `ENABLE+FORCE`). Dos correcciones al overview original verificadas contra el repo real: la primitiva de "admitir un estudiante nuevo" es `GroupManagement::Student.create!` (molde `Core::RosterImport::Strategies::Students`), nunca `Schedules::Enrollment` (matrícula de MATERIA, no de colegio); y un aspirante no es cobrable vía Finance antes de ser un `Student` real — el `Finance::Charge` de la cuota nace SOLO en `Admissions::AcceptanceConverter`, nunca al radicar. Un aspirante nunca toca `Core::User`/`Core::InstitutionUser` hasta la aceptación. `AcceptanceConverter` bloquea `application` (nunca `applicant`/`campaign`), crea el `Student`, liga al acudiente (`Core::People::Resolver`+`Core::GuardianStudent`, molde `Strategies::Guardians`) y cobra la cuota en la MISMA transacción (molde `Extracurriculars::EnrollmentCreator`). `Application#grade_level_id` aliasea `target_grade_level_id` (molde `Route#route_id`), habilita el scope RBAC `:grade_level` ya real sin migración. Metering M1 cableado desde el día uno. Ítem de backlog nuevo pedido por el owner (`OPEN_PROCESS.md` #7): `finance` necesita su propio diseño para procesar/aprobar estos cobros. Incremento 3 (pasos configurables + tracker público por token) sigue pendiente. Ver `HISTORIA.md` v1.55.0. + **`admissions` Incremento 3 (pasos configurables + tracker público), CIERRA `admissions` COMPLETO (v1.56.0, `guidelines/library_prompt.md`, `OPEN_PROCESS.md` #1)**: `admission_step_templates`/`admission_application_steps` — instancias MUTABLES reales por fila (nunca un snapshot jsonb como rúbricas, el estado de un paso cambia con el tiempo). Tracker público (`/admisiones/solicitud/:token`, subdominio-scoped, molde `Invitations::Issuer` — solo el digest se persiste) — cero precedente en el repo de una página pública que redacta CAMPOS de un registro por lo demás legible (toda otra superficie gatea FILAS por RBAC/relación); resuelto con `Admissions::Tracker::PublicView` (molde `AnalyticsBi::Lens::AuraScope`), un `Data` allowlist sin accessor para `private_notes`/`evaluator` — probado con un test de integración explícito (nota/evaluador nunca en el body). `ApplicationSubmitter` (extendido, no un servicio nuevo) fan-out de pasos + genera el token + encola `Admissions::TrackerMailer`, todo gateado por `previously_new_record?`. Ruta pública TOP-LEVEL (molde `invitations`, nunca anidada en el namespace RBAC-gated). Cero permiso nuevo (reusa `admissions.campaigns.manage`/`admissions.applications.manage`). Stepper vainilla nuevo (`shared/_stepper.html.erb`, cero librería externa). Ver `HISTORIA.md` v1.56.0. + **RBAC real: roles/asignaciones + fechado por término + role de persona (v1.57.0, `OPEN_PROCESS.md` B2/P2)**: resolver B2 reveló que `IdentityAccess::AssignmentsController#create` (única UI real de asignación) era un STUB sin persistencia, y que crear un `Role` tampoco tenía UI (`resources :roles, only: %i[index show]`) — el owner confirmó llevarlo hasta el final. `role_assignments.academic_term_id` (nullable, opt-in): cuando se setea, `Core::AcademicTermsController#close` capa `valid_until` al `ends_on` del término (solo si sigue abierta), dentro de la MISMA transacción que ya usaba para el snapshot del HPS — no existe mecanismo de eventos en este repo. `RolesController` gana CRUD completo (permisos agrupados por prefijo de `key`, roles `system` rechazan edición); `AssignmentsController`/`UsersController` reescritos contra `Role`/`RoleAssignment`/`Core::InstitutionUser` reales (antes `RoleRoster`/`UserRoster`/`RoleAssignmentRoster`, los tres retirados); `IdentityAccess::RoleCatalog` valida `assignable_scope_types` (5 roles canónicos; cualquier rol custom admite cualquier alcance) y alimenta `qa_seed.rake` en vez del stub. Un `RecordNotUnique` se atrapa dentro de un SAVEPOINT (`transaction(requires_new: true)`, molde `AcademicTermsController#activate`) — sin esto, Postgres ya había abortado la transacción completa de la request. P2 (alcance achicado por el owner): filtro por `institution_users.role` en "Personas" + fila "Rol de cuenta" en "Mis datos"; el organigrama/jerarquía que el owner también pidió queda como su propio ítem de backlog nuevo, sin construir. Ver `HISTORIA.md` v1.57.0. + **Onboarding: batch-invite tras alta de acudientes por roster import (v1.58.0, `OPEN_PROCESS.md` ítem #1a)**: `Core::RosterImport::Strategies::Guardians#commit_row!` invita al acudiente SOLO cuando `Core::People::Resolver#new_user` es `true` (molde `Bootstrap::FirstAdmin`), atribuido a quien subió el batch (`Core::RosterImportBatch#created_by`, ya existía sin usarse, ahora fluye vía `Strategy.for(kind, institution:, created_by:)` — nuevo kwarg opcional, kind-agnóstico: solo `Guardians` lo usa). Idempotente gratis: un acudiente con N hijos genera N filas, pero `new_user` solo es `true` en la que efectivamente lo crea — las demás, y cualquier re-commit posterior, nunca re-invitan. Ver `HISTORIA.md` v1.58.0. + **Billing: `BillingPeriod` propio + registro manual de abonos (v1.59.0, `OPEN_PROCESS.md` ítem #2 — cierra el pendiente gateado desde v1.33.0)**: `ControlPlane::BillingPeriod` (nueva, GLOBAL, misma postura que `invoices`) reemplaza `period_start`/`period_end` como columnas propias de `invoices` — ahora es `billing_period_id` (FK restrict), con `#period_start`/`#period_end` delegados vía `alias_method` para que las vistas/formulario existentes no cambien. `PeriodCut`/`PeriodCutJob`/`PeriodCutAllJob` reciben `billing_period:`/`billing_period_id:`, nunca fechas sueltas — un re-cut o un re-run del mes nunca duplica el período (`find_or_create_by!`). `ControlPlane::Payment` (tabla `control_plane_payments`, prefijo por la colisión de nombre con `Finance::Payment`) es un registro MANUAL sin `status` (existir es el único estado con comportamiento real), vía `ControlPlane::Billing::PaymentRecorder` (molde `Finance::PaymentRecorder` sin `lock!`, `paid_cents`/`balance_due_cents` son un `sum` computado, nunca una columna mutable). Prorrateo de `addon_fee` y edición manual de líneas: decisión explícita de NO construir (tarifa plana / `recut` ya alcanza), documentadas, no diferidas. Ver `HISTORIA.md` v1.59.0. |

### Convención de versionado de ESTE documento

- **MAJOR** (`vX.0.0`): cambia una decisión de arquitectura asentada (tenancy, modelo de identidad, planos), o se reestructura el mapa de dominios.
- **MINOR** (`v1.X.0`): se cierra una iteración (se implementa un módulo/dominio), se resuelve un borde abierto, o se añade un dominio/plano.
- **PATCH** (`v1.0.X`): correcciones, aclaraciones, ajustes de redacción, reconciliación con el repo, reorganización editorial.

Cada cambio de código se registra en el **Changelog de `HISTORIA.md`**. Nunca se borra historia: las decisiones revertidas se marcan como *supersedidas*, no se eliminan.

---

## 1. Resumen ejecutivo

`edu_platform` es un **SaaS educativo multi-tenant** (K-12 y educación superior) construido como
**monolito Rails 8 vanilla**, optimizado sin piedad para un **desarrollador solo que lo mantendrá
por años**: código aburrido, explícito y greppable sobre abstracciones ingeniosas. Filosofía
37signals. Se agota PostgreSQL nativo + Rails nativo antes de proponer cualquier herramienta externa.

El sistema tiene **dos planos** con reglas de seguridad opuestas:

- **Plano de inquilino** (la app del colegio): dominios de negocio scoped por `institution_id` bajo RLS.
- **Plano de control** (panel de plataforma): cross-tenant, por encima de RLS, solo super-admin — catálogo de addons, entitlements y billing por uso.

Una institución "enciende" dominios comprando addons (entitlement), y dentro de ella el acceso se
resuelve por **RBAC con scope** (rol + alcance). Son dos compuertas **en serie**: primero *¿la
institución puede?* (`entitled?`, real desde S2b), luego *¿el usuario dentro puede?* (`authorize!`,
real desde P1, `IdentityAccess::PermissionCheck`). **Ambas compuertas son reales.**

---

## 2. Stack bloqueado (no sustituir)

| Capa | Elección | Notas |
|---|---|---|
| **Ruby** | 4.0.x con **YJIT** | ZJIT es experimental — **NO usar ni depender de él**. |
| **Rails** | 8.1.x | Autenticación **nativa** de Rails 8 (`has_secure_password`, `Session`, concern `Authentication`, `Current`). **Sin Devise.** **Ya implementada.** |
| **PostgreSQL** | 18 GA | Nativo: `uuidv7()`, RLS + FORCE, `WITHOUT OVERLAPS`, `UNIQUE NULLS NOT DISTINCT`, JSONB, FTS nativo, pgvector. **Sin extensiones para UUID.** |
| **Async / cache / cable** | Solid Queue / Solid Cache / Solid Cable | **Sin Redis, sin broker, sin Sidekiq, sin Elasticsearch.** Correos ya van por Active Job sobre Solid Queue (`deliver_later` en `OtpMailer`/`InvitationMailer`); importación pesada (roster) todavía no existe. |
| **Front-end** | Propshaft + importmap (sin Node/build), turbo-rails + stimulus-rails, CSS vanilla con `tokens.css` y `@layer` | **Sin Tailwind, sin Sass, sin gema de componentes, sin icon-font.** |
| **Tests** | Minitest (default) | **Sin RSpec.** |
| **Autorización** | RBAC casero sobre PG | **Sin Pundit / CanCan / rolify.** `IdentityAccess::PermissionCheck` es el resolver real (P1, cerrado) — real-only, fail-closed: sin `RoleAssignment` que aplique, cero permisos. |
| **Eventing cross-dominio** | In-process (`ActiveSupport::Notifications` / bus liviano en app + service objects en los bordes) | **Nunca** un broker distribuido. |

> **PK universal:** UUID vía default de columna PG18 `default: -> { "uuidv7()" }`. Los IDs legibles de
> negocio (`student_code`, etc.) son columnas aparte, tenant-scoped, con unique compuesto.
> Soft-delete vía `deleted_at` donde ya se use. `institution_users` usa en cambio `status`
> (`active`/`suspended`, CHECK en BD) en lugar de soft-delete — una membresía suspendida sigue
> existiendo y siendo auditable, no desaparece.

---

## 3. Arquitectura asentada (verdad de base — construir encima, NO reabrir)

### 3.1 Tenancy — row-level (shared-schema)

- Toda tabla tenant-owned lleva `institution_id`.
- **RLS es el backstop de BD** (`ENABLE` + `FORCE ROW LEVEL SECURITY`, para que muerda incluso al owner de la tabla).
- El **scoping primario** es explícito en Rails vía **Query objects / `Tenant::Resolver` + `Tenant::Guc`** (nombres reales en el repo — no `CurrentTenant`), **NUNCA `default_scope`**.
- Predicado RLS: `institution_id = current_setting('app.current_institution_id')::uuid`, con `WITH CHECK` gemelo en INSERT/UPDATE.
- El GUC se fija con `SET LOCAL` dentro de una transacción por request (`TenantScoped#within_tenant`, `around_action`) o por job (`ApplicationJob#around_perform`, con `Tenant::Guc.reset!` explícito — ver Guardrails).
- Existe un **seam de resolución de tenant** (`Tenant::Resolver::SubdomainStrategy`, para futuro sharding horizontal). El sharding en sí **NO** se construye (YAGNI a escala solo-dev).

### 3.2 Identidad — global, multi-institución

- `institutions` y `users` son **GLOBALES** (sin RLS). Todo lo demás tenant-owned es **tenant-scoped**.
- **Una persona = un `users`** (login único por `email`, `citext`, único global); puede pertenecer a varias instituciones vía `institution_users`. Confirmado y en producción de código (⚠-1, cerrado — ver `HISTORIA.md`).
- Resolución de tenant **por subdominio** (`institutions.slug`), vía `Tenant::Resolver`.
- `student` y `guardian` son **entidades-persona, NO roles RBAC**. Su acceso se resuelve por **relación** (`students.user_id`, `guardian_students`), no por `role_assignments`. Un menor de K-12 puede existir sin `user_id`; un acudiente siempre tiene login (⚠-2, cerrado — ver `HISTORIA.md`). La tabla `guardian_students` real ya existe (dominio `core`), en paralelo a la legacy `student_support.student_guardians` (ambas coexisten a propósito; no se migró la legacy). **Confirmado en v1.8.0:** la membresía `institution_users` de un acudiente no es opcional/cosmética — `SessionsController#authenticate_credentials` exige `user.memberships.active.exists?(institution_id:)` para autenticar, así que sin ella un acudiente no podría loguear ni después de completar su invitación. `Core::People::Resolver` la crea; **cero `role_assignments`** siempre (un acudiente no es staff).

### 3.3 Roles de Postgres y realidad operativa de BD

- **`edu_app_runtime`** — sirve la app. `NOSUPERUSER`, **sin `CREATE`** en `public`, **sin `BYPASSRLS`**.
- **`edu_migrator`** — corre migraciones. Tiene `CREATE`. Debe tenerlo en **todas** las bases (primaria **y** las tres Solid: cache/queue/cable).
- **`edu_bi_reader`** (rol auditado) — **único** rol con `BYPASSRLS`; solo para lecturas cross-tenant de super-admin / BI (nombre real en `lib/tasks/roles.rake`). **El runtime nunca hace lecturas cross-tenant.**
- **Migraciones corren con `bin/migrate`** (que exige `EDU_MIGRATOR_PASSWORD` no vacío), **NO** con `rails db:migrate` (cuidado: conecta con el rol runtime, sin `CREATE`, y falla).
- `schema_format = :sql`. Las bases Solid se pueblan por *schema load* desde `db/*_structure.sql` (no tienen carpeta de migraciones).
- Ver Guardrails (§13) para los gotchas operativos durables (`EDU_MIGRATOR_PASSWORD`, migrar dev vs. test, timestamps de migración).

### 3.4 Estructura de código — bounded contexts sin Packwerk

`app/domains/<dominio>/` es autoload root. Zeitwerk **colapsa** `app/domains/*/{models,queries,services,jobs,policies}` (ver `config/application.rb`), así que la capa intermedia NO aparece en el nombre de la constante:
- `app/domains/core/models/user.rb` → `Core::User` (no `Core::Models::User`).
- `app/domains/identity_access/services/otp/issuer.rb` → `IdentityAccess::Otp::Issuer`.
- `app/domains/core/services/people/resolver.rb` → `Core::People::Resolver`.

Librería de componentes compartidos en `app/views/shared/`; se **reutiliza antes de crear local** y se **promueve a `shared/` cuando un componente se usa en ≥2 dominios**.
El **plano de control vive FUERA de `app/domains/*`** — namespace propio `app/control_plane/`, montado en `/control_plane`, con su propio layout y auth real (`platform_admins` + MFA, S0).

---

## 4. Mapa de dominios

> Un **addon = un dominio (1:1)**. La institución habilita dominios comprando addons.

### Tier A — dominios base (existentes)

| Dominio | Propósito | Posee / notas |
|---|---|---|
| `core` | Espina académica **+ identidad de personas** | Acudientes (`guardian_students`), `academic_terms` (con índice de "un solo término activo" — **corrección de recon**: `students`/matrícula/`disciplinary_logs` NO viven aquí, ver `group_management`/`schedules`/`student_support`). Posee `Core::User`, `Core::InstitutionUser`, `Core::Session`, `Core::People::Resolver`, y `Core::Headcount::{Snapshotter,SnapshotJob}` (S3a) — la identidad y el pipe de headcount viven aquí, no en `identity_access` ni en el control plane. Casi todo le hace FK, incluido `Schedules::Enrollment.academic_term_id` desde v1.15.0 (cierra la mitad de modelo de Cav./B2). También posee `Core::RosterImport::*` (batch import de estudiantes/acudientes por CSV) — `RowPurger`/`PurgeRowsAllJob` (v1.53.0) purgan `roster_import_rows` (PII cifrada) de batches comiteados fuera de una ventana de retención de 30 días. **Parse+validar full-async desde v1.60.0** (`OPEN_PROCESS.md` ítem #1, mitad "hardening no bloqueante"): `#create` ya no corre `Parser`/`Validator` inline — crea el batch en estado nuevo `"queued"` con el CSV en `pending_content` (columna cifrada, `encrypts`, transitoria — nunca Active Storage ni argumento de job, ambos tablas globales sin RLS) y encola `Core::RosterImport::ParseAndValidateJob`, que la limpia apenas termina de parsear. |
| `staff_management` | **Staff generalizado (D1, CHECKPOINT E cerrado)** | `StaffManagement::StaffMember` — empleo/vinculación de TODO el personal (`staff_category` incl. `teaching`), `Department` (`kind` academic/operational, referenciado por `role_assignments.scope_department_id`), `EmploymentPeriod` (profundidad HR opcional). `department_id` **nullable** — un no-académico no necesita departamento académico. Fundacional (sin `Entitlement::Registry`). |
| `teacher_management` | Docentes — **especialización de staff (D1)** | `Teacher` es la extensión docente de un `StaffManagement::StaffMember` (`teachers.staff_member_id`, FK **nullable**, aditiva — un docente puede o no tener el link poblado). Posee lo exclusivamente docente: `teacher_code`, `faculty` (universidad), `teaching_assignments` (materia). Departamentos/perfil base viven en `staff_management`, no aquí. |
| `group_management` | Grupos | `groups` (`kind` homeroom/…), membresía/rosters. `students.user_id` y `students.national_id` (cifrado) viven en el modelo de este dominio (`GroupManagement::Student`). **Desde v1.36.0 también posee la GEOMETRÍA física del aula** (decisión A2 de `BI_DOCUMENT.md`): `GroupManagement::ClassroomLayout`/`SeatAssignment` (efectivo-fechadas, append-only, `EXCLUDE gist`), escritas por `ClassroomReconfigurer`/`SeatAssigner` (gate `groups.manage`); `analytics_bi` solo las lee para la Lente 1. **Desde v1.38.0 también posee la HISTORIA de placements** (decisión A1 de `BI_DOCUMENT.md`): `GroupManagement::StudentPlacement` (efectivo-fechada, append-only, `EXCLUDE gist`), escrita SOLO por `GroupManagement::SectionReassigner` (el único seam que mueve un estudiante de sección, mantiene `students.section_id` como caché vivo en lock-step); `analytics_bi` solo la lee (`AnalyticsBi::PlacementScope`) para la temporalidad año-a-año. |
| `schedules` | Libreta de notas **+** horario institucional, ambos reales | **Real**: `Subject`/`Enrollment`/`Assessment` (calificaciones, v1.14.0); `Enrollment.academic_term_id` conecta con `academic_terms` (v1.15.0 — `Schedules::ActiveTermEnrollmentScope` es el resolver canónico de "matriculado en el término activo"). **Horario/salones reales desde v1.50.0** (`Schedules::Room`/`MeetingPattern`, `guidelines/CLOSURE_PLAN.md` Fase D) — patrón plano (`day_of_week`+horas), sin tabla `periods` compartida; doble-booking de salón PERMITIDO, conflicto calculado en lectura (`MeetingPatternPresenter`), nunca bloqueado en BD. |
| `student_support` | Bienestar | **Convivencia real desde v1.45.0** (`StudentSupport::DisciplinaryLog`, molde `counseling`, append-only, auditado, `guidelines/CLOSURE_PLAN.md` Fase B) — **historia médica, alergias y acomodaciones también reales desde v1.48.0** (`MedicalHistory`/`StudentAllergy`/`Accommodation`, dos tiers de permiso, `guidelines/CLOSURE_PLAN.md` Fase D). Sensible, sin cifrado de columna (misma postura que `counseling` T3 — RLS+RBAC es la protección real). Tabla legacy `student_guardians` coexiste con `core.guardian_students` (no migrada; ver §3.2). |
| `cafeteria` | Alimentación | **Menú/Compra/Saldo REALES desde v1.51.0** — `Cafeteria::MenuItem`/`Purchase`/`PurchaseLine` reemplazan `MenuRoster`/`StudentAccountRoster` (ambos retirados). Checkout con bloqueo por alérgeno REAL desde v1.47.0 (`Cafeteria::DietaryRestriction`, propio de este dominio, NO leído de `student_support`). Una compra es un `Finance::Charge` contra la ÚNICA cuenta compartida (`finance.student_accounts`) vía `Cafeteria::PurchaseRecorder` (`account.lock!` transaccional, molde `ChargeCreator`/`EnrollmentCreator`) — **sin wallet propio de cafetería**, nunca una deducción de crédito prepago (no hay flujo de "recarga"). Sin UI de autoría de menú (seeded, molde `dietary_restrictions`/`character_frameworks`). |
| `library` | Biblioteca | **Real desde v1.54.0** (`Library::Resource`/`ResourceCopy`/`Loan`, greenfield puro — primer dominio de Fase D sin stub previo, `guidelines/library_prompt.md`, `OPEN_PROCESS.md` #1). `Library::LoanRecorder`/`ReturnRecorder`: **primer molde de este codebase que bloquea una fila y guarda SU PROPIA columna de estado** (`copy.lock!`, nunca `loan.lock!` — la carrera real es el entrelazado entre préstamos distintos sobre el MISMO ejemplar, no dos operaciones concurrentes sobre un mismo préstamo), respaldado por índice único parcial `WHERE status='active'` (molde `activity_enrollments`). `borrower_institution_user_id` XOR `borrower_student_id` (CHECK `num_nonnulls`, molde `conversation_participants` — nunca polimórfico) porque la spec solo nombraba `institution_user` pero su propia UX exige que estudiantes vean sus préstamos en portal. `MAX_ACTIVE_LOANS_STUDENT`/`STAFF` son PLACEHOLDER (sin settings-por-institución, A3). Multas por mora DIFERIDAS a propósito (la spec las condiciona a una regla configurable que no existe). Metering M1 cableado desde el día uno (no retrofit). |
| `admissions` | Admisiones | **COMPLETO desde v1.56.0** (`guidelines/library_prompt.md`, `OPEN_PROCESS.md` #1 — los tres incrementos de Fase D cerrados: `library` v1.54.0, base v1.55.0, este v1.56.0). Un aspirante nunca toca `Core::User`/`Core::InstitutionUser` hasta la aceptación. `Admissions::AcceptanceConverter` bloquea `application` (nunca `applicant`/`campaign`) — crea el `GroupManagement::Student` real (molde `Core::RosterImport::Strategies::Students`, corrigiendo el overview original que apuntaba a `Schedules::Enrollment`, matrícula de MATERIA, primitiva equivocada), liga al acudiente vía `Core::People::Resolver`+`Core::GuardianStudent` (molde `Strategies::Guardians`), y cobra `Finance::Charge` real SOLO al aceptar (un aspirante no es cobrable antes — sin precedente de cobro prospectivo en Finance). `Application#grade_level_id` aliasea `target_grade_level_id` (molde `Transportation::Route#route_id`), habilita el scope RBAC `:grade_level` ya real sin migración. **Incremento 3 (v1.56.0)**: `admission_step_templates`/`admission_application_steps` — instancias MUTABLES reales por fila (nunca un snapshot jsonb, el estado cambia con el tiempo) — y el tracker público del aspirante (`/admisiones/solicitud/:token`, subdominio-scoped, molde `Invitations::Issuer`). `Admissions::Tracker::PublicView` (molde `AnalyticsBi::Lens::AuraScope`) retorna un `Data` allowlist sin accessor para `private_notes`/`evaluator` — la única página pública de este repo que redacta CAMPOS de un registro por lo demás legible, en vez de gatear FILAS enteras por RBAC/relación como toda otra superficie de lectura. `ApplicationSubmitter` (extendido) hace fan-out de pasos + genera el token + encola el correo, todo gateado por `previously_new_record?`. Metering M1 cableado desde el día uno de la base. Único pendiente: ítem de backlog #7 (`finance` procesando/aprobando el cobro de admisión), fuera de alcance de `admissions` en sí. |
| `transportation` | Rutas | **Real desde v1.49.0** (`Transportation::Route`/`RouteStop`/`RouteRider`/`BoardingEvent`, `guidelines/CLOSURE_PLAN.md` Fase D). Conductor = `StaffManagement::StaffMember` real; `route_riders.shift` (am/pm) permite ruta distinta por turno. El scope `:route` de RBAC (`role_assignments.scope_route_id`) también se volvió real en este slice — antes solo existía en tests. Notifica a acudientes (Turbo Streams/Solid Cable — diferido, ver `OPEN_PROCESS.md`). |
| `analytics_bi` | Reporting + HPS (ver `guidelines/BI_DOCUMENT.md`) | `InstitutionDashboard` (v1.34.0, tenant-scoped) y `CrossTenantReportRoster` (v1.35.0, cross-tenant vía `edu_bi_reader`/`BYPASSRLS`, auditado) — **ambas mitades reales**. **Lente 1 "Mapa de Empatía Espacial" real (v1.36.0, Slice 2)**: lee la geometría de aula de `group_management` (nunca la posee) y deriva un heat in-memory de T1 (notas/asistencia), SVG server-rendered, gate `hps.classroom.view` (tenant-scoped). **Lente 5 "Auras de Cuidado" real (v1.37.0, Slice 3)**: posee `care_auras`, la PROYECCIÓN (enum cerrado + guía, cero PII clínica) que `counseling` publica vía `AnalyticsBi::Aura::Projector` y el docente lee como un ícono abstracto sobre la Lente 1 (`hps.aura.view`); `analytics_bi` nunca lee tablas de `counseling` (aislamiento clínico probado a nivel de modelo). **Temporalidad año-a-año real (v1.38.0, Slice 4)**: posee `hps_term_snapshots` (payload jsonb congelado por término, molde `report_cards.lines_snapshot`, escrito por `AnalyticsBi::Hps::Snapshotter` vía el fan-out `HpsTermSnapshotJob`); lee (nunca posee) `student_placements` de `group_management` (decisión A1) para el eje histórico. **Instrumento de carácter T2 real (v1.39.0, Slice 5)**: posee el instrumento staff-autoría molde rúbrica (`CharacterFramework`/`CharacterEvaluation`/`CharacterDimensionScore`, `framework_snapshot` congelado al publicar) y, separado, `peer_appreciations` (catálogo cerrado, umbral de agregación, XOR de dador, moderación append-only) gateado por `CharacterProgramConsent` — el primer consentimiento del codebase. **Lente 2 "Ficha de Personaje" real (v1.40.0, Slice 6)**: la primera lente de AUTOSERVICIO (`GuardianScope`/`StudentSelfScope`, sin RBAC, sin nav) — `AnalyticsBi::Lens::CharacterCard` lee (nunca re-posee) el instrumento del Slice 5 para armar radar/brújula/medallas/crecimiento; cero tablas nuevas. **Lente 3 "Constelación de Afinidades" real (v1.42.0, Slice 7)**: posee `affinity_taxonomy`/`student_affinities` (T2, árbol curado + vínculo estudiante↔talento, scope institución-wide o `department_id`); primera librería JS real del codebase (Cytoscape.js, importmap, progressive enhancement). **Lente 4 "Núcleo Familiar" real (v1.43.0, Slice 8) — ÚLTIMA de las 8 lentes/slices del roadmap original, todas cerradas**: posee `households`/`guardian_relationships` (T2, extienden `core.guardian_students` 1:1, nunca lo duplican); grafo orbital con Cytoscape.js reusado, hermanos detectados por query (cero tabla nueva), tensión del vínculo viva sobre señales T1 reales, alerta de lazos fraternales auditada; `custody_kind` segregado por construcción (nunca en el grafo). **Lente 6 "Alertas Tempranas" real (v1.46.0, amendment MAJOR — la única lente que NO es parte del roadmap original de 8)**: cero tabla propia, `AnalyticsBi::Lens::EarlyWarningScope` sintetiza señales de las Lentes 1/4/5 + `student_support` (heat, lazos fraternales, convivencia), gate `hps.early_warning.view` (institución-wide, revalida cada señal por su propio permiso de origen), entrega nunca automática. `BI_DOCUMENT.md` redefine el alcance del dominio (Sistema de Posicionamiento Humano, ahora 6 lentes, T1/T2/T3) y manda sobre `LINEAMIENTOS_MVP.md` para este dominio. |

### Tier B — identidad/roles

| Dominio | Propósito |
|---|---|
| `identity_access` | IAM/RBAC **+ onboarding**. Posee el catálogo global `roles`/`permissions`/`role_permissions`, `role_assignments` (tenant, scope por columnas explícitas — `scope_department`/`scope_grade_level`/`scope_group`/`scope_route`, y desde v1.57.0 `academic_term_id` opt-in), `invitations`, `email_otps`, `audit_events`, los servicios `Otp::*` e `Invitations::*`, `Audit`, y el controller/vistas de `people`. **Real desde v1.57.0**: `RolesController` (CRUD completo, antes solo `index`/`show` sobre un stub) y `AssignmentsController`/`UsersController` (antes stubs `RoleRoster`/`UserRoster`/`RoleAssignmentRoster`, ahora `Role`/`RoleAssignment`/`Core::InstitutionUser` reales) — `IdentityAccess::RoleCatalog` valida `assignable_scope_types` (5 roles canónicos; cualquier rol custom admite cualquier alcance). **No posee** `users`/`institution_users` (son de `core`); referencia por FK. |

### Tier B-bis — confirmados

| Dominio | Propósito |
|---|---|
| `counseling` | Psicoorientación. **Carve-out de `student_support`.** Casos/expedientes, sesiones/notas, remisiones, planes de intervención. Puede *leer* (no poseer) la historia médica de `student_support`. **Frontera de confidencialidad más estricta** que convivencia. **Desde v1.37.0 es el ÚNICO autor de las "auras de cuidado"** (Lente 5 del HPS): publica la proyección abstracta `analytics_bi.care_auras` vía `AnalyticsBi::Aura::Projector` (gate `counseling.write`, superficie anidada bajo el caso) — el diagnóstico (T3) NUNCA sale de aquí; solo cruza el enum+guía sin PII clínica. |
| `finance` | Tesorería/cartera **dentro** del tenant (el colegio cobra pensiones a acudientes). Cargos, pagos, estados de cuenta, planes de pago. **≠ billing de plataforma.** Tenant-scoped. **UI real desde v1.18.0**: `StudentAccount`/`Charge`/`Payment` (existían desde el primer commit) ahora tienen superficie de supervisión (molde #4, `finance.read`/`finance.write` — permisos que YA existían y ya reusaba `Cafeteria::BalancesController`) y portal del acudiente (solo lectura, mismo camino de lectura — `Finance::AccountStatement`). Dinero en `decimal(12,2)`, NO `*_cents bigint` (ver Guardrails). `PaymentPlan`/`Installment` (planes de pago/cuotas) siguen **sin UI**, diferidos a su propio slice — no alimentan el saldo hoy. |
| `communication` | Hub de comunicación. Ver §8 (anexo). **Ambos subsistemas reales**: (A) anuncios (v1.19.0, `Communication::Announcement`, difusión org-wide, RBAC para publicar + lectura por membresía); (B) mensajería (v1.20.0, `Conversation`/`ConversationParticipant`/`Message`, multiparte, participante `institution_user` **o** `guardian_user` — CHECK exactamente-uno, cuatro caminos de acceso RBAC/participación/auditoría). Diferidos anotados en §8.2 (fan-out 1:1, threading, tags, acudiente-inicia). |
| `attendance` | **Asistencia diaria por homeroom (v1.16.0, item #2 del MVP)** — dominio NET-NEW, real desde el día uno (sin fase stub). `AttendanceRecord` (`student_id`+`group_id`+`date`, único `(institution_id, student_id, date)`). Consume `Schedules::ActiveTermEnrollmentScope` (nunca re-deriva el join a término); molde #4 completo (per-row `can?`, `authorize!`, nav). Addon-gated. Por-materia diferido. |
| `report_cards` | **Boletines (v1.17.0, item #3 del MVP)** — dominio NET-NEW, addon-gated, lee `schedules` por FK (nunca posee `Subject`/`Enrollment`/`Assessment`). `ReportCard` (`student_id`+`academic_term_id`, único `(institution_id, student_id, academic_term_id)`) — snapshot **congelado al publicar** (`lines_snapshot` jsonb + `overall_average`, nunca recomputado al leer un publicado). "Draft" es cómputo vivo sin fila (`ReportCards::Computation`, consumido tanto por el preview de supervisión como por `ReportCards::Publisher`). Dos superficies: supervisión (molde #4, `report_card.view`/`report_card.publish`) y portal (por relación, solo publicados, sin `authorize!`, fuera de `Navigation::Registry`). Consume `Schedules::ActiveTermEnrollmentScope` igual que `attendance`. Asistencia en el boletín y escala Decreto 1290 diferidos. |
| `assignments` | **Tareas académicas — TRACK COMPLETO, slices 1–4/4 (v1.21.0–v1.26.0, item #6 del MVP)** — dominio NET-NEW, addon-gated, cuelga del gradebook de `schedules` por FK (`Assignments::Assignment` → `subject_id`; `schedules::Assessment` gana `assignment_id` nullable, aditivo). **La nota vive SOLO en `schedules::Assessment`** — publicar hace fan-out (una fila `Assessment` por matrícula del roster, `score: nil`, SIEMPRE per-student, con o sin `group_work`/rúbrica); calificar `UPDATE`-ea esa misma fila (`Assignments::GradeRecorder`), nunca un almacén paralelo. Roster = `Schedules::ActiveTermEnrollmentScope` ∩ la materia ∩ scope RBAC del docente (vía `grade_level_id` de `Subject`, mismo mecanismo que ya usaba `grades.write`). Supervisión (molde #4, `assignment.manage`) + portal (por relación, solo `published`, con la nota leída del mismo origen que `report_cards`). **v1.22.0**: entrega de texto (`Assignments::Submission`, en-domain, NO anclada a `assessment_id` — pareo entrega↔nota vía `Assignments::GradingView`), ingresable por el estudiante o su acudiente (B1) — primer write de portal, gateado por relación. **v1.23.0**: `group_work` toggle por-tarea (bloqueado tras publicar); `Submission` generalizada a estudiante **XOR** `SubmissionGroup` (CHECK real, patrón de `conversation_participants` v1.20.0); grupos por-tarea (`GroupMembership`, estudiante en ≤1); nota grupal = bulk-set per-student (`GroupGrader`) + override individual, sin almacén grupal; entrega compartida editable por cualquier integrante sin `group_id` explícito. **v1.24.0**: adjuntos de entrega (docx/pdf/jpg/png, ≤10MB, ≤5) sobre una `Submission` ya existente — tabla puente tenant-scoped `Assignments::SubmissionAttachment` (RLS `ENABLE+FORCE`; las tablas crudas de Active Storage NUNCA llevan RLS, ver `OPEN_PROCESS.md`); content-type real vía Marcel en un service object (`AttachmentAdder`); tres controllers de servicio, nunca las rutas firmadas de Active Storage. **v1.25.0**: materiales del docente (`Assignments::Material`, mismo molde de tabla puente, dueño `Assignment`) — escritura gateada por RBAC (`assignment.manage`, 403 sin permiso), no por relación; lectura de portal SIN CAMBIOS (`StudentView`/`GuardianScope`), un borrador/archivada es inalcanzable gratis; `Assignments::AttachmentTypeCheck` (nuevo) comparte la validación de tipo/tamaño con `AttachmentAdder`. **v1.26.0 — CIERRA EL TRACK**: rúbricas — biblioteca reutilizable normalizada (`RubricTemplate`/`RubricCriterion`/`RubricLevel`/`RubricCellDescriptor`) asociada vía `evaluation_method` (`direct`/`rubric`, mismo freeze de `group_work`); estructura congelada como snapshot jsonb al publicar (molde `price_tiers_snapshot`/`lines_snapshot`); `Assignments::RubricScore` calcula, `RubricGrader`/`GroupRubricGrader` persisten la evaluación (`RubricEvaluation`, estudiante XOR grupo) y escriben la nota vía `GradeRecorder`/`GroupGrader` SIN CAMBIOS — la rúbrica NUNCA almacena la nota; portal ve nivel+descriptor por criterio (`StudentView.rubric_breakdown_for`), sin RBAC. Ver `HISTORIA.md` v1.21.0–v1.26.0. |
| `calendar` | **Calendario compartido con cuidadores (v1.27.0, item #7 del MVP)** — dominio NET-NEW, addon-gated, real desde el día uno (sin fase stub). `Calendar::Event` (`calendar_events`: `title`/`description`/`starts_at`/`ends_at`, CHECK `ends_at >= starts_at`). **Audiencia por dos columnas de scope mutuamente exclusivas** (`scope_grade_level_id` XOR `scope_group_id`, ambas null ⇒ institución-wide; CHECK de exclusividad + validación de modelo), mismo idioma que `role_assignments.scope_*`. **Deliberadamente SIN `kind`** (supersede a `LINEAMIENTOS_MVP.md §4.2`) — los "deadlines" se derivan en memoria, nunca como fila. `created_by_institution_user_id` nullable+nullify (atribución). Índice `(institution_id, starts_at)` (líder `institution_id` para el RLS guard + orden cronológico) + índices en las dos columnas de scope. **Tres ramas de `authorize!` sobre el único permiso `calendar.manage`**, según la audiencia elegida (grupo→`Section`, grado→`GradeLevel`, institución-wide→`Current.institution`), sobre el mismo `Authorization::Assignment#covers?`+`SCOPE_READERS`; `GroupManagement::GradeLevel#grade_level_id` (nuevo) es el primer consumidor real de `scope_grade_level_id` fuera de `PermissionCheck`. Dos superficies: gestión de staff (molde #4, `ManageableScope`, `calendar.manage`, solo `calendar_events` reales) y portal estudiante/acudiente (relación, `VisibleScope`+`Timeline`, sin `authorize!`, fuera de `Navigation::Registry`). `Calendar::Timeline.for(student:)` mezcla eventos reales con los `due_date` de las `assignments` publicadas visibles a ESE estudiante (entradas sintéticas `Calendar::Timeline::Entry`, nunca filas) — **solo en el portal** (asimetría con la gestión de staff, "un solo camino de lectura"). Ver `HISTORIA.md` v1.27.0. |
| `extracurriculars` | **Actividades extracurriculares (v1.27.0, item #8 del MVP)** — dominio NET-NEW, addon-gated, real desde el día uno. `Activity` (`kind` deporte/arte/refuerzo, `academic_term_id` FK NOT NULL, `fee_cents` bigint — dinero NUEVO en F6, nunca `decimal`) + `Enrollment` (`activity_enrollments`, soft active/withdrawn, `enrolled_via` staff/guardian). **Cupo** = invariante agregado (`activity.lock!` + `COUNT` en `EnrollmentCreator`, nunca un CHECK declarativo) respaldado por un índice único PARCIAL `(institution, activity, student) WHERE status='active'`. **RBAC ownership-vs-hierarchy**: `activity.manage` (institución-wide) vs. `activity.instruct` (propiedad de fila — `Extracurriculars::ActivityScope` filtra directo por `instructor_staff_member_id`, NO un `scope_type` nuevo en `role_assignments`). Ambas vías de inscripción (colegio y acudiente). Portal estudiante (solo lectura) + acudiente (lectura + inscribir/desinscribir per-child, sin RBAC). Actividad paga = un `Finance::Charge` (el puente `fee_cents`→`BigDecimal`/100 vive en `Activity#fee_amount`, un solo lugar). Ver `HISTORIA.md` v1.27.0. |

### Tier C — candidatos (crear SOLO bajo confirmación explícita)

Ninguno pendiente. (`admissions` ya NO es candidato — Incremento 2 real desde v1.55.0, ver Tier A
arriba; Incremento 3, pasos configurables + tracker público, sigue pendiente pero ya no es un
candidato greenfield genérico. `library` ya NO es candidato — real desde v1.54.0, ver Tier A
arriba. `staff_management` ya NO es candidato — CHECKPOINT E cerrado v1.12.0, ver §10/`HISTORIA.md`:
existe y resuelve el staff generalizado desde el primer commit del repo.)

### Orden de dependencias (creación / migraciones)

1. `core`, `staff_management`, `teacher_management` (extensión docente de `staff_management`), `group_management` (proveen destinos de scope).
2. `identity_access` (referencia los anteriores por FK).
3. `schedules`, `student_support`, `cafeteria`, `transportation`, `analytics_bi`.
4. `counseling` y `finance` (FK a `core`; `counseling` puede leer `student_support`).
5. `communication` (FK a `core` e `identity_access`; consume notificaciones del resto).
6. Tier C, según se confirmen.

---

## 5. RBAC — rol + scope

El rol dice **qué**; la asignación dice **sobre qué**. Toda autorización responde a:
*"¿tiene un rol que incluya este permiso **con un alcance que cubra este recurso**?"*.

### Catálogo — `permissions` global; `roles`/`role_permissions` tenant-scoped con RLS

> **Corrección P1 (v1.6.0):** el recon de P1 confirmó contra el disco que **solo `permissions` es
> global** (sin `institution_id`, sin RLS). `roles` y `role_permissions` SÍ son tenant-scoped
> (`institution_id NOT NULL` + `FORCE ROW LEVEL SECURITY`) — versiones previas de este doc los
> daban por globales. Implicación operativa: el catálogo de roles se siembra **por institución**
> (bajo GUC), no una sola vez para toda la plataforma.

- **`roles`**: `key` (slug greppable: `teacher`, `area_lead`, `academic_coordinator`, `coexistence_coordinator`, `counselor`, `dean`, `principal`, `institution_admin`, …), `name`, `system` (boolean; la columna real se llama `system`, no `is_system`). **No existe** columna `assignable_scope_types` en el esquema real — es un concepto solo del panel admin decorativo de `identity_access` (`IdentityAccess::RoleRoster`, un stub aparte del RBAC real), no algo que el motor valide.
- **`permissions`**: `key` (`teachers.view`, `teacher.evaluate`, `students.read`, `people.manage`, …), `description`. Catálogo completo en `IdentityAccess::SeedPermissions::CATALOG` — este SÍ es global.
- **`role_permissions`**: relaciona `role_id`+`permission_id`, tenant-scoped (ver arriba), `ON DELETE CASCADE`.

### Asignaciones — TENANT-SCOPED, con RLS

- **`role_assignments`**: el corazón del scope. Columnas de alcance **explícitas** (no polimórfico, pero **SÍ con FK real** a `departments`/`grade_levels`/`sections`/`routes` respectivamente — confirmado en P1 para las tres primeras, `scope_route_id` se agregó real en v1.49.0, no inventar lecturas contra ids que no correspondan a filas reales): `scope_department_id` / `scope_grade_level_id` / `scope_group_id` / `scope_route_id` (las no usadas en NULL = institución-wide), `valid_from` / `valid_until` (fechado efectivo — columnas agregadas en P1, no existían antes; ver Guardrails y `HISTORIA.md`). **DECIDIDO** — no re-preguntar.

### Roles de plataforma (globales, cross-tenant)

- **super-admin** auditado y **`bi_auditor`** (solo lectura BI) — sobre el rol Postgres `BYPASSRLS`, nunca runtime.

### Catálogo de roles de staff (sobre `institution_users`)

Académicos/docentes (`teacher`, `area_lead`), coordinación (`academic_coordinator`, `coexistence_coordinator`), dirección (`principal`, `dean`), registro (`academic_secretary`, `registrar`), bienestar (`counselor`, `psychologist`, `social_worker`), clínico (`medical_staff` — dueño de historia médica), servicios (`cafeteria_staff`, `transport_coordinator`, `driver`, `route_monitor`), administración (`institution_admin`), BI (`bi_auditor`). Casi todos requieren **scope**, no rol suelto.

> `student` y `guardian` **no** están en este catálogo: son entidades-persona, acceden por portal.
> `institution_users.role` (columna string libre, default `"member"`) **no es** este catálogo RBAC —
> es un campo heredado, sin lectores en el código todavía (P2, abierto — ver §11).

---

## 6. Vistas + navegación (regla de 3 clics)

### 6.1 Principios

1. **Funcionalidad sobre estética.** Vistas aburridas, legibles, componibles con `shared/`. Cero rediseño.
2. **Regla de 3 clics.** Cualquier vista de trabajo se alcanza en **≤ 3 clics** desde el dashboard.
3. **Rol + scope, no solo rol.** Puerta dura en el controlador; reflejo cosmético en la vista.
4. **Reutilizar > reconstruir.**
5. **Multi-rol y multi-institución.** El shell resuelve institución activa + roles efectivos por request.

### 6.2 Árbol de navegación (profundidad 3)

```
CLIC 0  Dashboard (landing por rol)   → tiles SOLO de dominios que el rol permite y la institución tiene entitled
CLIC 1  Índice de dominio             → ya pre-filtrado por scope
CLIC 2  Registro (show)               → una entidad
CLIC 3  Detalle / acción              → pestaña del show o formulario
```

Piezas transversales (ya construidas): barra de navegación por rol + entitlement (si no hay
permiso del dominio o el addon no está habilitado, el tile **desaparece** — no "deshabilitado"),
selector de institución, selector de "actuando como" (opcional), dashboard por rol (2–5 atajos),
buscador global acotado por scope, portales de persona (student/guardian, sobre datos reales de
relación vía `Core::Access::GuardianScope`/`StudentSelfScope`, v1.9.0), **autoservicio de staff
("mis datos", `/mis_datos`) sobre datos reales por identidad** (`Core::Access::StaffProfileScope`/
`StaffRoleAssignmentsScope`, v1.10.0) — ninguna de estas tres superficies pasa por `authorize!`,
todas se gatean por identidad/relación, no por RBAC, y viven fuera de `Navigation::Registry`
(cuyas entradas SIEMPRE filtran por `can?`) — vista **403 amable** (RBAC) y página **"módulo no
habilitado"** (entitlement, distinta del 403).

**La INVERSIÓN respecto a lo anterior: el visor de `audit_events` + bandeja de discrepancias**
(`/identity_access/audit_events`, v1.11.0) **SÍ es una superficie administrativa RBAC-gateada**
(`authorize!("audit_events.read")`) y **SÍ vive en `Navigation::Registry`** — es la lectura de
"quién hizo qué", no "mis propios datos", así que la puerta correcta es RBAC, no identidad. La
bandeja de discrepancias es la MISMA query pre-filtrada al marcador de `DiscrepancyReporter`, nunca
una tabla nueva. `audit_events` sigue append-only (`REVOKE UPDATE/DELETE` a nivel de rol de BD);
esta vista solo lee.

**Capa PRE-login:** layout separado (`layouts/auth`, sin nav de dominio ni selector de institución)
para `sessions/`, `email_otps/`, `invitations/`, y como fallback cuando la página de entitlement
dispara sin `Current.institution` resuelta — deliberadamente minimalista.

### 6.3 Las dos compuertas en serie

**Compuerta #1 (entitlement) — real de punta a punta desde S2b.** `Entitlement::Controller`
(concern único, incluido una vez en `ApplicationController`) corre un `before_action` que consulta
`Current.entitled_addon_keys` (memo por request sobre `Core::Institution#entitled?` →
`ControlPlane::Entitlements::Check`) y corta con la página "módulo no habilitado" **antes** de que
la acción llegue a `authorize!`. Ver §7.1 para el detalle del mecanismo.

**Compuerta #2 (RBAC) — real (P1, cerrado).**
- **`IdentityAccess::PermissionCheck`** (`app/domains/identity_access/services/permission_check.rb`): resuelve *"¿puede U ejecutar A sobre R?"* con scope, una vez por request (memoizado). Lee `role_assignments` reales (vigentes, `.effective_now`) → `roles` → `role_permissions` → `permissions`. **Real-only, fail-closed**: sin `RoleAssignment` aplicable, cero permisos — no hay fallback a ninguna persona stub.
- **`can?(permission, resource = nil)`** (helper de vista): mostrar/ocultar acciones — **solo cosmética**.
- **`authorize!(permission, resource)`** (concern de controlador, `Authorization::Controller`): **puerta dura, llamada a mano al inicio de cada acción** — la que protege de verdad. Resuelve siempre contra `IdentityAccess::PermissionCheck` (el seam ya tenía el `if defined?` esperando; el fallback a `Authorization::StubResolver`/`StubAssignments` queda muerto en runtime, confirmado con `rails runner`).
- Los índices filtran por **Query object de scope**, nunca `default_scope`. `PermissionCheck#scope_for` existe para que un dominio filtre directo (en vez de recorrer+`can?` fila por fila), pero su adopción es **incremental por dominio** — ningún dominio lo consume todavía; `teacher_management` sigue con el patrón per-row `can?`, igual de válido.

### 6.4 Matriz rol × dominio y caso de aceptación de referencia

La matriz completa rol×dominio vive en `HISTORIA.md` (referencia estable, no cambia seguido).
Caso de aceptación que la valida end-to-end: `teacher_management`, **real desde P1** (antes probado
solo contra la persona stub) y **desde v1.13.0 real también contra las VISTAS** (índice/show/acción,
no solo el `authorize!` unitario — ver §6.6). María = `(teacher, group:10-A)`, `(teacher,
group:11-B)`, `(area_lead, department:Matemáticas)`, sembrada como `role_assignments` reales, contra
un `TeacherManagement::Teacher`/`StaffManagement::Department` reales (ya no la ex-`TeacherRoster`/
`DepartmentRoster` en memoria). `authorize! teacher.evaluate` = **sí** sobre un docente de
Matemáticas, **no** sobre uno de Sociales; y no ve al resto de la institución. El descriptor de
scope (recurso expone `department_id` vía `Teacher#department_id` delegado a
`StaffManagement::StaffMember`, y `Department#department_id` aliasa `id`) solo se cableó de verdad
en `teacher_management` — el resto de dominios sigue con su catálogo de recursos en stub (backlog
#4), aunque ahora las ASIGNACIONES que los autorizan ya son reales en todos los dominios probados
(ver `HISTORIA.md` v1.6.0).

### 6.5 Patrones canónicos (la "convención de la casa")

Cinco esqueletos que hacen converger a todos los dominios: (1) Query object de índice con scope,
(2) controlador con `authorize!` al inicio, (3) gating con `can?` en vista, (4) pestañas gateadas por
permiso, (5) auto-registro de navegación en archivo propio del dominio (no editar un partial central).

### 6.6 Molde de vista de negocio por dominio — implementación de referencia: `teacher_management`

**#4 (vistas de negocio por dominio) arranca aquí (v1.13.0, slice 1).** Los cinco esqueletos de §6.5
dejaron de ser solo un principio: `teacher_management` es ahora su **implementación real de
referencia**, contra la que los seis dominios restantes (`core`, `student_support`,
`group_management`, `schedules`, `counseling`, `cafeteria`, `transportation`) se copian en slices
posteriores — no se reinventan.

- **Esqueleto #1 (query object)**: `TeacherManagement::TeacherScope`/`DepartmentScope`,
  `StaffManagement::StaffScope` — relation real + `institution_id` explícito + **per-row `can?`**
  vía `.select`, nunca `default_scope`, nunca `PermissionCheck#scope_for` (§6.3: ambos son
  equivalentes; per-row es el que este molde fija como el "aburrido" a copiar).
- **Esqueleto #2/#3** (`authorize!`/`can?`): `TeachersController`/`DepartmentsController`/
  `StaffController`/`TeacherEvaluationsController` — puerta dura al inicio de cada acción, `can?`
  solo cosmético en la vista (botón "Nueva evaluación").
- **Esqueleto #4** (pestañas gateadas): `teachers/show` — tabs "Perfil"/"Evaluaciones", la acción
  dentro de una pestaña se oculta con `can?`, nunca la pestaña misma (un `secretary` de solo lectura
  sigue viendo el estado de evaluaciones, solo no el botón de crear una).
  **Esqueleto #5** (auto-registro de nav): `config/navigation/{teacher_management,
  staff_management}.rb` — ya existía desde antes de #4, sin cambios.
- **Es supervisión, NO autoservicio** (frontera dura — ver `CONCEPTOS_TECNICOS.md`): estas vistas
  muestran a OTRAS personas dentro del alcance del actor; se gatean por RBAC + scope y viven en
  `Navigation::Registry`. La contracara exacta de `/mis_datos` (v1.10.0, identity-gated, sin
  `authorize!`, fuera del registry) — no confundir las dos superficies para un mismo dominio.
- **`evaluate` sin modelo destino (BV6)**: no existe `TeacherManagement::Evaluation` — este slice
  cableó el GATE real (per-row, sobre un `Teacher` real) y la afordancia en vista; el `create` sigue
  siendo un stub sin persistencia (`flash` únicamente). Construir el modelo de evaluación real es
  follow-up, no parte de #4 slice 1.
- **Sin migraciones** — el descriptor de scope real (`staff_members.department_id`,
  `teachers.staff_member_id`) ya existía desde CHECKPOINT E (v1.12.0); este slice solo leyó contra
  él en vez de contra los rosters en memoria.

---

## 7. Plano de control · addons, entitlements y billing de plataforma

> **No es un dominio.** Vive en `app/control_plane/` (fuera de `app/domains/*`), cross-tenant, por
> encima de RLS. **Billing de plataforma ≠ dominio `finance`**: aquí la plataforma cobra al colegio;
> en `finance` el colegio cobra a los acudientes. Servido por el rol runtime normal
> (`edu_app_runtime`), **sin `BYPASSRLS`** — estas tablas son globales y no necesitan cruzar tenants
> para leerse. **Track de billing completo (S0→S4) + hardening (v1.59.0)** — pendiente solo emisión
> real de uso por dominio (S3b, requiere M1); el riel de pago AUTOMÁTICO/pasarela sigue fuera de
> alcance de v1, pero el registro MANUAL de abonos (`ControlPlane::Payment`) ya es real.

### 7.1 El gate de entitlements (dos compuertas en serie)

1. **Entitlement** (control plane): *¿la institución PUEDE usar este dominio?* → contrato/plan.
2. **RBAC con scope** (`identity_access`): *¿el usuario DENTRO puede?* → rol + alcance.

**Real de punta a punta desde S2b — con datos reales, no solo diseño.** El punto único de
verificación es `Core::Institution#entitled?(addon_key)` (delega en
`ControlPlane::Entitlements::Check` de S2a); `Current.entitled_addon_keys` lo memoiza una vez por
request (`Core::Access::EntitledAddonKeys.for(institution)`). Del lado del inquilino, **una sola
pieza** — el concern `Entitlement::Controller`, incluido una vez en `ApplicationController` — infiere
el `addon_key` del namespace del controller y corta con la página "módulo no habilitado" **antes**
de que la acción llegue a `authorize!`; la nav (`ApplicationHelper#nav_items`) filtra por el mismo
memo antes de aplicar el `can?` cosmético existente. Los fundacionales (`core`,
`teacher_management`, `group_management`, `identity_access`) **nunca** pasan por este gate — su
ausencia de `Entitlement::Registry` (declaración tenant-side propia, `config/entitlements/*.rb`, un
archivo por dominio addon-gated) ES la señal de "no gateado", sin necesitar una lista aparte.
`Entitlement::Registry` **no referencia `ControlPlane::AddonCatalog::DOMAIN_KEYS` en runtime** — solo
un test (`test/models/entitlement/registry_consistency_test.rb`) cruza ambas listas para atrapar
drift. El código de cada dominio no se ramifica — se enciende/apaga por institución desde un solo
lugar. **Gate #2 (RBAC) es real desde P1** (`IdentityAccess::PermissionCheck`) — **ambas compuertas
son reales**; orden #1→#2 verificado intacto (`entitlement_gate_test.rb`).

### 7.2 Estado de las piezas (tabla de estado)

| Pieza | Slice | Estado | Invariante clave |
|---|---|---|---|
| `platform_admins` (+ MFA propio) | S0 | ✅ Real | Aparte de `Core::User`, no un flag. MFA (`ControlPlane::Otp::*`) independiente de `IdentityAccess::Otp::*`. |
| `addons` | S1 | ✅ CRUD real | 1 addon = 1 dominio addon-able (F14); `ControlPlane::AddonCatalog::DOMAIN_KEYS` es la lista canónica; retiro suave, nunca hard-delete. |
| `plans` / `plan_price_tiers` | S1 | ✅ CRUD real | Tarifa base por alumno + tiers hijos explícitos (no JSONB); sin FK a `addons` (F9); pricing no se aplica a nada hasta que se firma una subscription. |
| `subscriptions` | S2a | ✅ CRUD real | **Snapshot inmutable** al firmar (`price_tiers_snapshot` jsonb + escalares) — nunca re-lee el catálogo vivo después; una activa por institución; `institution_id` FK global, no tenancy. |
| `institution_entitlements` (`ControlPlane::Entitlement`, no `InstitutionEntitlement`) | S2a | ✅ CRUD real | Overrides negociados **almacenados** (aplicados recién en S4); un activo por institución+addon; retirar un addon con entitlements activos se bloquea (F10-bis). Predicado: `ControlPlane::Entitlements::Check.entitled?`. |
| Gate de entitlement en el inquilino | S2b | ✅ Real | Ver §7.1 — `Entitlement::Controller` + `Entitlement::Registry` + nav filtrada. Único touch histórico en `app/domains/*` fue en `core`. |
| `student_headcount_snapshots` | S3a | ✅ Real | Empujado por el tenant bajo su propio GUC (`Core::Headcount::SnapshotJob`, hereda `ApplicationJob`), nunca leído en vivo por el control plane. Cuenta `students.status == "active"` — **no** matrícula en término activo (limitación conocida, ver Guardrails). Un snapshot por `(institution, as_of_date)`. |
| `usage_events` / `usage_daily_rollups` | S3a | ✅ Pipe real, sin emisión | `ControlPlane::Usage::Ingest` idempotente (no-op en duplicado), exige `metered: true`, no exige entitlement activo. `RollupJob` idempotente (recomputa, nunca incrementa). Sin GUC (tablas globales). **Ningún dominio emite eventos reales (S3b, requiere M1).** |
| `invoices` / `invoice_line_items` | S4 | ✅ Corte real | `ControlPlane::Billing::PeriodCut` produce factura **borrador**, nunca auto-emitida; aplica overrides (`coalesce`) y el snapshot congelado por primera vez; idempotente (re-cortar un draft reemplaza líneas vía `delete_all`, que bypasea a propósito el `readonly?` de `InvoiceLineItem`); rechaza sin subscription activa (H9) o sobre una factura `finalized`. Sin GUC. `billing_period_id` (FK a `billing_periods`, no ya dos columnas de fecha propias) ancla el período; `#period_start`/`#period_end` sobreviven delegados. **Finalizar ≠ cobrar** — sigue sin riel de pago automático, pero un pago MANUAL sí se puede registrar (ver fila siguiente). |
| `billing_periods` | v1.59.0 | ✅ Real | Entidad propia, GLOBAL (misma postura que `invoices`) — antes eran dos columnas sueltas en `invoices`. Único `(institution_id, starts_on, ends_on)`; `has_many :invoices, dependent: :restrict_with_exception` (nunca se destruye uno con facturas). `PeriodCut`/`PeriodCutAllJob` usan `find_or_create_by!` — un re-cut o un re-run del mes nunca lo duplica. |
| `control_plane_payments` (`ControlPlane::Payment`) | v1.59.0 | ✅ Real (manual) | Registro MANUAL de un abono contra una factura — prefijo `control_plane_` porque `payments` ya es una tabla real de `Finance` (dominio distinto). Sin `status` (existir es el único estado con comportamiento real). `ControlPlane::Billing::PaymentRecorder` (molde `Finance::PaymentRecorder` sin `lock!` — `Invoice#paid_cents` es un `sum` computado, no una columna mutable), idempotente por `idempotency_key`, siempre atribuido a un `platform_admin` interactivo, auditado (`payment.recorded`). Sigue sin existir cobro automático/pasarela. |

> El detalle narrativo de "cómo se construyó" cada pieza (decisiones H1–H9, F-numeradas, bugs
> encontrados durante el desarrollo) vive en `HISTORIA.md` → Changelog + "Narrativa detallada: modelo
> de datos del plano de control".

### 7.3 Modelo de cobro: híbrido

**Implementado desde S4.** Tres piezas ortogonales: **base por alumnos** (`base_seats`) + **fee por
addon** (`addon_fee`) + **overage por uso** (`usage_overage`). El **corte de periodo**
(`ControlPlane::Billing::PeriodCut`, invocable manual/rake vía `ControlPlane::Billing::PeriodCutJob`
— schedule recurrente diferido) produce una **factura borrador** (`draft`) para revisión humana —
**nunca auto-emitida**. Finalizar (`Invoice#finalize!`) es una acción manual y auditada de
`platform_admin`; **finalizar ≠ cobrar** — sigue sin existir riel de pago automático/pasarela. Las
líneas de `usage_overage` dan cero/ausentes hoy porque `usage_daily_rollups` está vacío hasta que
S3b cablee emisión real por dominio — la maquinaria del corte ya está completa y probada con
rollups sintéticos.

**Registro manual de abonos (v1.59.0).** Prorrateo de `addon_fee` y edición manual de líneas de un
borrador: decisión explícita del owner de **no construir** (tarifa plana / `recut` ya alcanza) —
no son un backlog diferido, son un "no" documentado. `billing_periods` (entidad propia, GLOBAL) SÍ
se construyó — los períodos deben distinguirse entre sí y un abono debe poder apuntar a cuál
liquida. `ControlPlane::Payment` (`ControlPlane::Billing::PaymentRecorder`) registra un abono
MANUAL contra una factura — el owner confirmó esto al resolver `billing_periods`, revisando el
no-goal de "riel de pago" (ver abajo): sigue sin cobro automático, pero ya hay dónde anotar que
un pago llegó. `Invoice#paid_cents`/`#balance_due_cents` son computados (`sum` de `payments`),
nunca persistidos.

---

## 8. Dominio `communication` (anexo de diseño)

Dos subsistemas que NO se aplanan en un solo modelo — decisión cerrada en v1.19.0, supersede el
borrador anterior de este anexo (que proponía una tabla `conversations` unificada con `kind`
incluyendo `announcement`; recon de v1.19.0 confirmó que no existía nada real todavía y el owner
decidió separarlos por ser estructuralmente distintos):

### 8.1 Subsistema (A) — Anuncios — ✅ real desde v1.19.0

Difusión de una vía, org-wide dentro del tenant (staff + acudientes + estudiantes con cuenta, sin
segmentar por grado/grupo). Tabla dedicada `announcements` (`title`/`body`/`status` ∈
published/retracted/`author_institution_user_id`/`published_at`/`retracted_at`) — **no** reutiliza
ningún modelo de mensajería. Publicar/editar/retractar es RBAC (`announcement.publish` +
`Navigation::Registry`); leer es **membresía** (cualquier miembro activo, sin `authorize!`, fuera
del Registry, enlazado aparte — ver Guardrails, "tercer tipo de gate"). Retract es SIEMPRE soft
(`status`), nunca `destroy`. Camino de lectura único (`Communication::AnnouncementFeed`) compartido
por staff y ambos portales. Ver `HISTORIA.md` v1.19.0 para la narrativa completa.

### 8.2 Subsistema (B) — Mensajería — ✅ núcleo real desde v1.20.0

Las tres preguntas de diseño quedaron resueltas en el checkpoint (no re-abrir sin razón de negocio
explícita — mismo tratamiento que ⚠-1/⚠-2):

1. **Modelo de hilo = multiparte** (no hub-and-spoke): 2+ participantes, todos ven todos los
   mensajes en una misma conversación. El fan-out 1:1 "director → cada cuidador en privado" queda
   como **fast-follow** — un helper de composición que crea N conversaciones de 2, sin cambiar el
   modelo base.
2. **Auditor = rol de institución** (`conversation.audit`), nunca el super-admin de plataforma.
3. **El auditor ve contenido completo**; el rastro **no** se surfacea a los participantes (solo
   vive en `audit_events`, RBAC-gated).

**Modelo real:** `conversations` (`subject`, `status` ∈ active/closed, `created_by_institution_
user_id`/`closed_by_institution_user_id` — atribución, nullable+nullify), `conversation_
participants` (`institution_user_id` **XOR** `guardian_user_id` — CHECK `num_nonnulls(...) = 1`,
CASCADE en ambas identidades porque son la identidad del registro, no atribución opcional;
`last_read_at` para no-leídos), `messages` (mismo XOR para el emisor, sin `parent_message_id` —
threading diferido, se agrega aditivo cuando se construya). Sin tabla de tags (diferido).

**Cuatro caminos de acceso, nunca colapsados** (ver Guardrails): iniciar (RBAC,
`conversation.compose`, destinatarios acotados — staff de la institución ∪ acudientes de
estudiantes en el scope RBAC del actor, **nunca** un directorio) → bandeja/responder (participación,
sin `authorize!`, camino de lectura único `Communication::Inbox` compartido por staff y el portal
del acudiente) → auditar (RBAC, `conversation.audit`, permiso **distinto** de compose, loguea
`conversation_audited` en `audit_events` solo si el accesor NO es participante).

**Diferidos, anotados, no construidos:** difusión 1:1 a todos los cuidadores (fast-follow sobre la
base multiparte); threading (`parent_message_id`); tags/clasificación por temática; mensajería
iniciada por el acudiente (bloqueada por Habeas Data — "quién puede contactar a quién libremente"
necesita su propio diseño de directorio acotado); videollamada + acta (futuro lejano). Ver
`HISTORIA.md` v1.20.0 para la narrativa completa y los hallazgos de recon.

<details>
<summary>Preguntas originales del checkpoint (resueltas arriba — histórico)</summary>

1. ~~"Director ↔ todos los cuidadores": ¿hilo grupal o hub-and-spoke?~~ → multiparte, ver arriba.
2. ~~¿Quién audita?~~ → rol de institución, ver arriba.
3. ~~Frontera de confidencialidad: ¿contenido completo o solo metadata? ¿rastro visible a los
   participantes?~~ → contenido completo para el auditor; rastro nunca visible a participantes, ver
   arriba.

</details>

**Colisión de nombres a evitar** (seguía aplicando del borrador anterior): *canales de discusión*
(Slack-like, si mensajería termina adoptando el concepto) vs. *canales de entrega* de notificaciones
(in-app/email/push) — nombrar distinto (`_channel_*` vs `_delivery_channel_badge`) si ambos
terminan existiendo. **Tiempo real (Turbo Streams/Solid Cable) diferido** para ambos subsistemas.
Los correos de OTP e invitación **no** pasan por este dominio — van directo por
`ApplicationMailer`/Active Job; centralizar todo envío saliente aquí sería una decisión de
arquitectura nueva, no asumida hoy. La faceta de **notificaciones del sistema** (sistema→persona,
`notifications`/`notification_preferences`/plantillas) tampoco se ha diseñado — queda fuera de
ambos subsistemas descritos arriba, un tercer subsistema (C) a definir si hace falta.

---

## 9. Módulo de autenticación / onboarding

> Principio rector: **una cuenta debe corresponder de forma verificable a un ser humano real**
> (mitigación de ciberacoso, cuentas de desecho, datos de menores).

**Qué existe (real, no solo diseñado):** nadie se autorregistra — la institución crea los registros
(`Core::People::Resolver`) y la persona completa su cuenta vía invitación
(`IdentityAccess::Invitations::Completer`); login nativo Rails 8 + MFA por correo (OTP,
rate-limited, anti-enumeración, lockout a 5 intentos); auditoría append-only real a nivel de rol de
BD (`REVOKE UPDATE/DELETE`); gestión de personas (crear/invitar/reenviar/suspender/reactivar) con
permiso `people.manage`; suspensión con efecto real (bloquea login y quita grants en la siguiente
request); `national_id` cifrado determinísticamente en ambos lados (`Core::User` global,
`GroupManagement::Student` tenant-scoped) como identificador de *alcance*, nunca credencial.
Detalle completo de cómo se construyó (⚠-1/⚠-2 cerrados, el gotcha de `has_secure_password`, la
tabla pieza-por-pieza) en `HISTORIA.md`.

### 9.1 Pendiente real (no implementado, no solo "vista futura")

1. ~~`Core::RosterImport::Parser` / `Validator` / `Committer`~~ — ✅ **real para AMBOS kinds**
   (estudiantes v1.7.0, acudientes v1.8.0 — ver `HISTORIA.md`). Estructura por-kind (G7):
   `Core::RosterImport::Strategy.for(kind, institution:)` → `Strategies::{Students,Guardians}`;
   `Parser`/`Validator`/`Committer` son kind-agnósticos, nunca ramifican. Estudiantes: upsert
   DIRECTO de `GroupManagement::Student` por `national_id`, sin `Core::People::Resolver` (ese
   resolver crea `Core::User`, un estudiante K-12 normalmente no tiene login). Acudientes: **sí**
   vía `Resolver` (crea `Core::User` + membresía `institution_users`, **cero `role_assignments`** —
   un acudiente no es staff) + `find_or_create` aditivo del link `guardian_students` (nunca borra un
   link ausente del CSV). Ninguna migración fue necesaria en ninguno de los dos slices.
2. ~~`Core::Access::GuardianScope`~~ — ✅ **cerrado (v1.9.0)**. Portales de estudiante/acudiente
   cableados a datos reales de relación (`GuardianScope`/`StudentSelfScope`); los stubs
   `Portals::{Guardian,Student}Dashboard` se eliminaron. Caso de aceptación de seguridad (cross-
   tenant + revocados + sin buscador) verificado end-to-end — ver `HISTORIA.md`.
3. ~~Vistas de "mis datos" con datos reales para docente/coordinador/director~~ — ✅ **cerrado
   (v1.10.0)**. `/mis_datos`, identity-gated (`Core::Access::StaffProfileScope`/
   `StaffRoleAssignmentsScope`), sin `authorize!`. "Mis grupos"/"mi departamento" se derivan de los
   `scope_group_id`/`scope_department_id` de los propios `role_assignments` vigentes del actor — no
   existe ningún vínculo directo profesor↔grupo en el esquema (`sections` no tiene
   `homeroom_teacher_id`). "Mi horario" queda como **vista previa** (reusa el stub de `schedules`,
   filtrado por identidad, nunca por RBAC) — ese dominio no tiene ninguna tabla real todavía, ni
   siquiera parcial. Ver `HISTORIA.md`.
4. ~~Visor de `audit_events` + bandeja de discrepancias reportadas~~ — ✅ **cerrado (v1.11.0)**.
   `IdentityAccess::AuditEventIndex` (tenant-scoped, filtros actor/acción/fecha sobre un conjunto
   conocido, paginado) alimenta tanto el visor (`/identity_access/audit_events`) como la bandeja
   (`/identity_access/audit_events/discrepancies`, misma query con `action` fijo al marcador de
   `DiscrepancyReporter`). RBAC-gateado (`audit_events.read`, permiso nuevo), a diferencia de las
   superficies self-service. Hallazgo del recon: faltaba un índice `(institution_id, created_at)`
   real para paginar ordenado — única migración del slice. Ver `HISTORIA.md`.
5. **`IdentityAccess::Expirer`/`BounceHandler` sin disparador real** — `Expirer` corre
   oportunísticamente desde `PeopleController#index` (no hay job recurrente en Solid Queue);
   `BounceHandler` no está conectado a ningún webhook real. (`Resender` no se construye — `Issuer`
   ya invalida-y-recrea.) Único remanente (opcional) del track de onboarding.
6. **`IdentityAccess::PermissionCheck` real** — ✅ **cerrado** (P1, v1.6.0) — ver `HISTORIA.md`. Nota
   operativa: cada persona nueva creada vía `PeopleController` necesita `RoleAssignment` reales para
   tener acceso (ya no hay persona stub que la cubra por defecto) — el punto de extensión para
   asignar rol al crear persona **no se construyó** (follow-up señalado, no bloqueante hoy porque
   `PeopleController` sigue siendo el único camino de alta y es un flujo manual/administrado).
7. **Job de Solid Queue que reestablece el GUC de tenant** — ✅ **cerrado**, primer caso real en
   S3a (`Core::Headcount::SnapshotJob`). El patrón durable a copiar vive en Guardrails (§13); el
   relato completo del bug encontrado vive en `HISTORIA.md`.

### 9.2 Consideraciones legales (Colombia) — vigentes

Datos de menores y Habeas Data (Ley 1581 + decreto de datos de NNA): documento y datos de menores
como sensibles desde el diseño; minimizar dónde vive el documento en claro (ya mitigado con cifrado
determinístico); registrar el tratamiento. El eslabón crítico es la **captura del correo en
matrícula** → su validación es deber institucional explícito (todavía no hay UI de matrícula que lo
capture). Rate limiting/bloqueo en login, OTP y completado — **implementado**. Nunca exponer
directorios de estudiantes ni autocompletar por documento/nombre — `Core::Access::GuardianScope`
(✅ real, v1.9.0) **nació y se mantiene sin buscador** — verificado con test que inspecciona la
firma del método (no acepta término de búsqueda) y con aserciones sobre la vista (sin
`input[type=search]`/`input[name=q]`/`form[action*=search]`).

---

## 10. Decisiones abiertas / bordes a resolver

> Los bordes ya cerrados (⚠-1, ⚠-2, B1) tienen el razonamiento completo en `HISTORIA.md` — aquí
> solo el estado.

| # | Borde | Contexto | Estado |
|---|---|---|---|
| ⚠-1 | Identidad global vs. un-correo-un-tenant | — | ✅ Cerrado — ver `HISTORIA.md`. No re-abrir sin razón de negocio explícita. |
| ⚠-2 | Dónde vive el campo de documento | — | ✅ Cerrado — ver `HISTORIA.md`. |
| **E** | **CHECKPOINT E: `teacher_management` → `staff_management`** | Personal no docente (cocina, transporte, etc.) sin dominio claro. D1 (generalizar) vs D2 (dominio HR nuevo). | ✅ **Cerrado (D1, v1.12.0)** — ver `HISTORIA.md`. Recon reveló que D1 **ya estaba resuelto en el esquema desde el primer commit** (`637a998`, anterior al track de onboarding): `StaffManagement::StaffMember` es el empleo generalizado de TODO staff (`staff_category` incl. `teaching`, `department_id` nullable); `TeacherManagement::Teacher` es su extensión docente vía `teachers.staff_member_id` (FK nullable, aditiva). No fue necesaria ninguna migración ni rename — el gap real era que nadie lo había verificado end-to-end para un staff NO docente ni corregido este documento (que describía `staff_management` como "solo cierra un nav huérfano", cierto de la vista pero no del modelo). Las vistas de directorio (`StaffManagement::StaffRoster`/`TeacherManagement::TeacherRoster`) siguen en stub — eso es backlog #4, no este checkpoint. |
| B1 | Estudiantes sin login | `students.user_id` nullable. | ✅ Confirmado como diseño (ver `HISTORIA.md`). Consistencia en portales ya documentada (v1.9.0): un estudiante sin `user_id` propio simplemente no tiene cuenta con la que entrar al portal en primer lugar (no hay sesión que iniciar); uno CON `user_id` pero cuyo registro aún no se resolvió ve el empty state de `Core::Access::StudentSelfScope`, nunca un error. |
| **B2** | **Fechado efectivo de asignaciones vs. `academic_terms`** | ¿`role_assignments.valid_from/until` se acopla a ciclos lectivos o es independiente? | ✅ **Cerrado (v1.57.0)** — opt-in por asignación: `role_assignments.academic_term_id` (nullable). Cuando se setea, `Core::AcademicTermsController#close` capa `valid_until` al `ends_on` del término (solo si sigue abierta — `valid_until: nil`); sin setear, comportamiento sin cambios (fechas independientes). Ver `HISTORIA.md` v1.57.0. |
| **M1** | **Unidad de metering por dominio medido** | El control plane solo consume rollups; `addons.unit` sigue provisional. | 🟡 **Medio cerrado (v1.30.0 S3b + v1.52.0 + v1.54.0 + v1.55.0).** Cierra per-dominio, no de una vez — 10 dominios ya emiten real (`communication`/mensajes, `attendance`/registros, `report_cards`/boletines, `assignments`/entregas, `extracurriculars`/inscripciones, `finance`/transacciones desde v1.30.0; `cafeteria`/compras y `transportation`/abordajes desde v1.52.0; `library`/préstamos vía `Library::LoanRecorder` desde v1.54.0; `admissions`/solicitudes vía `Admissions::ApplicationSubmitter` desde v1.55.0 — ambos cableados desde el día uno, sin retrofit — todos vía `ControlPlane::Usage::Ingest.emit`). Sigue abierto solo para `student_support`/`counseling`/`analytics_bi`/`schedules`-timetable (Clase C o sin evento de negocio claro todavía), ver `OPEN_PROCESS.md`. |
| P1 | `IdentityAccess::PermissionCheck` real | — | ✅ Cerrado (v1.6.0) — ver `HISTORIA.md`. |
| **P2** | **Rol libre `institution_users.role`** | Columna string sin lectores en el código (ver §5). Puede generar confusión con el RBAC real. | ✅ **Cerrado (v1.57.0), alcance achicado por decisión del owner** — conectada a dos usos reales: filtro en "Personas" (`IdentityAccess::PeopleController#index`) y una fila "Rol de cuenta" en "Mis datos" (`SelfServiceController`). El organigrama/jerarquía que el owner también pidió es una feature bastante más grande (catálogo de cargos + relación de reporte) — quedó como su propio ítem de backlog nuevo (`OPEN_PROCESS.md`), sin construir. Ver `HISTORIA.md` v1.57.0. |
| **Cav.** | **Headcount de `base_seats` no filtra por matrícula/término** — **mitad de MODELO cerrada (v1.15.0), mitad de FACTURACIÓN sigue abierta a propósito** | `enrollments.academic_term_id` (FK real, nullable) ya conecta `Schedules::Enrollment`↔`academic_terms` — `Schedules::ActiveTermEnrollmentScope` resuelve "estudiantes matriculados en el término activo" de verdad. **Pero** `Core::Headcount::Snapshotter` sigue contando `students.status == "active"`, deliberadamente sin tocar (F3) — S4 sigue facturando sobre ese mismo número. | 🟡 **Medio cerrado.** El join ya existe y es consumible por slices académicos (asistencia, notas-por-término, actividades, asignaciones — ver `LINEAMIENTOS_MVP.md`). Facturar sobre "matriculado en el término activo" en vez de `status == "active"` es una **decisión separada y explícita**, no reabierta aquí — ver Guardrails (§12) y `HISTORIA.md` v1.15.0. |

---

## 11. Próximas iteraciones (backlog pendiente)

> **Vive en `OPEN_PROCESS.md`.** Ese archivo se reorganizó (2026-07-21) para contener SOLO la lista
> de pendientes de los siguientes slices — nada cerrado. Todo lo ya realizado queda registrado aquí
> (mapa de dominios §4, "estado en una línea" en Metadatos, §10 decisiones abiertas) y en el
> changelog completo de `HISTORIA.md`; `OPEN_PROCESS.md` ya no repite esa narrativa.

---

## 12. Changelog

El changelog completo, versión por versión (`v1.0.0` → última), vive en **`HISTORIA.md`** — este
documento no repite entradas individuales (limpiado en la reorganización de 2026-07-21; antes
acumulaba una entrada por versión hasta `v1.5.1`, 100% duplicada con `HISTORIA.md`).

---

## 13. Guardrails operativos (memoria operativa acumulada)

> **Movido aquí desde `OPEN_PROCESS.md` en la reorganización de 2026-07-21.** Son invariantes ya
> ASENTADOS (no tareas pendientes) — la razón de que vivan en este documento y no en el backlog.
> Se agrega uno nuevo cada vez que un slice deja una lección estructural que un slice futuro
> necesita conocer ANTES de construir (un patrón a copiar, un error ya cometido y corregido, una
> regla de negocio no-negociable). Consultar esta sección antes de tocar cualquier dominio que ya
> tenga guardrails propios.

- Migraciones: **`bin/migrate`**, nunca `rails db:migrate`. `edu_migrator` con `CREATE` en las 4 bases.
- **Migrar development NO migra test** — correr también `RAILS_ENV=test bin/migrate` tras cada migración nueva, o el test suite falla con `NoMethodError` (no con un error de SQL) sobre la columna/tabla nueva.
- `EDU_MIGRATOR_PASSWORD` es solo de entorno, nunca vive en el repo. Si se pierde en dev local, resetear con `psql` (auth `trust` del superusuario del SO) vía `ALTER ROLE edu_migrator PASSWORD '...'` — nunca dejar la contraseña en archivos temporales tras usarla.
- **Timestamps de migración no pueden ser >24h futuros respecto al reloj real de la máquina** (Rails 8 valida `version.to_i < (Time.now.utc + 1.day).strftime(...)`) — generar el timestamp con el reloj real al momento de escribir el archivo, no proyectando una fecha narrativa.
- **Suite de tests: correr en serie.** La paralelización por fork (`workers: :number_of_processors`, default cuando la suite pasa ~50 tests) **crashea el proceso Ruby** en esta máquina (YJIT + fork). `PARALLEL_WORKERS=1 bin/rails test` corre limpia (Rails respeta esa env var como override de `parallelize`, sin tocar `test_helper.rb`). Un crash de proceso ≠ fallo de test — no asumirlo sin correr en serie primero.
- **Sin `default_scope`** para tenancy ni scope de rol — siempre Query objects.
- **RLS con `FORCE`**; runtime sin `BYPASSRLS`; cross-tenant solo por el rol auditado (`edu_bi_reader`).
- **`authorize!` en el controlador** (puerta dura, llamada a mano dentro de cada acción); `can?` en la vista solo cosmético.
- **Sin `RoleAssignment` real que aplique = cero permisos (fail-closed), siempre** (P1, cerrado) — no hay fallback a ninguna persona stub en runtime; `Authorization::StubAssignments`/`StubResolver` quedan retirados (solo `StubResolver` sigue vivo como contexto fijo para un caso de test puntual, ver `HISTORIA.md`). Cada persona nueva creada vía `PeopleController` necesita `RoleAssignment` reales para tener acceso — importante para onboarding real.
- **`has_secure_password` con `validations: false` en `Core::User`** — un `password_digest` nulo es un estado válido (persona invitada, no completada). No "arreglar" agregando de vuelta la validación de presencia por defecto sin releer la narrativa en `HISTORIA.md` (§9.4 original).
- **Suspender (`institution_users.status`) bloquea login y quita grants reales en la siguiente request** (confirmado, P1) — `Current#resolve_institution_user` solo resuelve membresías `active`, así que sin `institution_user_id` el motor real (`IdentityAccess::PermissionCheck`) no carga ningún `role_assignment`: cero permisos, de verdad. NO destruye sesiones ya abiertas de otras instituciones del mismo usuario.
- **Sin gemas nuevas** salvo bottleneck documentado e irresoluble con lo nativo. `bcrypt` no cuenta como nueva — es la gema que el propio scaffold de Rails deja comentada para `has_secure_password`.
- Propshaft sin `@import`/Sass; importmap sin build; **tokens-only** (no tocar `tokens.css` salvo token faltante justificado); accesibilidad AA.
- Cross-domain siempre por **FK + stub** hasta que exista el modelo real; no inventar tablas de otros dominios.
- El **plano de control fuera de `app/domains/*`** — no scopearlo por `institution_id` por costumbre.
- **Zeitwerk colapsa `app/domains/*/{models,queries,services,jobs,policies}`** — un archivo en `app/domains/<d>/services/<ns>/foo.rb` define `<D>::<Ns>::Foo`, NO `<D>::Services::<Ns>::Foo`. Verificar con `bin/rails zeitwerk:check` antes de dar por buena una constante nueva.
- **Gate #1 (entitlement) siempre antes de gate #2 (RBAC)** — `Entitlement::Controller` (una sola pieza, incluida una vez en `ApplicationController`) corre antes de que la acción llegue a `authorize!`; un módulo no habilitado nunca revela detalles de RBAC. Ver `PROJECT_STATE.md` §7.1.
- **Los dominios fundacionales nunca se gatean por entitlement** — su ausencia de `Entitlement::Registry` (`config/entitlements/*.rb`) ES la señal de "no gateado"; no existe (ni debe crearse) una lista aparte de "fundacionales".
- **`Entitlement::Registry` no referencia `ControlPlane::AddonCatalog::DOMAIN_KEYS` en runtime** — solo `test/models/entitlement/registry_consistency_test.rb` cruza ambas listas. No "simplificar" el runtime del inquilino para que lea la constante del control plane directamente; ese acoplamiento es exactamente lo que este seam evita.
- **`sign_in_as_member` (test helper) otorga entitlement de todos los dominios gateados por defecto** — la institución efímera de test se comporta como un tenant completamente aprovisionado. Un test que necesite el escenario "no entitled" debe **revocar** el dominio específico, no asumir que parte sin ninguno.
- **`sign_in_as_member` (P1) también otorga por defecto un `RoleAssignment` real institución-wide** (`grant_default_role: false` para optar por "cero grants"). Un test que necesite un escenario de scope MÁS ANGOSTO llama a `with_grants`/`grant_role!` (test_helper.rb) — `with_grants` **revoca primero** cualquier asignación existente antes de sembrar la nueva (los `RoleAssignment` reales solo SUMAN, no se reemplazan entre sí como sí hacía swapear `Authorization::StubAssignments.all`).
- **`scope_department_id`/`scope_grade_level_id`/`scope_group_id` tienen FK real** (a `departments`/`grade_levels`/`sections`) — sembrar un `RoleAssignment` con scope real exige que esa fila exista primero (`grant_role!` lo hace por `find_or_create_by!(id: scope_id)`). Reusar el mismo id fijo entre archivos de test distintos es seguro porque cada test corre en su propia transacción revertida (fixtures transaccionales) — no hay colisión entre tests.
- **Todo job nuevo que toque datos tenant-scoped debe heredar `ApplicationJob`** (no reinventar el manejo del GUC) — ese mecanismo incluye un `Tenant::Guc.reset!` explícito en el `ensure`, no solo confiar en que el `COMMIT` de la transacción limpie el `SET LOCAL` (dentro de un test envuelto en una transacción englobante, la transacción del job es un SAVEPOINT, y Postgres no limpia `SET LOCAL` al liberar un savepoint).
- **Verificar "el GUC no se filtró" con una query real bajo RLS, nunca con una relectura de `current_setting()`** — esa relectura puede devolver un valor obsoleto por el query cache de ActiveRecord dentro de una transacción, dando un falso positivo de fuga (o, peor, un falso negativo). La prueba real es: sin ningún GUC fijado, ¿una tabla RLS-protegida devuelve cero filas?
- **El headcount cuenta `GroupManagement::Student.status == "active"` de la institución, NUNCA matrícula/término — a propósito, y esto sigue así aunque el join YA exista (v1.15.0, F3).** `enrollments.academic_term_id` (FK real) y `Schedules::ActiveTermEnrollmentScope` existen y son consumibles desde v1.15.0 — pero el headcount de facturación (S3a/S4) deliberadamente NO los usa. `academic_term_label` en el snapshot sigue siendo solo una etiqueta descriptiva, nunca un filtro del conteo. Facturar sobre "matriculado en el término activo" en vez de `status == "active"` es una **decisión de negocio separada y explícita** (reabrir Cav. del lado de facturación) — nunca un efecto colateral de cablear un consumidor académico del nuevo query object. Verificado con un test de regresión dedicado (`Core::Headcount::SnapshotterTest`, v1.15.0).
- **El corte de periodo (`ControlPlane::Billing::PeriodCut`) nunca re-lee el catálogo vivo** — solo el `price_tiers_snapshot`/`base_price_per_student_cents` congelados en `subscriptions` al firmar (S2a). Si un futuro cambio "optimiza" esto para leer `plans`/`plan_price_tiers` directamente, rompe la garantía de snapshot inmutable que todo el track de billing (S2a→S4) depende de sostener.
- **Los overrides se aplican con `coalesce(override, catálogo)` campo por campo, nunca de-todo-o-nada** — un entitlement puede tener override de fee pero no de cupo, por ejemplo. `source_ref` de cada línea registra si se aplicó un override, para que la factura sea defendible sin tener que abrir la base de datos.
- **`InvoiceLineItem#readonly?` bloquea `update`/`destroy` de una fila individual, pero `PeriodCut` regenera un `draft` con `invoice.line_items.delete_all`** (bulk SQL, bypasea `readonly?` a propósito) — no confundir "una línea no se edita nunca" con "un borrador no se regenera nunca". Solo `delete_all`/`create!` desde el servicio, nunca `destroy`/`update` de una línea suelta.
- **Nunca escribir código después de un `.enqueue`/`.enqueue_for` que dependa del GUC del tenant seguir fijado** (RosterImport, v1.7.0) — bajo el adaptador de test de ActiveJob (`perform_enqueued_jobs`) un job se ejecuta SINCRÓNICAMENTE, y `ApplicationJob#around_perform` resetea incondicionalmente el GUC en su `ensure` al terminar; cualquier query tenant-scoped (p. ej. un `Audit.log`) que corra DESPUÉS de encolar en la misma acción puede fallar RLS aunque siga siendo, técnicamente, la misma request. Esto no es solo un artefacto de test — cualquier adaptador de cola que corra inline expone lo mismo. Ordenar siempre: auditar/hacer trabajo tenant-scoped **antes** de encolar, nunca después.
- **Cifrar un valor suelto dentro de una columna `jsonb` (no un atributo `encrypts` completo)** — usar la API de bajo nivel `ActiveRecord::Encryption.encryptor.encrypt/decrypt(valor, key_provider: ActiveRecord::Encryption.key_provider, cipher_options: { deterministic: true })` (ver `Core::RosterImport::Cipher`), NO la macro declarativa `encrypts` (que opera sobre un atributo AR entero, no sobre una clave dentro de un hash jsonb). Mismo cifrado determinístico que cualquier `encrypts ..., deterministic: true` existente, así que sigue siendo comparable contra esos atributos vía query normal (Rails encripta el lado de la query de forma transparente).
- **Al enmascarar un dato sensible para mostrarlo en una vista, nunca reveles más de la mitad de los caracteres** — una regla de "muestra los últimos 4" revela el valor COMPLETO si el dato tiene 4 caracteres o menos (encontrado real en RosterImport's preview, v1.7.0, no solo hipotético). Calcular el tramo visible como `[largo/2, N].min`, nunca un valor fijo sin tope relativo al tamaño real. (v1.8.0: esta regla se movió de un helper de vista a `Core::RosterImport::Cipher.mask` — cada strategy decide qué se enmascara en su propio `preview_columns`; la vista nunca decide qué es sensible.)
- **Un acudiente (`Core::RosterImport` kind `guardians`) obtiene SIEMPRE cero `IdentityAccess::RoleAssignment`** — no es staff, su acceso es por relación (`guardian_students`), nunca por RBAC. `Core::People::Resolver` ya garantiza esto por construcción (nunca toca `role_assignments`); no "arreglar" esto agregando un rol "para que el portal funcione" cuando `GuardianScope` se construya — el portal se gatea por relación, no por permiso.
- **El upsert de un link `guardian_students` (o cualquier vínculo similar) es SIEMPRE aditivo: un CSV/import nunca borra un vínculo ausente del archivo** — solo crea vínculos nuevos o re-afirma (actualiza `relationship`, reactiva si estaba `revoked`) los que el archivo menciona. Desvincular es una acción explícita y separada, nunca un efecto secundario de un re-import. Verificado con un test dedicado ("el link ausente del CSV sobrevive"), no solo inferido.
- **Estrategia por-kind para pipelines multi-tipo (`Core::RosterImport::Strategy.for`)** — cuando un mismo pipeline (parse→validar→commit) debe soportar variantes con reglas distintas, extraer un objeto-estrategia por variante (columnas, validación, upsert, preview) y mantener la orquestación compartida sin ningún `if kind == ...`. Añadir un tipo nuevo es agregar una clase, no editar las existentes ni sus tests.
- **El portal de una persona (estudiante/acudiente) se gatea SIEMPRE por relación (`Core::Access::GuardianScope`/`StudentSelfScope`), nunca por RBAC** — ninguno de los dos tiene ni necesita `authorize!`; el query object explícito+scoped ES la puerta. No agregar un rol/permiso "para que el portal funcione" cuando se cablee un dominio nuevo dentro de él (backlog #4) — el gate sigue siendo la relación, el entitlement (si aplica al dominio colgado) se resuelve aparte. Y **`GuardianScope` nace y se mantiene sin buscador** — ninguna versión futura le agrega un parámetro de término/búsqueda; es el invariante de Habeas Data no negociable de este proyecto.
- **`GuardianScope`/`StudentSelfScope` siguen sin filtrar por término, por elección, no por limitación de esquema** — desde v1.15.0 el join `enrollments.academic_term_id`↔`academic_terms` SÍ existe (`Schedules::ActiveTermEnrollmentScope`), así que ya no es cierto que "no hay cómo". Estos self-scopes resuelven "mis hijos activos"/"mi propio registro" por relación pura, sin necesidad de acotar por término — agregarlo sería scope creep, no una corrección. Si un slice futuro quisiera "mis hijos matriculados este término" como una vista DISTINTA, compone `ActiveTermEnrollmentScope` aparte; no se modifica el self-scope existente.
- **El autoservicio de staff se gatea por identidad (self-scope en `services/access/`), nunca por `authorize!`** — muestra solo datos **propios** (perfil, roles vigentes, grupos/departamento derivados de las propias `role_assignments`). Mostrar a OTRA persona (mis colegas de departamento, mis estudiantes) es supervisión (backlog #4, RBAC con scope), no autoservicio — cruzar esa línea saca la vista de esta sección. Ninguna de estas superficies (student/guardian/staff) vive en `Navigation::Registry`, porque esa lista SIEMPRE filtra por `can?`; se enlazan aparte (header del shell / portal).
- **El filtro de término activo es real solo donde el FK existe** — `role_assignments.effective_now` (P1); `enrollments.academic_term_id`↔`academic_terms` (v1.15.0, matrícula por término, cierra la mitad de modelo de Cav.). **La salvedad B2 sigue vigente para `role_assignments.valid_from/until`** (fechado independiente del calendario lectivo, sin FK a `academic_terms` — un borde DISTINTO al de matrícula, no tocado por v1.15.0). `schedules`' mitad de horario/timetable es real desde v1.50.0 (`meeting_patterns`), pero SIN ningún FK a `academic_terms` — un patrón de reunión no está acoplado a un término lectivo específico, mismo borde B2 sin resolver. El headcount de facturación sigue sin filtrar por término A PROPÓSITO (ver el guardrail de headcount, arriba) — no confundir "el join ya existe" con "todo lo que toca estudiantes ya filtra por término".
- **El visor de auditoría se gatea por RBAC (`authorize!`), a diferencia de las superficies self-service — y `audit_events` es append-only: ninguna vista lo muta.** Un futuro "marcar atendida" (si se construye) **es un evento nuevo append** (`Audit.log` de, p. ej., `discrepancy_acknowledged`) con estado **derivado** de su presencia — nunca un `UPDATE` sobre la fila original (la BD ya lo impediría: `REVOKE UPDATE, DELETE ON audit_events FROM edu_app_runtime`).
- **Los filtros del visor de auditoría nunca son un directorio de estudiantes** — actor/acción/fecha sobre metadata, con acción tomada de un **conjunto conocido** (`IdentityAccess::AuditEventIndex::ACTIONS`), jamás texto libre ni autocompletado de personas (Habeas Data, mismo invariante que `GuardianScope`). El filtro de actor sí lista al STAFF de la propia institución (no son menores; es la misma superficie ya visible en "Personas").
- **El staff vive en UN SOLO hogar (`staff_management`), y el docente es una especialización, no un dominio aparte** (CHECKPOINT E, D1, v1.12.0) — `StaffManagement::StaffMember` es el empleo generalizado de TODO staff (`department_id` **nullable** para no-académicos); `TeacherManagement::Teacher` es su extensión docente vía `teachers.staff_member_id` (FK **nullable, aditiva** — el link puede faltar sin que el perfil base esté incompleto). `Core::Access::StaffProfileScope` ya resuelve a ambos por identidad, sin distinguir. Esto **ya estaba en el esquema desde el primer commit** — no inventar una migración de "generalizar staff" sin releer `HISTORIA.md` v1.12.0 primero. Los directorios `StaffRoster`/`TeacherRoster`/`DepartmentRoster` **ya son reales** desde v1.13.0 (ver abajo).
- **El molde de vistas de negocio (#4) es `teacher_management` (los cinco esqueletos de §6.6 de `PROJECT_STATE.md`, per-row `can?`); los demás dominios lo COPIAN, no lo reinventan** — un slice de #4 sobre `core`/`student_support`/`group_management`/`schedules`/`counseling`/`cafeteria`/`transportation` que introduce un query object de índice con una forma distinta a `TeacherScope`/`DepartmentScope`/`StaffScope` (institución explícita + per-row `can?` vía `.select`, nunca `default_scope`, nunca forzar `PermissionCheck#scope_for`) debe justificar por qué, no asumirlo por defecto.
- **#4 es supervisión: RBAC + scope + `Navigation::Registry`; la contracara del autoservicio (identidad, `/mis_datos`) — no confundir las dos superficies para un mismo dominio.** Un índice que muestra a OTRAS personas dentro del alcance del actor SIEMPRE lleva `authorize!` y vive en el registry; uno que muestra "lo mío" nunca lleva `authorize!` y nunca vive ahí. `teacher_management`/`staff_management` ya tienen ambas superficies simultáneamente y son el ejemplo canónico de que no se pisan: `/mis_datos` (autoservicio) y `/teacher_management/*`+`/staff_management/staff` (supervisión) leen las MISMAS tablas por caminos de acceso completamente distintos.
- **Triage A/B/C/S antes de cablear CUALQUIER dominio de #4** (barrido v1.14.0) — antes de aplicar el molde §6.6 a un dominio nuevo, verificar contra el disco (no contra la presencia de un query object/roster, que puede ser 100% stub) si sus tablas de negocio SON reales. `student_support` parecía cableable (tenía `queries/`/`services/` con nombres "reales") y resultó ser Clase C completo — cero migración para `disciplinary_logs`/`medical_history`/`accommodations` en todo el repo. **La señal real es `grep create_table` en `db/migrate/`, nunca la presencia de un archivo con nombre de query object.** Ver `CONCEPTOS_TECNICOS.md` para la taxonomía completa (A/B/C/S).
- **Un dominio con modelos reales pero CERO vista/controller (`finance`) no es lo mismo que un stub a reemplazar** — cablearlo es construir desde cero (rutas, controller, nav), no swapear una fuente de datos. Tratarlo como su propio slice de alcance distinto, no como un ítem más del barrido de #4.
- **`students.section_id`/`Schedules::Assessment` ya existían como target real** — a diferencia de `teacher.evaluate` (v1.13.0, sin modelo destino), la acción de matrícula de `group_management` y el registro de nota de `schedules` SÍ se cablearon como escritura real completa, no solo el gate. La regla general (BV6): cablear el gate siempre; el workflow completo SOLO si el modelo destino ya existe — verificarlo caso por caso, no asumir "gate-only" por defecto.
- **`Schedules::ActiveTermEnrollmentScope` es EL resolver canónico de "estudiante matriculado en el término activo"** (v1.15.0, cierra la mitad de modelo de Cav./B2) — todo slice académico futuro (asistencia, notas-por-término, actividades, asignaciones) lo CONSUME cuando el hecho crudo que necesita ES matrícula por término (ver la salvedad de mensajería y de `assignments` más abajo — la elección de resolver es semántica caso por caso, no automática). No es identity-gated (a diferencia de `GuardianScope`/`StudentSelfScope`) ni RBAC-scoped por sí mismo — resuelve el hecho académico crudo; cada consumidor aplica su propio scope de RBAC encima (mismo layering que `TeacherManagement::TeacherScope`). Solo un término activo por institución (invariante de BD ya existente) — el query NUNCA reimplementa esa resolución, solo lee `Core::AcademicTerm.active`.
- **"Roster tomable" = resolver académico ∩ scope de negocio ∩ scope RBAC — tres capas, nunca colapsadas en una** (`attendance`, v1.16.0, primer consumidor real de `ActiveTermEnrollmentScope`): (1) `ActiveTermEnrollmentScope.resolve(institution:)` — el hecho crudo, institución completa; (2) `.where(section_id: group.id)` — el scope de NEGOCIO (este grupo/homeroom); (3) `authorize!("attendance.record", group)` + el query object per-row (`Attendance::GroupScope`) — el scope de RBAC del actor (sus propios grupos). El mismo layering aplica a todo slice académico futuro que "actúe sobre los alumnos de un grupo en el término activo" (notas-por-término, actividades, asignaciones) — no reinventar el orden ni colapsar las tres capas en una sola query ad hoc.
- **Un dominio net-new addon-gated real desde el día uno (sin fase stub) es un patrón válido** (`attendance`, v1.16.0) — a diferencia de los dominios de la Fase 0 (que nacieron 100% stub y se cablearon después, backlog #4), un dominio construido HOY con recon-first + checkpoint de diseño no necesita pasar por una fase de roster en memoria: el modelo real, el query object y las vistas se construyen en el mismo slice.
- **Un snapshot congelado al publicar NUNCA se recomputa al leer** (`report_cards`, v1.17.0) — `ReportCards::Publisher` llama a `ReportCards::Computation` UNA sola vez, en el momento de publicar, y persiste el resultado en `lines_snapshot`/`overall_average`. Ningún controller/vista de un `ReportCard` ya publicado vuelve a tocar `Schedules::Assessment`; editar una nota viva después de publicar nunca cambia lo ya publicado (verificado con un test de regresión dedicado, el análogo del test de headcount). El "draft" es la única superficie que lee en vivo (`ReportCards::Computation`, consumido también por el preview de supervisión) — nunca hay una fila con `status: "draft"` hoy (ver el modelo de datos: la fila solo existe al publicar).
- **`ReportCard#readonly?` (= `persisted?`, mismo patrón que `ControlPlane::InvoiceLineItem`) bloquea `update`/`destroy` de una fila individual — la regeneración va SIEMPRE por `delete_all` + `create!` desde `ReportCards::Publisher`, nunca por un `destroy_all`/`update` sobre el AR object.** `destroy_all` instancia cada registro y llama a `#destroy`, que Rails bloquea si `readonly?` es true (a diferencia de `delete_all`, que emite el DELETE en bulk sin pasar por el guard) — el mismo error que atrapó a este slice en desarrollo (`ActiveRecord::ReadOnlyRecord` al intentar `destroy_all`). Mismo balance que `ControlPlane::Billing::PeriodCut` ya documenta para `InvoiceLineItem`: "una línea no se edita nunca" ≠ "un snapshot no se regenera nunca".
- **El portal de una persona (§ guardrail de arriba, `GuardianScope`/`StudentSelfScope`) sigue sin chequear `Entitlement::Registry`** (`report_cards`, v1.17.0, mismo patrón ya implícito en `cafeteria`/`transportation`'s portal stubs; confirmado de nuevo en `finance` v1.18.0, `communication` v1.19.0/v1.20.0, y `assignments` v1.21.0) — `Entitlement::Controller` infiere el `addon_key` del namespace del controller (`Portals::*`, nunca registrado), así que ningún controller de portal queda gateado por addon hoy. Es una superficie ya aceptada, no un gap nuevo de ningún slice — si algún día se decide gatear el portal también, es una decisión de diseño explícita (probablemente `gated_by_addon`), no un efecto colateral de cablear un dominio addon-gated más.
- **`report_cards` es el segundo consumidor real (tras `attendance`) del layering de tres capas — y confirma que la RBAC puede partirse en dos permisos (`report_card.view`/`report_card.publish`) sobre las MISMAS tres capas**, a diferencia de `attendance.record` (un solo permiso). Publicar es una acción más sensible que previsualizar (mismo espíritu que el split `accommodations.view`/`accommodations.manage`) — un dominio futuro con esa misma asimetría debe partir el permiso, no forzar uno solo por "seguir el molde de attendance" literalmente.
- **El dinero de `finance` (`student_accounts.balance`, `charges.amount`, `payments.amount`, `payment_plans.total_amount`, `installments.amount`) es `decimal(12,2)`, NO `*_cents bigint`** (`finance`, v1.18.0) — comiteado en el primer commit del repo, antes de que F6 (cents-bigint) se adoptara para el billing del control plane. `decimal`/`BigDecimal` es aritmética exacta en Postgres/Ruby, no tiene el problema de drift que F6 previene — es una representación DISTINTA, igualmente segura, no una violación de F6 disfrazada. **No migrar estas columnas a bigint-cents "para unificar" sin una razón de negocio explícita** — el invariante real y no negociable aquí es "nunca castear a Float, toda la aritmética de saldo en `BigDecimal`", verificado con tests que comparan `BigDecimal` exacto, nunca `Float`. F6 (cents-bigint) sigue vigente para todo lo que nace en el control plane (`ControlPlane::Addon`/`Invoice*`/`Subscription`) — los dos esquemas de dinero coexisten a propósito, por historia, no por descuido.
- **Toda escritura de dinero pasa por un servicio, nunca por el controller directo, y es transaccional con row lock (`account.lock!`) sobre `StudentAccount`** (`Finance::PaymentRecorder`/`Finance::ChargeCreator`, v1.18.0) — el `Payment`/`Charge` y el update del saldo ocurren en la MISMA transacción; si cualquier paso falla (p. ej. un `method`/`status` que viola el `CHECK` de la BD), la transacción entera revierte y no queda ni el registro de dinero ni el saldo tocado (verificado con un test de atomicidad dedicado). El lock es pessimista (`.lock!`, serializa) sobre el `lock_version` optimista que la tabla YA tenía desde el primer commit — ambos coexisten sin conflicto porque el lock pessimista impide que la condición de carrera que el optimista detectaría llegue a ocurrir.
- **Idempotencia de pago/cargo: el controller genera un `idempotency_key` una sola vez en `#new` (campo oculto), y el servicio lo usa como guarda — un re-submit del MISMO formulario nunca duplica** (`finance`, v1.18.0) — `Finance::Payment`/`Finance::Charge` YA tenían una columna `idempotency_key` con índice único por institución desde el primer commit (sin usar hasta este slice); el servicio chequea esa clave ANTES y DESPUÉS de tomar el lock (para cerrar la ventana de carrera entre el pre-check y adquirir el lock), y el índice único de la BD es el backstop final si dos requests corrieran verdaderamente en paralelo.
- **Un solo camino de lectura del estado de cuenta (`Finance::AccountStatement`), consumido por supervisión Y portal** (`finance`, v1.18.0) — mismo patrón que `ReportCards::Computation` (v1.17.0): una sola computación, dos superficies, así que las cifras nunca pueden discrepar entre sí. `Finance::Charge` no tiene FK a `Finance::StudentAccount` (solo a `student_id` directo) — este servicio es el único lugar que puentea cuenta→estudiante→cargos, no reinventar ese puente en otro lugar.
- **`finance` ya estaba addon-gated, en `Navigation::Registry` y con sus permisos (`finance.read`/`finance.write`) sembrados desde ANTES de que existiera un controller real** (`config/entitlements/finance.rb`/`ControlPlane::AddonCatalog::DOMAIN_KEYS`/`config/navigation/finance.rb`, todos desde v1.3.0/S2b) — un recon que asuma "dominio sin gating porque no tiene UI todavía" puede estar equivocado; verificar siempre contra `Entitlement::Registry.domains`/`ControlPlane::AddonCatalog::DOMAIN_KEYS`, nunca contra la presencia de un controller. **`finance.read` también lo reusa `Cafeteria::BalancesController`** (su propia función de "Saldos", sin relación con este slice) — cualquier cambio futuro al significado de `finance.read` debe revisar ese consumidor cruzado.
- **Planes de pago/cuotas (`PaymentPlan`/`Installment`) siguen sin ninguna UI y NO alimentan el saldo** (`finance`, v1.18.0, confirmado por recon de modelo: `Installment` no tiene ningún callback/servicio que lo conecte a `StudentAccount.balance`) — el estado de cuenta de este slice es completo y correcto SIN ellos, no una vista parcial. Cablearlos es su propio slice futuro (gestión de cuotas + su propio efecto sobre el saldo, a decidir ahí).
- **"Membresía" es un TERCER tipo de gate, distinto de RBAC y de self-service/relación** (`communication` anuncios, v1.19.0) — RBAC (`authorize!` + scope + `Navigation::Registry`) protege una acción/recurso específico; self-service/relación (`GuardianScope`/`StudentSelfScope`) protege "lo mío"; membresía protege "cualquier miembro activo de esta institución, sin distinción de rol" — más amplio que ambos, pero todavía tenant-scoped (nunca cross-tenant). Su forma: sin `authorize!`, sin permiso, fuera de `Navigation::Registry`, enlazado aparte (mismo `shared/_self_service_link.html.erb`-style partial, ver `shared/_announcements_link.html.erb`), gateado por `Current.entitled_addon_keys.include?(domain)` en vez de por `can?`. El controller sigue viviendo bajo el namespace del dominio (`Communication::FeedController`), así que `Entitlement::Controller` lo sigue gateando por inferencia de namespace igual que cualquier otro — "membresía" cambia el gate #2 (RBAC → ninguno), nunca el gate #1 (entitlement).
- **Publicar (RBAC) y leer (membresía) son DOS superficies con gates distintos sobre el MISMO dominio, y NUNCA se colapsan en una** (`communication` anuncios, v1.19.0) — mismo espíritu que supervisión vs. autoservicio en `teacher_management`, pero con membresía en vez de self-service del lado de lectura. `Communication::AnnouncementsController` (gestión, `announcement.publish`) y `Communication::FeedController` (lectura, membresía) son controllers DISTINTOS aunque lean la misma tabla — un slice futuro que necesite "publicar Y leer" en el mismo dominio debe replicar este split, no fusionar ambos gates en un controller.
- **Retract es SIEMPRE soft (`status` + `retracted_at`), nunca `destroy`** (`Communication::Announcement#retract!`, v1.19.0) — mismo principio que `audit_events` (append-only) y el soft-delete de otros dominios: un anuncio retractado sobrevive como fila, solo desaparece de `Communication::AnnouncementFeed` (que filtra `status: "published"`). Verificado con un test dedicado que confirma la fila sigue existiendo tras retractar.
- **Un solo camino de lectura (`Communication::AnnouncementFeed`), compartido por staff Y ambos portales** (`communication` anuncios, v1.19.0) — mismo patrón que `Finance::AccountStatement`/`ReportCards::Computation`: una sola query, N superficies, nunca pueden discrepar. A diferencia de `finance`/`report_cards` (per-hijo/per-cuenta), este feed es institución-wide — ninguna superficie de lectura pasa por `GuardianScope`/`StudentSelfScope`, el scope es `Current.institution` a secas.
- **`communication` ya estaba addon-gated desde S2b/v1.3.0 (como `finance`) — pero, a diferencia de `finance`, NO tenía entrada en `Navigation::Registry` todavía** — un recon que asuma "si está gateado, también tiene nav" puede estar equivocado; cada pieza (`Entitlement::Registry`, `AddonCatalog::DOMAIN_KEYS`, `Navigation::Registry`, `SeedPermissions::CATALOG`) se verifica por separado, nunca se infiere una de la presencia de otra.
- **Cuatro caminos de acceso sobre el MISMO conjunto de tablas, nunca colapsados** (mensajería, v1.20.0, la versión más elaborada del principio ya establecido por anuncios v1.19.0): iniciar (RBAC, `conversation.compose`, controller `Communication::ConversationsController`) / bandeja+responder (participación, sin `authorize!`, `Communication::InboxController` + `Communication::MessagesController`, mismo query compartido `Communication::Inbox` que usa el portal del acudiente) / auditar (RBAC, `conversation.audit` — permiso **distinto** de compose a propósito — `Communication::ConversationAuditsController`). Cuatro controllers, no uno con ifs por acción.
- **Identidad de un participante/emisor de mensajería: `institution_user_id` (staff) XOR `guardian_user_id` (acudiente, FK directa a `users` — el MISMO handle que `guardian_students.guardian_user_id`, nunca `institution_user_id` aunque el acudiente también tenga esa fila)** — un `CHECK (num_nonnulls(institution_user_id, guardian_user_id) = 1)` real en la BD lo garantiza, no solo la validación de modelo (verificado con un test que inserta SQL crudo bypaseando ActiveRecord). Las columnas de identidad usan `on_delete: :cascade` (no `nullify`) — a diferencia de columnas de atribución pura como `conversations.created_by_institution_user_id` — porque una fila sin ninguna identidad violaría el CHECK; mismo criterio que `guardian_students.guardian_user_id` ya usa.
- **"Staff" para un selector de destinatarios (mensajería v1.20.0) significa específicamente un `institution_user` respaldado por una fila `StaffManagement::StaffMember`, NUNCA "cualquier `institution_user` activo"** — un acudiente TAMBIÉN tiene una fila `institution_users` (así puede loguearse; `Core::People::Resolver` la crea siempre), así que "todo membership activo" incluiría acudientes en la lista de "personal" por error. La señal correcta es la fila `StaffMember`, nunca "tiene cero `role_assignments`" (una persona de staff recién invitada, sin rol asignado todavía, se vería como acudiente bajo esa señal).
- **La elección de qué resolver usar como "hecho crudo" en el layering de tres capas es SEMÁNTICA, caso por caso — nunca automática por precedente.** `Schedules::ActiveTermEnrollmentScope` (matrícula por materia/término) es el resolver CORRECTO cuando la pregunta es académica (asistencia, boletines, `assignments`) — pero mensajería (v1.20.0) NO lo usa: "de qué estudiantes es responsable este staff para poder contactar a sus acudientes" es una pregunta de scope de negocio (`GroupManagement::Section#students`), no de elegibilidad académica. Antes de reusar el molde de un slice anterior, preguntar qué significa literalmente el resolver candidato, no solo si "ya se usó para algo similar".
- **El log de auditoría de una conversación (`conversation_audited`) se escribe si y solo si el accesor tiene `conversation.audit` Y NO es participante** — un participante que también audita leyendo SU PROPIA conversación (por cualquiera de los dos caminos: bandeja o ruta de auditoría) nunca genera el evento; la comprobación es siempre "¿existe una fila `conversation_participants` para este actor en esta conversación?", nunca "¿qué ruta usó para llegar aquí?". El rastro nunca se muestra a los participantes — solo existe en el visor RBAC-gated de `audit_events` (`IdentityAccess::AuditEventIndex::ACTIONS`, ahora también escrito desde fuera de `identity_access`, ver esa constante).
- **Cerrar una conversación es SIEMPRE soft** (`status`+`closed_at`+`closed_by_institution_user_id`) — los mensajes y participantes de una conversación cerrada nunca se borran, y una conversación cerrada simplemente rechaza nuevas respuestas (`Communication::MessageSender` chequea `conversation.active?`) hasta que un participante-staff la reabre. Un acudiente participante nunca tiene la acción de cerrar/reabrir expuesta — no por una verificación extra en el controller, sino porque su fila de participante usa `guardian_user_id`, nunca `institution_user_id`, así que su identidad simplemente no calza con la búsqueda de participante que usa `Communication::InboxController` (staff-only por construcción, no por chequeo).
- **La nota de un estudiante vive SOLO en `schedules::Assessment` — cualquier dominio que necesite calificar (`assignments`, v1.21.0, y cualquier futuro) cuelga por FK y REUSA ese único libro de notas, nunca crea un segundo almacén.** Hallazgo de recon que fijó el modelo: `Assessment belongs_to :enrollment` (no `:subject`) y el `score` vive DIRECTAMENTE en esa fila — no existe una tabla separada de "grade-entries". Un dominio que quiera calificar a TODO un roster (no una fila) necesita fan-out: una fila `Assessment` por matrícula, no una sola. El FK va del gradebook HACIA el dominio nuevo (`assessments.assignment_id`, aditivo nullable — mismo patrón que `enrollments.academic_term_id`), NUNCA al revés (`assignments.assessment_id` no puede representar un roster completo). Calificar es siempre un `UPDATE` de la fila ya fanned-out, nunca un `CREATE` paralelo — verificado con un test que confirma cero duplicados tras re-calificar.
- **Publicar una tarea (`assignments`, v1.21.0) hace fan-out INMEDIATO: una fila `Assessment` por estudiante del roster, con `score: nil`** — un assessment recién creado sin nota es un estado normal (mismo principio que `report_cards` ya estableció: "materia sin notas no aporta línea, nunca un cero"), verificado con un test dedicado que confirma que `ReportCards::Computation` no se rompe ni inventa un cero. El fan-out es idempotente por construcción (solo dispara en la transición borrador→publicada, nunca en un re-publish que no existe en este slice).
- **Archivar una tarea publicada es SIEMPRE soft (`status`), nunca `destroy` — solo un borrador (cero assessments fanned-out por construcción) puede eliminarse de verdad.** Archivar nunca toca los `Assessment` ya fanned-out (las notas ya dadas sobrevivan intactas) — verificado con un test dedicado.
- **El PRIMER write desde un portal (`assignments` entrega de texto, v1.22.0) se gatea por la MISMA relación que ya gatea la lectura del portal, nunca por RBAC — el scope de relación limita qué se puede escribir exactamente igual que limita qué se puede leer.** `Assignments::StudentSubmissionsController`/`GuardianSubmissionsController` resuelven la tarea vía `Assignments::StudentView.for(student)` — la MISMA query que ya acota `#index`/`#show` — antes de llamar al servicio de escritura; una tarea fuera de ese scope (ajena, `draft`, `archived`) da 404 en la escritura exactamente como daría 404 en la lectura, nunca un "denied" aparte. Ningún dominio de portal necesita nunca `authorize!` para escribir "lo propio" — self-service y portal comparten esta misma disciplina de que la puerta de lectura ES la puerta de escritura.
- **Una entrega (`Assignments::Submission`, v1.22.0) pertenece SIEMPRE al estudiante, sin importar quién la escribió** — un menor sin login (B1) entrega a través de su acudiente; `submitted_by_user_id` registra la atribución (quién tecleó), nunca redefine de quién es el trabajo (`student_id`). El acudiente resuelve al hijo por `GuardianScope` (nunca confía en `params[:student_id]` crudo) y LUEGO resuelve la tarea por `StudentView.for(ese hijo)` — dos scopes encadenados, no uno.
- **Entrega y nota son ejes independientes, pareados solo en lectura (`Assignments::GradingView`), nunca por FK cruzado.** `submissions` no tiene columna ni referencia a `assessment_id`/`assignment_id` de `schedules` más allá de su propio `assignment_id` (en-dominio); calificar nunca exige una entrega, entregar nunca crea o modifica una nota. Decisión de diseño explícita del slice: anclar la llave de la entrega a `assessment_id` habría acoplado `assignments`→`schedules` en un eje nuevo innecesario — el emparejamiento se resuelve en un servicio de lectura (mismo patrón que `Finance::AccountStatement`), no en el esquema.
- **Tardía es un flag calculado (`Submission#late?`, comparando `submitted_at` contra `due_date`), nunca un bloqueo** — una entrega después de la fecha de vencimiento se acepta igual; no hay ninguna validación que la rechace. Si un futuro slice quisiera bloquear entregas tardías, es una decisión de producto nueva y explícita, no algo que este slice decidió por inercia.
- **`group_work` (`assignments`, v1.23.0) es un toggle POR TAREA, criterio del docente — nunca una regla del sistema por grado** — settable mientras `draft`, bloqueado tras publicar por un `before_validation` en el MODELO (`Assignment#lock_group_work_after_publish`, defensa en profundidad: descarta el cambio sin importar qué acción/controller lo intentó), no solo por omitir el checkbox en la vista. El fan-out per-student de `Assignments::Publisher` (v1.21.0) es IDÉNTICO con o sin `group_work` — cada estudiante del roster sigue recibiendo su propia fila `Assessment`; lo grupal es puramente una conveniencia de calificación/entrega encima de ese mismo fan-out, nunca un mecanismo de fan-out distinto.
- **`Assignments::Submission` se generalizó (v1.23.0) a pertenecer a un estudiante XOR un `SubmissionGroup`, mismo patrón `CHECK (num_nonnulls(...) = 1)` que `conversation_participants` estableció en v1.20.0** — verificado con SQL crudo que la BD rechaza tanto "ninguno de los dos" como "ambos". Las filas per-student de v1.22.0 siguen cumpliendo el CHECK sin tocarlas (nunca hay ambigüedad porque una tarea es individual O grupal, nunca mixta, así que todas sus entregas son de un solo tipo). Las validaciones `uniqueness` de Rails sobre `student_id`/`submission_group_id` llevan `allow_nil: true` — sin eso, Rails compararía "es NULL" como una colisión de duplicado (a diferencia del índice único de Postgres, que trata múltiples NULL como no-colisionantes), rechazando incorrectamente la segunda entrega grupal de una tarea.
- **Los grupos de trabajo (`Assignments::SubmissionGroup`) son POR-TAREA, nunca reutilizables entre tareas** — un estudiante está en como máximo un grupo POR ASIGNACIÓN (índice único real `institution_id+assignment_id+student_id` en `group_memberships`), pero el mismo estudiante puede estar en grupos completamente distintos en otras tareas. `Assignments::SubmissionGroup` es un namespace deliberadamente separado de `GroupManagement::Section` (el "grupo de clase"/homeroom) — no existe ninguna colisión de nombres real en el esquema (`GroupManagement::Group` nunca existió; el modelo real siempre fue `Section`), pero el nombre elegido evita cualquier ambigüedad de todos modos.
- **Una nota grupal es SIEMPRE un bulk-set de los `Assessment` per-student ya fanned-out (`Assignments::GroupGrader`, que reusa `Assignments::GradeRecorder` por integrante) — NUNCA un almacén de nota grupal aparte.** El docente puede sobrescribir la nota de un integrante por el mismo camino per-student de v1.21.0; re-aplicar la nota grupal re-setea a TODOS los integrantes, incluidos los que tenían un override individual — no se guarda ningún baseline de "cuál fue sobrescrito" (aceptado para este slice, ver `HISTORIA.md`). La verdad del sistema de notas sigue siendo, sin excepción, `schedules::Assessment`.
- **La entrega compartida de un grupo la puede crear/editar CUALQUIER integrante, sin que el request necesite (ni acepte) un `group_id` explícito** — `Assignments::SubmissionRecorder` resuelve el grupo del actor a través de SU PROPIA `GroupMembership` (`assignment_id`+`student_id` del actor ya resuelto por relación), nunca de un parámetro que el cliente podría manipular. Esto es lo que hace que "cualquier integrante edita la MISMA entrega" funcione gratis: dos integrantes distintos, al llamar al recorder con sus propios `student` ya resueltos por `StudentSelfScope`/`GuardianScope`, resuelven al MISMO `SubmissionGroup` y por lo tanto a la MISMA fila `Submission`.
- **Un estudiante sin grupo asignado todavía es un empty state normal, nunca un error — ni en lectura (el portal muestra "aún no tienes grupo") ni en escritura (intentar entregar sin grupo 404, vía `GroupMembership.find_by!` fallando, nunca un 500).** La vista del portal NUNCA renderiza el formulario de entrega sin un grupo resuelto primero, así que ese 404 solo es alcanzable por un request manipulado/obsoleto — el camino feliz del usuario nunca lo ve.
- **GUARDRAIL NUEVO (v1.24.0): las tablas de Active Storage (`active_storage_blobs/attachments/variant_records`) NUNCA llevan `institution_id` ni RLS — es una excepción consciente al patrón multi-tenant de este repo, igual que el control plane.** Un futuro recon NO debe "corregir" esto agregándoles RLS — son tablas globales de Rails, compartidas por diseño. El límite de tenant lo sostiene SIEMPRE la tabla puente (`Assignments::SubmissionAttachment`, RLS `ENABLE+FORCE`): un blob solo es alcanzable resolviendo primero una fila de la tabla puente, que RLS ya scopea — nunca un `has_one_attached`/`has_many_attached` directo sobre un modelo tenant-owned sin una tabla puente propia (mismo razonamiento que ya documentaba `Core::RosterImportBatch` antes de que existiera este slice). Servir el archivo SIEMPRE pasa por un controller propio de la app que resuelve el scope de lectura primero (`StudentView`/`GuardianScope`/roster) y luego transmite los bytes — nunca las rutas firmadas default de Active Storage (`rails_blob_path`/`rails_representation_path`), que no tienen ningún conocimiento del tenant.
- **El content-type real de un adjunto (`assignments`, v1.24.0) se valida con la detección nativa de Active Storage (Marcel, magic bytes vía `identify: true`), nunca la extensión del archivo ni el header `Content-Type` que declara el cliente — y la validación vive en un service object (`Assignments::AttachmentAdder`), nunca en una gema de validación (`ActiveStorageValidations`) ni en el modelo.** Un archivo cuyo tipo real está prohibido se purga inmediatamente si ya fue adjuntado (nunca queda un blob huérfano en disco). Nota de calibración de tests: Marcel prioriza la extensión cuando el contenido no tiene firma mágica propia (texto plano, por ejemplo) — para probar "tipo real distinto del nombre" el fixture necesita bytes con una firma mágica real que contradiga la extensión (p. ej. un `.pdf` con bytes de GIF), no cualquier texto renombrado.
- **Los adjuntos de entrega (`Assignments::SubmissionAttachment`, v1.24.0) cuelgan de la MISMA `Submission` que ya existe — nunca crean una — y comparten su disciplina de escritura por relación: el gate de lectura (`StudentView`/`GuardianScope`, y para grupos la `GroupMembership` del propio actor) ES el gate de escritura, igual que v1.22.0/v1.23.0.** Adjuntar/quitar está permitido mientras la tarea no esté `archived` — "tardía" sigue siendo un flag calculado, nunca un motivo de rechazo (mismo principio ya establecido para la entrega misma).
- **GUARDRAIL NUEVO (v1.25.0): los materiales de una tarea (`Assignments::Material`) los adjunta el docente y se gatean por RBAC (`authorize!` sobre `assignment.manage`), a diferencia de los adjuntos de ENTREGA (v1.24.0), que se gatean por relación de portal.** Es el MISMO molde de tabla puente tenant-scoped + Marcel + serving por controller propio, pero con dueño `Assignment` (no `Submission`) y puerta de escritura de supervisión, no de portal — un actor sin el permiso recibe **403**, nunca el 404 de relación que usan los portales. La LECTURA de un material sí llega a estudiantes/acudientes por su scope de portal (`StudentView`/`GuardianScope`) porque los materiales son públicos para el roster de la tarea — pero un material en una tarea `draft` (o `archived`: `Assignment.published` excluye ambos) es inalcanzable para ellos sin ningún chequeo extra, porque la tarea misma no está en su scope hasta publicarse. **No confundir las dos superficies del mismo dominio: escritura = RBAC del docente; lectura de portal = relación.** `Assignments::AttachmentTypeCheck` (nuevo, `services/`) es el único lugar que valida tipo real/tamaño para AMBOS adjuntos (entrega y material) — evita que esa lógica, genuinamente idéntica en los dos, diverja con el tiempo; el cupo (5 vs. 10) y el dueño siguen siendo de cada Adder, nunca del helper compartido.
- **GUARDRAIL NUEVO (v1.26.0, cierra el track `assignments`): una rúbrica NUNCA almacena la nota — es un PRODUCTOR hacia `schedules::Assessment` vía `Assignments::RubricGrader`/`GroupRubricGrader` (que a su vez reusan `GradeRecorder`/`GroupGrader`, v1.21.0/v1.23.0, sin ningún cambio), igual que la nota grupal de v1.23.0 es una conveniencia sobre el mismo fan-out.** La EVALUACIÓN (qué nivel se marcó por criterio, `Assignments::RubricEvaluation`) sí es dato del dominio `assignments`, pero la nota calculada (`(Σ pts×peso)/(Σ máx×peso)×5`, redondeada a 1 decimal, `Assignments::RubricScore`) solo existe en `Assessment` — nunca un segundo lugar donde "la nota" pueda leerse o divergir. La ESTRUCTURA de la rúbrica se congela como snapshot jsonb en la `Assignment` al publicar (molde `price_tiers_snapshot`/`lines_snapshot` — un futuro recon no debe "simplificar" esto a una FK viva a la plantilla); editar la plantilla-biblioteca después NUNCA toca una tarea ya publicada — verificado comparando el snapshot byte a byte tras una edición de la plantilla viva. Pesos relativos, NUNCA obligados a sumar 100 (la fórmula es una razón). Niveles = columnas COMPARTIDAS de la matriz (nunca por-criterio); descriptores por celda, opcionales, visibles al acudiente por el gate de portal de siempre (relación, `StudentView`/`GuardianScope`, sin RBAC) para hacer la nota defendible. `evaluation_method`/`rubric_template_id` comparten el MISMO mecanismo de freeze que `group_work` (`before_validation` en el modelo, defensa en profundidad) — un futuro toggle por-tarea similar debe copiar este molde, no reinventar uno nuevo.
- **Nota operativa (v1.26.0): un `bin/rails runner` de depuración contra `RAILS_ENV=test` sin envolver sus writes en una transacción con rollback dejó un `Core::User`/`Core::Institution` huérfano COMMITTED en la base de test, rompiendo dos tests preexistentes no relacionados que asumen conteos globales en cero.** Cualquier script de depuración ad-hoc contra la BD de test debe envolver TODO en `ActiveRecord::Base.transaction do ... raise ActiveRecord::Rollback end` (o limpiar explícitamente al final) — nunca asumir que el proceso de `bin/rails runner` deja el estado como lo encontró. Detectado por la corrida completa de la suite (nunca solo el archivo de test en el que se está trabajando) — otro recordatorio de por qué el checklist de cierre siempre corre la suite ENTERA, no solo los tests nuevos.
- **GUARDRAIL NUEVO (v1.27.0): el scope de "lo MÍO" por PROPIEDAD de fila es un Query object, NO un `scope_type` de `role_assignments`** (`extracurriculars`, `Extracurriculars::ActivityScope`). El motor genérico de scope (`Authorization::Assignment#covers?`+`SCOPE_READERS`, columnas `role_assignments.scope_department_id`/`scope_grade_level_id`/`scope_group_id`) modela JERARQUÍA (el actor cubre un departamento/grado/grupo). La PROPIEDAD de una sola fila —"la actividad cuyo `instructor_staff_member_id` == mi propio `StaffMember#id`"— es una relación de IDENTIDAD, estructuralmente más cercana a un filtro self-service (`StudentSelfScope`) que al caso group-scoped de María: se filtra DIRECTO por el FK en el query (`WHERE instructor_staff_member_id = ?`), nunca agregando un `scope_type`/columna a `role_assignments` ni tocando `covers?`. Regla: antes de inventar un scope de rol nuevo, preguntar "¿esto es jerarquía (cubro un conjunto) o propiedad (esta fila es mía)?" — si es propiedad, es un query, no el motor. `activity.manage` (institución-wide, ve todo) vs `activity.instruct` (solo lo propio, por el query) es el split resultante; el coordinador se siembra con AMBOS para que la tile de nav (un permiso por tile) le llegue por `activity.instruct`.
- **GUARDRAIL NUEVO (v1.27.0): dinero de un dominio NUEVO nace en `*_cents bigint` (F6), pero el puente a `finance` (decimal legacy grandfathered) se cruza UNA vez, explícito, en el borde** (`extracurriculars`, `Activity#fee_amount`). `activities.fee_cents` es `bigint`; `charges.amount` es `decimal(12,2)` (grandfathered, ver el guardrail de dinero de `finance` arriba). La conversión es `BigDecimal(fee_cents) / 100` — BigDecimal exacto, NUNCA Float, y vive en UN solo método del modelo consumido por `EnrollmentCreator` al crear el `Finance::Charge`. No "unificar" los dos esquemas: coexisten a propósito (uno legacy, uno nuevo); solo se tiende un puente preciso en la frontera, documentado. Cualquier dominio nuevo que cobre vía `finance` copia este molde (cents propios → `BigDecimal/100` → `ChargeCreator`), nunca arrastra Float ni guarda su propio decimal.
- **GUARDRAIL NUEVO (v1.27.0): un cupo (invariante AGREGADo) se hace cumplir con lock transaccional en el servicio + un índice único PARCIAL como respaldo, NUNCA con un `validates uniqueness` ni un trigger** (`extracurriculars`, `EnrollmentCreator` + `idx_activity_enrollments_active_unique`). "Nº de inscripciones activas < capacity" no es expresable barato como CHECK declarativo (requiere un agregado por fila → trigger, y este repo no usa triggers): se serializa con `activity.lock!` + `COUNT` dentro del lock (misma disciplina que `Finance::ChargeCreator`/`PaymentRecorder`). La doble inscripción ACTIVA la bloquea un índice único PARCIAL `(institution_id, activity_id, student_id) WHERE status='active'` — predicado inmutable, no corre carreras como sí lo haría `validates uniqueness`, y el `WHERE status='active'` preserva el historial (varias filas withdrawn + una active), coherente con "append, nunca destruir". La baja es un flip a `withdrawn` + `withdrawn_at`, jamás un `destroy`.
- **GUARDRAIL NUEVO (v1.28.0): antes de modelar un dominio de portal "faltante", verificar contra el disco cuánto YA está construido — el backlog en prosa puede quedar desactualizado.** El ítem "portal del cuidador ampliado" (LINEAMIENTOS_MVP.md §7 ítem #9) sonaba a un slice grande (seis dominios a colgar); un recon exhaustivo confirmó que cinco de seis (`report_cards`/`finance`/`communication`/`assignments`/`calendar`/`extracurriculars`) ya colgaban reales — el hueco real era solo `attendance`. La señal correcta es la misma que ya usa el triage A/B/C/S de #4 (`OPEN_PROCESS.md` guardrail de v1.14.0): grep el disco (`app/controllers/portals/`, `config/routes.rb`), nunca asumir el tamaño del slice por la redacción del backlog.
- **GUARDRAIL NUEVO (v1.28.0): `Attendance::StudentView.for(student:, institution:)` es el único camino de lectura de asistencia para portal — mismo molde "una computación, N superficies" que `Finance::AccountStatement`/`ReportCards::Computation`/`Calendar::Timeline`.** Reusado sin cambios por `Portals::GuardianAttendanceController` (tras `GuardianScope.for(user).find(student_id)`) y `Portals::StudentAttendanceController` (tras `StudentSelfScope.for(user)`) — ningún controller reimplementa el `where`/`order`. No re-scopea por RBAC ni re-valida relación: asume que el caller YA resolvió `student` por la vía correcta, igual que todo query object de portal de este repo.
- **GUARDRAIL NUEVO (v1.28.0): un link de "cerrar sesión" en un layout compartido (renderizado en TODA página autenticada) debe ser un `link_to` con `data: { turbo_method: :delete }`, NUNCA un `button_to`/`<form>`.** `button_to` genera un `<form>` real; como el logout vive en el HEADER de `layouts/application.html.erb` Y `layouts/portal.html.erb`, un `<form>` ahí aparece en CADA página renderizada bajo esos layouts — rompiendo cualquier test existente que asuma "cero forms" en una superficie de solo lectura (`finance`/`group_assignments` portal specs, ambos rotos al probar esto con `button_to` antes de corregir). Mismo idioma que ya usaba `identity_access/roster_imports#show` (`ui_button` + `data: { turbo_method: }`) para una acción no-GET renderizada como enlace. Antes de este slice **no existía NINGÚN enlace de logout en toda la app** (ni shell de staff ni portal) — `SessionsController#destroy` siempre existió y funcionaba, solo no había forma de alcanzarlo desde la UI; `shared/_logout_link` lo cierra en ambos layouts a la vez, un solo partial.
- **GUARDRAIL NUEVO (v1.29.0): `IdentityAccess::Bootstrap::FirstAdmin` es el ÚNICO lugar autorizado a crear un `RoleAssignment` sin que ya exista uno previo autorizándolo** — invocado exclusivamente desde `Provisioning::ProvisionInstitution` (nunca desde un controller de request normal de una institución ya existente). El rol `institution_admin` que siembra recibe TODO `IdentityAccess::SeedPermissions::CATALOG` **excepto** `cross_tenant_reports.view` — ese permiso sigue reservado a `bi_auditor`/`edu_bi_reader` (rol Postgres `BYPASSRLS`), nunca a un rol de institución, mismo guardrail que ya protegía `SeedPermissions`. Un futuro slice que agregue una permission nueva al catálogo la hereda automáticamente (la lista se deriva de `CATALOG.keys`, nunca se hardcodea aparte) — verificado con un test que compara el set completo, no una muestra.
- **GUARDRAIL NUEVO (v1.29.0): provisionar una institución es UN flujo, UNA transacción — `Provisioning::ProvisionInstitution` compone `CreateInstitution` (ya existía) + `IdentityAccess::Bootstrap::FirstAdmin` (nuevo), nunca dos requests/pasos separados.** Si cualquier paso falla (institución inválida, admin sin nombre/email), la transacción entera revierte — nunca queda una institución "a medias" sin admin que pueda operarla. `ControlPlane::InstitutionsController#create` valida presencia de `admin_name`/`admin_email` ANTES de llamar al servicio (boundary del form), y deja que `ActiveRecord::RecordInvalid` (de `Core::Institution`/`Core::User`) burbujee hasta un único `rescue` — mismo molde que `IdentityAccess::PeopleController#create`, nunca una validación duplicada a mano de lo que el modelo ya valida.
- **GUARDRAIL NUEVO (v1.29.0): el transporte de correo real es SMTP genérico (`config.action_mailer.smtp_settings` leyendo `Rails.application.credentials.dig(:smtp, ...)` con fallback a `ENV["SMTP_*"]`), nunca una gema de proveedor (Postmark/SendGrid/SES/etc.).** Cualquier proveedor real expone un relay SMTP — este único transporte cubre a todos sin violar "sin gemas nuevas salvo bottleneck irresoluble". `development.rb` usa `delivery_method: :file` (`Mail::FileDelivery`, ya viene con la gema `mail` que ActionMailer depende — CERO gemas nuevas) escribiendo a `tmp/mails/`, en vez de caer silenciosamente al default `:smtp` contra `localhost:25` (lo que pasaba ANTES de este slice, sin que ningún `config/environments/*.rb` lo declarara explícito). Un futuro slice que quiera un proveedor de API dedicado (no SMTP) necesita justificar el bottleneck explícitamente, no asumirlo por conveniencia.
- **GUARDRAIL NUEVO (v1.30.0): todo call site de dominio que emita uso real usa `ControlPlane::Usage::Ingest.emit`, NUNCA `.call` directamente.** `.call` (S3a) lanza `Rejected` si el addon no existe o no está medido — correcto para el propio test suite de control plane, pero catastrófico dentro de un service object de dominio: un mensaje/entrega/pago real NO puede fallar solo porque el catálogo de billing no tiene ese addon sembrado en este entorno. `.emit` rescata específicamente `Rejected` (documentado, esperado) y deja pasar cualquier OTRO error — nunca se usa un `rescue StandardError`/`rescue Exception` genérico ahí, que escondería un bug real.
- **GUARDRAIL NUEVO (v1.30.0): la clave de idempotencia de un `Usage::Ingest.emit` debe anclarse al HECHO de negocio, nunca al id de una fila que puede regenerarse.** Regla general: si el servicio de dominio hace upsert/`find_or_initialize_by` (mensaje, asistencia, entrega, inscripción, cargo, pago), el id de la fila persistida ES estable y sirve de ancla directa. Pero si el servicio hace `delete_all`+`create!` en cada re-ejecución (`ReportCards::Publisher`, v1.17.0), el id CAMBIA en cada regeneración — anclar ahí re-facturaría cada regrade/republish. La clave correcta en ese caso es la tupla semántica que identifica el hecho (`"report_card:#{institution.id}:#{student.id}:#{academic_term.id}"`), nunca `report_card.id`. Antes de escribir un call site nuevo, preguntar: "¿esta fila se regenera alguna vez?" — si sí, la clave NO puede ser su id.
- **GUARDRAIL NUEVO (v1.30.0): un addon nace `metered:true` en `seed_catalog.rb` SOLO cuando su dominio ya tiene un `Usage::Ingest.emit` real cableado — nunca especulativamente "por si acaso".** `transportation` fue el caso de estudio que fijó esta regla: estaba `metered:true, unit:"check-ins"` desde S1 (2026), pero el dominio es Clase C (cero modelos reales) — ningún checkout que emitir jamás. Un seed que promete medición sobre un evento inexistente es engañoso (una factura nunca mostraría overage real, pero el catálogo sugiere que sí se mide). Se corrigió a `metered:false` en v1.30.0. Ver también M1 en `PROJECT_STATE.md` — cierra por dominio, nunca de una vez.
- **GUARDRAIL NUEVO (v1.31.0): el RBAC del plano de control es un mapeo ESTÁTICO rol→permisos en código (`ControlPlane::Authorization::PERMISSIONS_BY_ROLE`), NUNCA el esquema tenant-side (`roles`/`permissions`/`role_assignments` con columnas de scope).** Los platform admins son un puñado de personas de ops, no un sistema de autoservicio con jerarquías/departamentos que necesiten scope — replicar el esquema completo del inquilino aquí sería sobre-ingeniería sin ningún caso de uso real detrás. Si algún día un `platform_admin` necesita un alcance MÁS FINO que "los 3 roles fijos" (ej. "solo esta institución"), esa es una decisión de producto nueva y explícita, no algo a inferir de este guardrail.
- **GUARDRAIL NUEVO (v1.31.0): `platform_admins.role` nace con default `super_admin` a propósito — backward-compatible, no un placeholder a "corregir".** Todo admin creado antes de este slice (y todo test que crea uno sin especificar `role:`) mantiene el mismo acceso total que ya tenía; solo un admin creado EXPLÍCITAMENTE con `role: "billing_ops"`/`"viewer"` queda restringido. Cambiar este default a algo más restrictivo bloquearía silenciosamente a cualquier admin/test preexistente — no hacerlo sin auditar primero cada creación de `PlatformAdmin` en el repo.
- **GUARDRAIL NUEVO (v1.32.0): un job per-institución (`enqueue_for(institution)`, `institution_id` de `ApplicationJob`) NUNCA cuelga directo de `config/recurring.yml` — necesita un fan-out `*AllJob` que itere `Core::Institution.find_each` y encole/llame uno por institución.** Solo un job GLOBAL/self-contenido (sin GUC, tablas sin RLS — `ControlPlane::Usage::RollupJob` es el único caso hoy) puede colgar directo de una entrada recurrente, porque esa entrada solo invoca UNA clase con args fijos, nunca "una vez por institución". Antes de agregar una entrada nueva a `recurring.yml`, verificar si el job de destino es per-institución o global — si es per-institución, escribir el wrapper `*AllJob` primero.
- **GUARDRAIL NUEVO (v1.32.0): un test que verifica "el GUC no se filtró tras el job" debe hacer esa aserción INMEDIATAMENTE después de llamar al job — nunca después de otras llamadas a `within_tenant`/helpers que a su vez fijan el GUC sin resetearlo.** Encontrado real durante este slice: un test de `ExpireAllJob` fallaba "GUC leaked" porque sus PROPIAS aserciones posteriores (`within_tenant(@institution) { ... }`, `within_tenant(other_institution) { ... }`) dejaban el GUC fijado — el job en sí nunca filtró nada (confirmado aislado con `bin/rails runner`). `within_tenant` (el helper de test, en TODO el repo) deliberadamente NUNCA resetea — es un atajo para "corre esto bajo el GUC de X", no una garantía de limpieza — así que cualquier aserción "sin fuga" tiene que ir ANTES del primer `within_tenant` posterior a la acción bajo prueba, nunca después.
- **GUARDRAIL NUEVO (v1.33.0): `Subscription#end!`/`Entitlement#revoke!` DEBEN cerrar su fecha de fin (`ends_on`/`valid_until`) al terminar/revocar — nunca dejar una fila "inactiva" con rango abierto hasta el infinito.** Antes de v1.33.0, `revoke!` NO cerraba `valid_until` (asimetría con `end!`, que sí cerraba `ends_on` desde su propio slice) — invisible mientras nada chequeaba solapamiento de rangos, y se convirtió en un bug real en cuanto `institution_entitlements_no_overlapping_periods` (GiST) empezó a exigirlo. Cualquier modelo nuevo con el mismo patrón "activo hasta que se cierra" (status + rango de fechas abierto) debe cerrar la fecha de fin en la transición a inactivo, simétrico siempre.
- **GUARDRAIL NUEVO (v1.33.0): un exclusion constraint GiST sobre un rango con `valid_until`/`ends_on` NULL debe tratarlo como `'infinity'::date` (`COALESCE(fin, 'infinity'::date)`), nunca dejar el rango sin acotar en la construcción del `daterange`.** Un rango con extremo `NULL` no es lo mismo que "sin límite" para `daterange()` — Postgres necesita el literal `'infinity'` explícito para que el operador `&&` compare correctamente contra una fila open-ended.
- **GUARDRAIL NUEVO (v1.33.0): un `revoke!`/`end!` que ahora puede fallar por su propia restricción de negocio ("no el mismo día") necesita el MISMO `rescue ActiveRecord::RecordInvalid` con mensaje amable en el controller que ya lo dispara — nunca dejar que un modelo nuevo con esa regla explote como 500.** `EntitlementsController#revoke` copió el `rescue`/mensaje exacto de `SubscriptionsController#terminate` (v1.33.0) — mismo texto, mismo patrón, para que la UI nunca muestre un error crudo por una regla de negocio ya conocida.
- **GUARDRAIL NUEVO (v1.34.0): un KPI de solo lectura NUNCA reusa un servicio que persiste como side-effect solo porque calcula el mismo número.** `Core::Headcount::Snapshotter.call` es la fuente de verdad de "cuántos estudiantes activos tiene la institución", pero también escribe una fila `StudentHeadcountSnapshot` + un `Audit.log` cada vez que corre — llamarlo desde una vista de dashboard habría generado ruido de auditoría/snapshots duplicados en cada page view. `AnalyticsBi::InstitutionDashboard` reimplementa la MISMA query (`status == "active"`, documentado como tal) en vez de llamar al servicio — la única excepción legítima a "un solo camino de lectura" es cuando el "camino" candidato tiene un efecto secundario real que una lectura no debe disparar.
- **GUARDRAIL NUEVO (v1.34.0): un KPI que puede no tener datos todavía (nota promedio sin notas, asistencia sin registros) devuelve `nil`, nunca un `0`/`0%` — un cero real y una ausencia de datos son estados DISTINTOS que un lector no puede diferenciar si ambos se muestran igual.** La vista muestra "—" para `nil`, mismo principio que "boletín sin notas no aporta línea, nunca un cero" ya establecido en `report_cards`.
- **GUARDRAIL NUEVO (v1.34.0): `analytics_bi` se cierra en DOS slices separados a propósito — el tenant-scoped (`InstitutionDashboard`, RLS normal) y el cross-tenant (`CrossTenantReportRoster`, `BYPASSRLS`/`edu_bi_reader`) NUNCA en el mismo commit.** El segundo es la primera vez que esta app conectaría con un rol Postgres distinto para una query real — merece su propio recon/checkpoint de diseño (cómo se cambia de rol, cómo se audita cada acceso), no colarse como aditivo de un slice que ya iba de otra cosa.
- **GUARDRAIL NUEVO (v1.35.0): una conexión BYPASSRLS/cross-tenant vive en su PROPIO pool (`establish_connection` dedicado en una clase abstracta propia), NUNCA reconfigura ni comparte el pool primario de `edu_app_runtime`.** `AnalyticsBi::BiReaderRecord` es la única clase que conecta como `edu_bi_reader`; los modelos que lee bajo esa conexión (`AnalyticsBi::BiReader::*`) son clases DEDICADAS — nunca se reusan `Core::Institution`/`GroupManagement::Student` etc. del pool primario, que quedarían atadas a la conexión equivocada (esas clases heredan de `ApplicationRecord`, ligadas para siempre a `edu_app_runtime`).
- **GUARDRAIL NUEVO (v1.35.0): todo agregado cross-tenant DEBE agruparse por `institution_id` explícito (`GROUP BY`/`.group(:institution_id)`) — un `.average`/`.count`/`.sum` SIN agrupar sobre una conexión BYPASSRLS mezcla instituciones en un solo número, silenciosamente.** Este es el "doble filtro a nivel de aplicación" que `BI_DOCUMENT.md §6.1.2` exige: una vez bypasseado RLS, el agrupamiento explícito de la app es la ÚNICA defensa — no hay backstop de BD detrás. Todo query object cross-tenant futuro debe poder mostrar, para cada fila que retorna, exactamente qué institución la originó.
- **GUARDRAIL NUEVO (v1.35.0): un test que ejercita una conexión BYPASSRLS/cross-tenant NO puede ser un test transaccional normal — esa conexión es un proceso/sesión de Postgres DISTINTO y nunca ve la transacción abierta-y-nunca-comiteada del test.** Requiere `self.use_transactional_tests = false` en su propia clase, aislado del resto del archivo. Y con eso viene una responsabilidad real: TODO lo que el test crea es un COMMIT de verdad — un `teardown` explícito que borra cada fila, en orden seguro de FK, es obligatorio, no opcional. **Nunca usar `grant_full_entitlements` (crea los ~13 `Addon` de catálogo, GLOBALES, para todos los dominios) dentro de un test no-transaccional** — otorgar solo el ÚNICO addon que el test necesita. Encontrado real en este slice: la primera versión sin teardown dejó ~13 `ControlPlane::Addon` + instituciones/usuarios reales en la BD de test, chocando con decenas de tests no relacionados (violación de índice único) más adelante en la MISMA corrida de la suite — nunca detectado corriendo solo el archivo nuevo, solo al correr la suite COMPLETA (mismo recordatorio de siempre).
- **GUARDRAIL NUEVO (v1.35.0): dentro de un test SIN wrapper transaccional, el GUC de tenant ya NO "gotea hacia adelante" entre sentencias — cada `ActiveRecord::Base.transaction do end` es un COMMIT real, no un savepoint, y `Tenant::Guc` limpia el GUC al comitear.** Los helpers compartidos (`grant_role!`, `with_grants`) fueron escritos asumiendo la conveniencia de un test transaccional normal (donde el GUC de un `within_tenant` anterior sigue vigente porque el commit real nunca ocurre, solo se libera un savepoint). Un test no-transaccional que los reuse debe volver a fijar el GUC explícitamente (`within_tenant`) justo antes de llamarlos.
- **GUARDRAIL NUEVO (v1.36.0): la geometría del aula (`classroom_layouts`/`seat_assignments`) la POSEE `group_management`, no `analytics_bi` (decisión A2).** `analytics_bi` la LEE (query object propio con filtro de inquilino explícito, `AnalyticsBi::Lens::SpatialClassroomScope`) para la Lente 1, exactamente como ya lee `Schedules::Assessment`/`Attendance::AttendanceRecord` sin poseerlas (§5.1 de `BI_DOCUMENT.md`). Regla general de este dominio: si el dato ya tiene un dueño natural (aquí el aula física es de `group_management`, dueño de `Section`/`Student`), `analytics_bi` lo referencia por lectura, nunca lo re-posee. La ESCRITURA (reconfigurar, asignar asientos) vive en el dominio dueño, gateada por su permiso (`groups.manage`), no por `hps.classroom.view` (que solo abre la vista de lectura).
- **GUARDRAIL NUEVO (v1.36.0): un servicio que abre su propia transacción Y cuyo caller RESCATA una violación de constraint (ej. un `EXCLUDE`) DEBE usar `ActiveRecord::Base.transaction(requires_new: true)` (SAVEPOINT).** Sin `requires_new`, la transacción del servicio se une (joinable) a la del request (`TenantScoped` around_action); una violación de exclusión aborta la transacción ENTERA, y aunque el controller rescate la excepción y redirija, el `COMMIT` del around_action explota con "current transaction is aborted". Con `requires_new`, la violación revierte solo al savepoint y re-lanza limpio — el rescate del controller funciona de verdad. Encontrado real en `GroupManagement::SeatAssigner` (double-booking de asiento): el primer intento sin `requires_new` envenenaba el request. Mismo patrón aplicable a cualquier servicio futuro cuyo caller espere rescatar un `RecordNotUnique`/`StatementInvalid`.
- **GUARDRAIL NUEVO (v1.36.0): un test de MODELO que ejercita una violación de constraint bajo RLS debe fijar el GUC sobre la transacción de fixtures (no una anidada `ActiveRecord::Base.transaction` joinable) — o el `create!` que viola aborta esa transacción joinable en vez de caer a su propio savepoint, y `assert_raises` "pasa" pero deja la transacción abortada (falla la siguiente sentencia con "current transaction is aborted").** El `within_tenant` de estos tests de modelo hace solo `Tenant::Guc.set_local(inst.id); yield` (sin abrir transacción); la transacción de fixtures es `joinable: false`, así que el primer `save` de cada `create!` obtiene un SAVEPOINT real (comportamiento estándar de Rails en tests transaccionales) y la violación es recuperable. NO copiar el `within_tenant` de los tests de query object (que SÍ abre transacción) para un test que espera una violación de constraint.
- **GUARDRAIL NUEVO (v1.36.0): cerrar un rango efectivo-fechado a mitad de vida se hace con `Date.current`, no con "ayer" — `daterange(from, COALESCE(until,'infinity'), '[)')` hace `[from, hoy)` y `[hoy, ∞)` ADYACENTES (nunca solapan), y funciona incluso el mismo día en que se abrió el rango.** `effective_until = Date.yesterday` violaría el CHECK `effective_until >= effective_from` para una fila creada hoy y dejaría un hueco de un día. Usado en `ClassroomReconfigurer`/`SeatAssigner` (v1.36.0), mismo mecanismo que `Subscription#end!`/`Entitlement#revoke!` ya usaban con `Date.current`. Si un doc conceptual (ej. `BI_DOCUMENT.md §5.3`) esboza "ayer", el código gana con `Date.current` — anotarlo en el changelog del slice.
- **GUARDRAIL NUEVO (v1.36.0): un SVG helper de dominio va bajo `services/svg/` (colapsa a `<Dominio>::Svg::*`), NUNCA bajo `helpers/` — `app/domains/*/helpers` NO está en la lista de colapso de Zeitwerk (`config/application.rb` colapsa solo `{models,queries,services,jobs,policies}`), así que un archivo en `helpers/svg/foo.rb` sería `<Dominio>::Helpers::Svg::Foo`, no `<Dominio>::Svg::Foo`.** `AnalyticsBi::Svg::SeatGrid` vive en `app/domains/analytics_bi/services/svg/seat_grid.rb`. `BI_DOCUMENT.md §8` ofrecía `helpers/` O `services/` — se eligió `services/` justo por el colapso. Verificar SIEMPRE una constante nueva con `bin/rails zeitwerk:check` antes de darla por buena.
- **GUARDRAIL NUEVO (v1.37.0): dato T3 (clínico) NUNCA sale de `counseling` salvo como la proyección `care_aura` (cero PII clínica) — y el aislamiento se prueba a nivel de MODELO, no solo HTTP.** La frontera se hace cumplir por CONSTRUCCIÓN, no por convención: (1) `AnalyticsBi::CareAura` no declara NINGUNA asociación a `Counseling::*` (`authored_by_counselor_id` es FK a `institution_users`, identidad pura); (2) el camino de lectura del docente (`AnalyticsBi::Lens::AuraScope`) devuelve un `Data` de 4 campos (`kind`/`guidance`/`effective_from`/`effective_until`), jamás el AR model con asociaciones navegables — allowlist por construcción (§6.2 de `BI_DOCUMENT.md`); (3) `analytics_bi` menciona "counseling" SOLO en comentarios (grep de cierre obligatorio). Todo slice sensible de este dominio (5, 6, 8) lleva un test que hace un **SQL tap** sobre el camino de lectura y afirma cero query a las tablas del tier prohibido — la aserción estructural de asociaciones + el SQL tap juntos son "a nivel de modelo", no un simple 403 de HTTP.
- **GUARDRAIL NUEVO (v1.37.0): el split de permiso "dos lados" (`counseling.*` autor ↔ `hps.aura.view` docente, §4 de `BI_DOCUMENT.md`) reusa el key de ESCRITURA existente del dominio dueño — no se inventa uno nuevo.** La autoría del aura vive en `counseling` (donde el orientador ve el `Case` que la motiva) y se gatea por `counseling.write` (el key que YA existía en `SeedPermissions::CATALOG` para "Registrar notas de orientación") — inventar un `aura.author`/`counseling.aura` habría duplicado una capacidad ya nombrada. El lado LECTURA del docente sí es un key nuevo (`hps.aura.view`), porque es una capacidad genuinamente nueva (ver la proyección abstracta sobre el mapa). Regla: antes de agregar un permiso de escritura para un dominio existente, leer su catálogo — si un key ya describe la capacidad, reusarlo.
- **GUARDRAIL NUEVO (v1.37.0): un overlay ADITIVO sobre una superficie ya construida (aquí el badge de aura sobre la Lente 1 de Slice 2) se gatea por un SEGUNDO permiso decidido en el controller (`with_auras: can?("hps.aura.view", @section)`), y sin él la superficie base queda BYTE-POR-BYTE igual.** El `can?` del segundo permiso es lo único que decide si la tabla de la proyección (`care_auras`) se consulta siquiera (`SpatialClassroom#auras_for` retorna `{}` sin él) — un observador con solo `hps.classroom.view` nunca dispara ni una query a `care_auras`. Verificado con un test dedicado (`g.seat__aura, count: 0` + `assert_no_match` del texto de guía). No re-estructurar la superficie base para acomodar el overlay — extenderla aditivamente (un campo `auras: []` por defecto en el `Data` del asiento, un `aura_marker` que retorna `nil` sin auras).
- **GUARDRAIL NUEVO (v1.37.0): concurrencia de filas efectivo-fechadas "una activa por (X, tipo)" se hace cumplir con un índice único PARCIAL `WHERE effective_until IS NULL` (molde `extracurriculars` v1.27.0), no un `EXCLUDE gist`, cuando la regla es "una ACTIVA a la vez" y no "no solapamiento de rangos históricos".** `care_auras` permite varias auras activas de kinds DISTINTOS pero no dos del MISMO kind: `idx_care_auras_one_active_per_student_kind` sobre `(institution, student, aura_kind) WHERE effective_until IS NULL`. El `EXCLUDE gist` (Slice 2, billing v1.33.0) es para cuando importa que NINGÚN par de rangos —activos o cerrados— se solape; el índice parcial es más simple y basta cuando solo importa el estado activo. Elegir según la pregunta real, no copiar el `EXCLUDE` por precedente. La escritura sigue siendo append-only con `requires_new: true` (`AnalyticsBi::Aura::Projector`, mismo molde `SeatAssigner`) por si dos publicaciones del mismo kind corren en carrera contra ese índice.
- **GUARDRAIL NUEVO (v1.38.0): cuando una columna mutable sigue siendo un CACHÉ vivo legítimo (`students.section_id`, §5.2 lo deja así a propósito — muchos flujos ya lo leen) Y se introduce una historia append-only paralela (`student_placements`), debe existir EXACTAMENTE UN seam de escritura que mantenga ambas en lock-step — nunca un call site que toque la columna cache directo y otro que solo abra placements.** `GroupManagement::SectionReassigner` es ese único seam; `memberships_controller` (el único call site preexistente que mutaba `section_id`) se refactorizó para pasar por él en vez de que este slice le sumara una historia paralela por fuera. Cualquier flujo futuro que reasigne un estudiante de sección (import de roster, transferencia) DEBE llamar a `SectionReassigner`, nunca escribir `section_id=` directo.
- **GUARDRAIL NUEVO (v1.38.0): un backfill idempotente para un seam de escritura ya existente debe REUSAR la auto-sanación de ese seam, no reimplementar su lógica de creación.** `GroupManagement::PlacementBackfill` no construye `StudentPlacement.create!` por su cuenta — llama a `SectionReassigner.call(student:, section:)` por cada estudiante candidato, y es el propio seam el que decide no-op / auto-sanar / crear. Evita que backfill y flujo normal diverjan en qué cuenta como "ya consistente" (ej. si mañana `SectionReassigner` gana una nueva condición de no-op, el backfill la hereda gratis).
- **GUARDRAIL NUEVO (v1.39.0): un índice único PARCIAL sobre una columna que puede quedar NULL por una relación XOR necesita un índice separado POR CADA LADO de la XOR, nunca uno solo sobre un lado.** Postgres trata cada valor `NULL` como *distinto* de cualquier otro `NULL` dentro de un índice único — así que un índice único parcial sobre `giver_student_id` nunca detecta un duplicado cuando `giver_student_id` es NULL en todas las filas de ese lado (el caso del dador-acudiente en `peer_appreciations`, §5.4/v1.39.0). El boceto original del doc conceptual proponía un solo índice; la construcción real reveló el hueco. Antes de dar por bueno un índice parcial sobre una columna opcional-por-XOR, preguntar explícitamente "¿qué pasa cuando esta columna es NULL en TODAS las filas relevantes?".
- **GUARDRAIL NUEVO (v1.39.0): cuando `BI_DOCUMENT.md` (o cualquier doc conceptual) cita un molde existente que en realidad no existe en el repo, se GREPEA primero, se reemplaza por la pieza mínima defendible, y se documenta como corrección de referencia — nunca se asume que el molde citado es real sin verificarlo.** `assignments.requires_consent` (§5.4 punto 5) no existe en ningún lugar del codebase (`grep -rn requires_consent app db/structure.sql` → cero resultados) — era una referencia aspiracional/obsoleta. Reemplazada por `AnalyticsBi::CharacterProgramConsent`, un primitivo nuevo y mínimo. Regla general: "el repositorio es la fuente de verdad" (preámbulo de `BI_DOCUMENT.md`) aplica también a los moldes que el propio doc cita sobre SÍ MISMO, no solo a lo que dice sobre el estado global.
- **GUARDRAIL NUEVO (v1.39.0): un primitivo de consentimiento nuevo se modela como una tabla propia y mínima del dominio/feature que lo necesita PRIMERO, nunca como un framework general de Habeas Data hasta que un SEGUNDO dominio lo pida de verdad.** `character_program_consents` es deliberadamente angosto (un booleano-por-fecha de consentimiento de un acudiente para la participación de su hijo en el camino de pares de `analytics_bi`) — generalizarlo antes de tener un segundo caso de uso real habría sido spec especulativo. Si `counseling`/`extracurriculars`/otro dominio necesita consentimiento de acudiente en el futuro, evaluar EN ESE MOMENTO si conviene generalizar, no antes.
- **GUARDRAIL NUEVO (v1.40.0): antes de modelar CUALQUIER tabla nueva para una lente de autoservicio, verificar si ya existe la maquinaria de escritura de un slice anterior — una lente que solo LEE no necesita tabla propia.** La Lente 2 (Ficha de Personaje) cerró con CERO tablas nuevas porque el Slice 5 ya había construido `character_evaluations`/`peer_appreciations`/`character_program_consents`; el único trabajo real fue un read-model (`CharacterCard`) + un query object de picker (`SectionClassmatesScope`) + los controllers de portal. Separar "quién escribe el dato" (slice sensible, supervisión) de "quién lo lee" (slice de autoservicio, más simple) en dos slices distintos — como ya hicieron los Slices 4/5→6 — sigue pagando dividendos; no colapsar ambos en un solo slice "para ahorrar tiempo" cuando el dato ya es sensible.
- **GUARDRAIL NUEVO (v1.40.0): un valor ORDINAL/de posición que solo existe para manejar la geometría de un gráfico (radio de un radar, altura de una barra) NUNCA debe alcanzar el HTML como texto — verificarlo con una aserción explícita sobre el string renderizado, no solo con revisión de código.** `AnalyticsBi::Lens::CharacterCard#axes_from` calcula el ordinal de un nivel cualitativo (posición 0/1/2 dentro de los niveles de una dimensión) exclusivamente para que `AnalyticsBi::Svg::RadarChart` lo use como fracción del radio; el test correspondiente afirma con una regex (`/Dimensión:\s*\d/` o similar) que ningún número crudo aparece donde debería ir el nivel cualitativo, en vez de confiar en que "el código no lo imprime en ningún lado que yo haya visto". Aplica a cualquier lente futura (constelación/Lente 3, núcleo familiar/Lente 4) que traduzca un dato interno a geometría visual.
- **GUARDRAIL NUEVO (v1.40.0): el comentario mágico `<%# locals: (nombre:) %>` de "strict locals" de Rails en un partial debe quedar SOLO en su propia línea — cualquier prosa después de `locals:` hasta el `%>` de cierre se interpreta como parte de la firma del método compilado, no como comentario.** Encontrado real construyendo `app/views/portals/_character_card.html.erb`: una descripción del partial escrita en la misma línea que la declaración de locals produjo un `SyntaxErrorInTemplate` en tiempo de render (Erubi puro no tiene este problema — es un manejador específico de Rails). Cualquier partial que documente su propia firma de locals debe separar la prosa en un comentario `<%# ... %>` aparte, inmediatamente después.
- **GUARDRAIL NUEVO (v1.42.0): antes de modelar el acceso de una lente con scope `:department`/`:grade_level`/`:group`/`:route`, confirmar que el lector YA existe en `Authorization::Assignment::SCOPE_READERS` y que el recurso a proteger puede exponer esa columna — si el recurso NO la tiene todavía, es válido AGREGAR una columna (documentada como extensión del ERD) al recurso para que el lector la cubra, en vez de inventar un `scope_type` nuevo.** `affinity_taxonomy` ganó un `department_id` nullable exactamente por esto: el §4 de `BI_DOCUMENT.md` pedía scope "institución-wide o department_id" para la Lente 3, el lector `:department` ya existía y funcionaba (`staff_management`), pero ningún nodo de la taxonomía tenía cómo exponer un departamento. La corrección correcta es extender el recurso, nunca duplicar el mecanismo de scope.
- **GUARDRAIL NUEVO (v1.42.0): `IdentityAccess::PermissionCheck#scope_for` es la vía de filtrado A NIVEL DE ÍNDICE para un Query object que necesita "todo lo que cubre este scope" como un conjunto — reservar el `can?` por-fila para cuando la decisión es genuinamente por-recurso, no por-conjunto.** El motor ya documentaba ambos caminos como equivalentes desde P1, pero ningún dominio había adoptado `scope_for` hasta `AnalyticsBi::Lens::ConstellationScope` (Slice 7) — que decidió usarlo porque la pregunta real es "¿qué talentos ve este especialista?" (un filtro de conjunto sobre un índice), no "¿puede ver este talento específico?" evaluado fila por fila. Adopción incremental por dominio, tal como el propio comentario del motor ya anticipaba.
- **GUARDRAIL NUEVO (v1.42.0): la prueba de "esta búsqueda nunca toca datos de una persona" (§1.1.6) se hace con una aserción ESTRUCTURAL sobre el SQL generado (`assert_no_match(/students/i, scope.to_sql)`), no solo con revisión de código.** `AnalyticsBi::Lens::TaxonomySearchScope` (Slice 7) es la ÚNICA superficie de búsqueda de esta lente — el test no confía en que "el código no parece tocar estudiantes", genera el SQL real y confirma que la tabla `students` no aparece en absoluto. Mismo nivel de rigor que el aislamiento clínico de la Lente 5 (v1.37.0), aplicado aquí a la frontera "sin buscador de personas" en vez de la frontera clínica.
- **GUARDRAIL NUEVO (v1.42.0): una librería JS nueva bajo la relajación acotada de §10.3 se verifica con progressive enhancement REAL — un `import` dinámico envuelto en `try/catch`, nunca una carga que rompa la página si el pin falla o el JS no se ejecuta.** `constellation_controller.js` (Slice 7, primera librería JS del codebase) intenta `await import("cytoscape")` dentro de `connect()`; si falla por cualquier razón, el fallback server-renderizado (ya en el DOM, siempre) queda visible y la página sigue funcionando. Cualquier librería futura bajo esta misma relajación (Lente 4, núcleo familiar) debe seguir este mismo molde, no asumir que el pin siempre resuelve.
- **GUARDRAIL NUEVO (v1.42.0, proceso): cuando un agente delegado es cortado a mitad de tarea por un límite de gasto de la organización, el trabajo parcial se AUDITA pieza por pieza contra el código real antes de continuar — nunca se asume que "lo que alcanzó a hacer" es correcto solo porque el reporte final suena completo, y NUNCA se delega el resto a otro agente nuevo sin primero evaluar si completar el trabajo directamente es más seguro.** En este slice, la migración/modelos/query objects/servicio/controllers/permisos ya construidos se revisaron uno por uno (incluyendo verificar que `scope_for`/`SCOPE_READERS` citados en comentarios fueran APIs reales, no inventadas) antes de completar las vistas/seed/tests faltantes directamente, evitando el riesgo de otro corte a mitad de un segundo agente grande.
- **GUARDRAIL NUEVO (v1.43.0): una columna sensible en una tabla T2 (no clínica T3) se segrega del camino de lectura por CONSTRUCCIÓN — el read-model simplemente no la incluye en su payload — y se prueba con una aserción ESTRUCTURAL sobre el objeto/JSON serializado, nunca solo con revisión de código.** `custody_kind` (`guardian_relationships`, Lente 4) es una columna plana y real, pero `AnalyticsBi::Lens::FamilyGraph` nunca la expone — tercera instancia de esta disciplina exacta, tras el aislamiento clínico de la Lente 5 (v1.37.0, SQL tap) y la prueba "sin buscador de personas" de la Lente 3 (v1.42.0, `assert_no_match` sobre SQL). El patrón general: identificar la frontera sensible, construir el read-model para que sea estructuralmente incapaz de cruzarla, y probarlo generando el artefacto real (SQL/JSON/HTML) y buscando la ausencia, no confiando en que "el código no parece hacerlo".
- **GUARDRAIL NUEVO (v1.43.0): antes de introducir una librería JS nueva bajo una relajación acotada como §10.3, verificar si una YA PINEADA por un slice anterior sirve para el caso nuevo — reusarla es la aplicación literal de "aburrido sobre ingenioso" a una decisión de DEPENDENCIA, no solo a una decisión de código.** El grafo orbital de la Lente 4 reusó el MISMO Cytoscape.js que la Lente 3 ya había pinneado y vendorizado (v1.42.0) en vez de evaluar una librería "más especializada en grafos familiares" — cero pin nuevo, cero archivo vendorizado nuevo, mismo molde de progressive enhancement copiado casi literalmente.
- **GUARDRAIL NUEVO (v1.43.0): una lente de supervisión con scope institución-wide-ÚNICAMENTE (sin lector de scope más pequeño posible) NO necesita — y no debe tener — una entrada en `Navigation::Registry` si su único punto de entrada natural sería un índice/directorio navegable de personas.** `hps.family.view` no tiene ítem de nav propio; se alcanza por un enlace `can?`-gateado agregado a una superficie per-estudiante YA EXISTENTE (`group_management/students#show`), el mismo criterio que ya usó la autoría de auras de la Lente 5 (v1.37.0, "sin entrada de nav nueva, alcanzada por la tile ya existente"). Antes de agregar una entrada de nav de nivel superior para una lente nueva, preguntar: "¿el índice que esta entrada implica sería, en la práctica, un buscador de personas?" — si la respuesta es sí, el punto de entrada correcto es un enlace desde una superficie per-persona ya existente, no un ítem de nav nuevo.
- **GUARDRAIL NUEVO (v1.46.0): un capstone de síntesis que lee de VARIAS lentes/dominios sensibles sin poseer tabla propia debe REVALIDAR el permiso ORIGINAL de cada señal individual — el permiso-paraguas de la superficie sintetizadora NUNCA sustituye esa revalidación.** `hps.early_warning.view` (Lente 6) solo abre la superficie; cada señal (convivencia, lazo fraternal, aura) vuelve a chequear `disciplinary_logs.manage`/`hps.family.view`/`hps.aura.view` por su cuenta. Mismo criterio que `StudentSupport::SupportDashboardController` ya aplicaba desde antes — con esta segunda instancia real, queda formalizado como el patrón general para CUALQUIER dashboard/vista de síntesis futuro que agregue datos de múltiples dominios sensibles: nunca un solo permiso-paraguas gatea todo lo que se muestra.
- **GUARDRAIL NUEVO (v1.46.0): una superficie que sintetiza señales sensibles sobre personas (riesgo académico, convivencia, familia) NUNCA envía nada por sí misma — siempre un enlace a un canal de comunicación YA EXISTENTE donde un humano decide, jamás un job/cron/mensaje auto-generado, incluso cuando la regla de negocio real de la alerta todavía no está confirmada.** `AnalyticsBi::EarlyWarningsController` solo enlaza a `communication`'s `conversation.compose` (sin tocar su lógica interna) y a la Lente 4 — cero automatización de contacto con una familia. Aplica a cualquier futura capa de "notificación"/"alerta" sobre datos de estudiantes: la decisión de contactar a alguien sobre información sensible es siempre humana.
- **GUARDRAIL NUEVO (v1.46.0): cuando el owner autoriza explícitamente proceder sin una regla de negocio confirmada, los umbrales/heurísticas resultantes se documentan como PLACEHOLDER tanto en el código (constantes con comentario explícito) como en el doc conceptual (una decisión abierta marcada, nunca "resuelta") — nunca se presentan como si fueran una decisión de producto ya validada.** `HEAT_RISK_THRESHOLD`/`RECENT_DISCIPLINARY_WINDOW_DAYS`/`TRIGGER_MIN_SIGNALS` (Lente 6) y, antes, los umbrales de `SiblingBondAlert` (Lente 4) siguen este mismo molde — la próxima vez que el owner defina una regla real, estos son los primeros lugares a revisar.
- **GUARDRAIL NUEVO (v1.49.0): agregar un `scope_type` nuevo a `Authorization::Assignment::SCOPE_READERS` NO es lo mismo que conectarlo al motor real — verificar SIEMPRE que `IdentityAccess::PermissionCheck#scope_type_for`/`#scope_id_for` reconozcan el `scope_type` nuevo Y que `role_assignments` tenga la columna `scope_*_id` real que lo respalda, antes de asumir que un grant scoped a esa dimensión puede existir en producción.** `:route` estuvo en `SCOPE_READERS` desde el slice original (stub) de `transportation`, pero `role_assignments` nunca ganó `scope_route_id`, y `scope_type_for` ni siquiera lo contemplaba (caía a `:group` por default) — "un conductor ve solo su propia ruta" solo existía como un override de test (`Authorization::StubResolver`), nunca como un `RoleAssignment` real. Cerrado explícitamente en v1.49.0: columna nueva (FK a la tabla dueña, `on_delete: cascade`, mismo patrón que `scope_department_id`/`scope_grade_level_id`/`scope_group_id`) + rama nueva en `scope_type_for`/`scope_id_for` + `idx_ra_unique_scope` recreado incluyéndola. Antes de dar por sentado que un scope dimension "ya existe" porque aparece en `SCOPE_READERS`, confirmar las tres piezas (columna real, engine real, índice único que la incluya) por separado — la presencia de una NUNCA implica la de las otras dos.
- **GUARDRAIL NUEVO (v1.50.0): cuando un stub retirado dice "REFLEJA un flag, nunca lo calcula", la versión real debe CALCULARLO de verdad sobre el conjunto COMPLETO (institución-wide), nunca sobre el subconjunto ya filtrado por el scope del caller.** `Schedules::MeetingPatternPresenter` computa el conflicto de cada `meeting_pattern` contra TODOS los patrones de la institución antes de que `ScheduleScope`/`TimetableScope` apliquen su propio `can?` — invertir el orden (filtrar primero, calcular después) haría que un conflicto con una fila fuera del scope del caller (ej. la clase de OTRA sección compartiendo el mismo salón) nunca se detectara. Regla general: "una computación, N superficies" (`Attendance::StudentView`, `Finance::AccountStatement`) también aplica aquí — el cálculo derivado vive en un solo lugar, ANTES del filtro de scope, nunca después.
- **GUARDRAIL NUEVO (v1.50.0): antes de modelar una tabla nueva como una jerarquía compartida (ej. un grid de "periodos" institución-wide), verificar si el dato REAL que se está reemplazando ya la contradice.** El stub retirado de `schedules` (`ScheduleEventRoster`) nunca asumió un bloque de horas compartido — cada evento llevaba su propio día/hora ad-hoc, con materias distintas usando combinaciones libres. Migrar a un modelo PLANO (`meeting_patterns.day_of_week`+`starts_at`/`ends_at` directo) en vez de inventar `periods` fue la lectura correcta de esa evidencia, no una simplificación arbitraria — un molde más rígido habría sido sobre-ingeniería sin ningún dato que lo pidiera. Regla: el dato de ejemplo del stub que se retira es evidencia real de la forma esperada, no solo un placeholder a ignorar.
- **GUARDRAIL NUEVO (v1.50.0): "permitir y calcular" vs. "bloquear con EXCLUDE gist" es una decisión de producto explícita, no un default — depende de si el invariante real es "nunca debe existir" o "debe ser visible cuando existe".** `classroom_layouts`/`seat_assignments` (v1.36.0) bloquean solapamiento con `EXCLUDE gist` porque un asiento doble-asignado es SIEMPRE un error de datos. `meeting_patterns` (v1.50.0) permite doble-booking de salón a propósito — un horario real a veces necesita una excepción temporal, y el propio stub retirado marcaba el conflicto como algo a MOSTRAR, no a rechazar. Antes de copiar el molde `EXCLUDE gist` "porque ya se usó para overlap en otro dominio", preguntar cuál de las dos semánticas aplica aquí.
- **GUARDRAIL NUEVO (v1.55.0): una entidad PROSPECTIVA (todavía no es la identidad real que Finance/RBAC/etc. modelan) nunca es cobrable/asignable a través del mecanismo real de esa identidad — verificar la FK requerida antes de asumir que un servicio existente puede reusarse contra ella.** Un `Admissions::Applicant` no es un `GroupManagement::Student` (`Finance::StudentAccount.student_id` es NOT NULL, `on_delete: :restrict`, sin precedente de pagador prospectivo en todo el codebase) — el overview original asumía que se podía facturar la cuota de inscripción al radicar, contra una entidad que ni siquiera existe todavía como fila real. La corrección: el cobro real (`Finance::Charge`) nace SOLO en `Admissions::AcceptanceConverter`, una vez el `Student` (y su `StudentAccount`) existen — la solicitud solo guarda un SNAPSHOT del monto (`fee_cents`) mientras tanto. Regla general: antes de diseñar "cobrar/asignar/vincular X a Y" para una entidad todavía no-identidad (aspirante, invitado, borrador), grepear el modelo real de Y en busca de FKs NOT NULL que asuman que X ya es una fila consolidada — si la asunción no se sostiene, el cobro/vínculo se difiere al momento en que X se convierte en esa identidad real, nunca se inventa un camino paralelo.
- **GUARDRAIL NUEVO (v1.56.0): una página PÚBLICA (sin sesión, sin RBAC) que debe ocultar CAMPOS específicos de un registro por lo demás legible NUNCA gatea a nivel de vista — retorna un `Data.define` allowlist desde un query object/presenter dedicado, molde exacto `AnalyticsBi::Lens::AuraScope` (aislamiento clínico, guardrail v1.37.0), y el controller/vista SOLO tocan ese `Data`, nunca el AR record.** Toda otra superficie de lectura de este repo (portales, `care_auras`) gatea FILAS completas por RBAC/relación — nunca necesitó redacción de CAMPO porque el lector siempre podía ver el registro entero. El tracker público de `admissions` (Increment 3) es el primer caso genuinamente distinto: cualquiera con el token ve la solicitud, pero `private_notes`/`evaluator_institution_user_id` deben quedar invisibles SIEMPRE. Confiar en "la vista simplemente no imprime esos campos" es frágil — un partial futuro que itere `application_step.attributes` los expondría sin que ningún test lo detecte a menos que sea explícito. La forma correcta es la MISMA que ya protege T3 clínico: el objeto que cruza el límite público NO TIENE accessor para el campo prohibido, así que no puede filtrarse por descuido. Probado con un test estructural (`Result.members`/`StepView.members` no incluyen el símbolo prohibido) MÁS un test de integración de extremo a extremo (datos sensibles poblados de verdad, aserción `assert_no_match` sobre el body real) — igual que el SQL tap de `care_auras`, un solo tipo de prueba no basta.
- **GUARDRAIL NUEVO (v1.60.0): cuando datos tenant-sensibles necesitan sobrevivir de un request a un job async, la columna que los guarda debe estar en una tabla CON RLS — nunca Active Storage ni el argumento del job, ambos backed por tablas GLOBALES sin RLS.** `Core::RosterImportBatch` ya rechazaba explícitamente `has_one_attached :file` por esta razón (v1.7.0); mover el parse+validar de `RosterImport` a un job async (`ParseAndValidateJob`) reabrió la misma pregunta desde otro ángulo — el CSV tiene que llegarle al worker DE ALGUNA forma. `solid_queue_jobs` es tan global/sin-RLS como `active_storage_blobs`, así que serializarlo como argumento del job (cifrado o no) es el MISMO patrón de riesgo con otro nombre. La solución fue una columna (`pending_content`, `encrypts` no determinístico) en la MISMA tabla tenant-scoped que ya tiene RLS, limpiada por el job apenas la lee — el job en sí solo recibe el `id` (opaco) como argumento. Regla general: antes de decidir CÓMO pasarle contenido sensible a un job, listar en qué tabla física va a aterrizar en cada punto del camino (columna propia vs. argumento serializado vs. adjunto) y verificar RLS en cada una — "está cifrado" no compensa "está en una tabla sin RLS" si el objetivo es que solo el tenant dueño pueda leerlo ni siquiera con acceso de rol.

